# 交接：2026-09-22（版本号 1.0.0 之后）

> **当前状态：M0–M3 全部结项，15 项挂起全部清零，用户已实测确认。** 没有拦路的待办。
> 包已升到 **1.0.0**（ADR 041），用户已拿到新包待验。
> 下一个会话大概率是**开新功能**，不是接着修东西 —— 见 §3 的开工步骤。
> 要发给别人时看 §2（**只能发 zip，不能单发 exe**）。
> 本文取代 `HANDOFF-2026-09-21.md`。

## 0. 一句话

项目从"打磨运行时"转入"加新功能"阶段。便携版最新包 `dist-win/desktop-pet/`（373 MB；
分发用 `desktop-pet-1.0.0-win-x64.zip`，154 MB，**名字从 `package.json` 的 `version` 派生**）。
退出崩溃、自启值名两件事已由用户实测确认收口；版本号 1.0.0 已落地（ADR 041）。

## 1. 用户已确认（2026-09-22）

> 「我运行了，然后点了几次，没有错误框。退出也没有错误框。开启自启已勾选。」

⇒ ADR 035（退出崩溃）与 ADR 036（自启值名）双双**经用户实测收口**，**挂起 15 项全部结项**。
宠物可能需要用户重新双击启动一次（验证新包时起过的实例都已关闭）。
**2026-09-22 之后新加一件待验项**：右键托盘看菜单最上方那一行 `版本：1.0.0`
（只读小字），以及悬停提示里**不该**出现版本号 —— 见 `docs/acceptance.md` 第 12–14 条。

## 2. 打包与分发（要发给别人时看这里）

**分发单位是「整个目录」，不是那个 exe。**

```
dist-win/desktop-pet/                  373 MB、232 个文件  ← 这一整个目录才是"软件"
dist-win/desktop-pet-1.0.0-win-x64.zip 154 MB             ← 发给别人的那**一个**文件
```

- 生成：`npm run make:portable`（只出目录）或 `npm run make:portable:zip`（顺带压 zip）。
- **zip 名从 `package.json` 的 `version` 派生**（ADR 041）：改版本号即改名，
  **目录名刻意不带版本** —— 自启注册表里记的是绝对路径，目录改名会让老用户的条目失效。
- **`desktop-pet.exe` 不能单独发人** —— 它只是入口，启动时要在**同级目录**找
  `resources/`、`*.dll`、`*.pak`、`locales/`；只发一个 exe 出去，对方双击的症状是
  **毫无反应且没有任何报错**（这个症状本项目实测踩过）。详见 ADR 037。
- 实测过：把 zip 解压到全新目录能直接启动（窗口显示、精灵图就绪、零未捕获异常）。
- **进 zip 的文件名一律 ASCII** —— 非 ASCII 名会被 bsdtar 写坏（`说明.txt` 曾变成乱码，
  现为 `README.txt`）。同理：`.bat` 纯 ASCII、exe 版本资源的值 ASCII。
- **顺手核对 exe 版本资源**（打包后一条 PowerShell 命令）：
  `FileVersion` / `ProductVersion` 应为当前版本、`CompanyName`/`LegalCopyright` 为 `desktop-pet`、
  `OriginalFilename` 为 `desktop-pet.exe`；**`ProductName` 必须仍是 `desktop-pet`**
  （它决定自启注册表值名，动不得）。

## 3. 开新功能时怎么开工

> **2026-09-22 补：第一件新功能已完成 —— 控制条动作排 / 手动把玩（ADR 038）。**
> 面板第 3 段现在是 9 个动作按钮（点一下就演、手动不出气泡、左右走真的走）。
> **不要再重做它**；下一轮问用户**下一件要什么**，并提示可以打包一次试手感
> （`npm run make:portable`）。详见 `PLAN.md` §3b 与 `docs/decisions/038-*.md`。

1. 新建对话，**工作区选到 `desktop-pet` 这个目录**（不要用默认目录，否则文件与记忆会分散）。
2. 读 `PLAN.md`（§3 状态表 + §4 挂起清单）。
3. **按 `AGENTS.md` 的路由表**确定"你要动什么 → 读哪份约束 → 跑哪些判据"，
   不要去全读 `docs/constraints/`。
4. 做完按**收工四件事**：PLAN.md §3/§4 → 新增 ADR → journal → `git commit`。
   完整叙述追加到 `docs/changelog.md`（§7 只留最近三条）。
5. **做完就打包给他 —— 不要问"要不要打包"**（2026-09-22 用户明确要求）。
   > 「你改完了之后不打包的话，我也没法测试吧。所以碰到这种加了功能或者进行了修复
   > 必须要我测试的时候，你肯定还是直接帮我打包了比较好」
   >
   ⇒ 本条**取代**之前"阶段结束后视情况再打包"的说法。用户唯一的验证途径是双击那个 exe，
   不打包等于本轮没交付。命令：**先 `node tools/run-build.cjs`（打包脚本自己不构建），
   再 `node tools/make-portable.mjs --zip`**（约 1–2 分钟，产物在 `dist-win/`，已 gitignore）。
   打完在回复里写清"产物在哪、让他测哪几条"（验收清单见 `docs/acceptance.md`）。

**注意**：打包前要**先关掉正在运行的便携版**，否则 exe 文件被占用。
关的时候按进程名 `desktop-pet.exe` 杀，**绝不能按 `electron.exe`** —— 宿主 WorkBuddy 自己也是 Electron。

## 4. 已建立的三条"自动守门"，新功能别绕过它们

| 守门 | 位置 | 管什么 |
|---|---|---|
| 定时器生命周期 | `npm run check:timers`（并挂在单测第 ⑳ 节） | 裸 `setInterval` / `*Watch` 未接返回值 → 直接失败。防止"对象已死、定时器还在撞它"（ADR 035） |
| 状态仲裁全量断言 | `npm run test:status`（**313 项**） | 状态机、气泡出口、控制条策略、渲染层不许自带判据 |
| 窗口/托盘/命中真实探针 | `spikes/m2-*/run*.mjs` | 见 AGENTS.md 路由表。**跑完要看日志里有没有 `[uncaught]`** —— 只核对"进程消失"曾经放过一个用户可见的崩溃 |

## 5. 不要重新做的事

- **不要再提议**（用户已明确）：M2 ⑤ 多宠物切换、行为层忙碌动作与跨屏漫游、
  **启动优化**（"等改动少了再优化"）、**Proma 通道**（仍冻结）。
- **不要再讨论**"打包形态"（ADR 033 定为免安装绿色目录）与"要不要上 electron-builder"
  （只为改 exe 版本资源引入了 `rcedit` 一个小依赖，不是整个打包器）。
- **不要再猜**开机自启值名的来源 —— 三条候选已逐个实测，只有 exe 版本资源有效（ADR 036）。
- **不要再在 `reg.exe` 上浪费轮次** —— 会被安全策略拦，读注册表一律走 PowerShell。

## 6. 环境事实（本轮新增两条）

- **PowerShell 读注册表**：`(Get-ItemProperty -Path "HKCU:\...\Run" -Name "<值名>").'<值名>'`
  —— 直接取属性。**不要用 `Get-ItemProperty` 的表格输出，长值会折行**，解析出来是残缺内容。
- **rcedit 是原生 exe**（`node_modules/rcedit/bin/rcedit.exe`）：含空格路径用
  `spawnSync(exe, args, { shell: false })`，走 shell 会被空格截断；
  写进版本资源的值**一律 ASCII**（中文读出来是乱码）。
- 原有：Bash shim 缺 head/tail/dirname；npm 可用；Edit 偶发"报告成功但文件没变"；
  路径含空格要用 `fileURLToPath`；跑 Electron 前删 `ELECTRON_RUN_AS_NODE`；
  `dist/`、`dist-win/` 已 gitignore。

## 6b. 本轮（动作排）新补的三条操作纪律

- **带反引号的字符串不能用 `node -e` 拼** —— 被 bash 吃掉，报 `Unterminated template`。
  正确做法：**先用 Write 写一个临时文件，再让 node 读它 append**（改文档/批量替换都适用）。
- **探针脚本改完先 `node --check`，再看 electron 的 stderr**：新增一个与既有变量重名的
  `const` 就会 `SyntaxError`，electron **秒退且不写日志**，而驱动只看到"报告未生成" ——
  光看"报告没出来"根本定位不到。
- **点动作按钮后要等够时长**再断言结束；`dwellMs` 从 sidecar 读，不要在探针里写死 6000。

## 7. 常去的面

- 状态/进度：`PLAN.md`｜决策：`docs/decisions/INDEX.md`（一行一条，**已决策的不要再讨论**）
- 编年史：`docs/changelog.md`｜操作手册：`docs/how-to-run.md`｜验收清单：`docs/acceptance.md`
- 运行时观测：`~/.desktop-pet/pet.log`（含 `isPackaged` 行）、`events.jsonl`、`status.json`、`prefs.json`
- 延迟自测：`node tools/measure-latency.mjs --since=60`（当前中位 **62ms**）

## 8. 2026-09-23 补记（在 §7 之后读）

- **新修**：系统「**减少动态效果**」此前把**整个渲染层的帧推进**关掉了（连用户显式点击的动作也吞），
  症状是"点哪个动作都是一张静态图"。现在它只停"它自己乱跑"，**动画照常播**（ADR 043）。
- **新探针**：`spikes/motion/`（`node spikes/motion/run.mjs`）—— 用
  `--force-prefers-reduced-motion` 在**本机**复现那台机器的条件，采样渲染层的**画面序列**。
  **这台机器上唯一能验"动画到底有没有在跑"的手段**，改渲染层时记得跑。
- **新工具**：`tools/zip-dir.mjs`（纯 Node 的 zip 实现）。`make-portable.mjs --zip` 已改用它，
  并在压完后**回读魔数**。**别再改回系统 `tar`** —— 它产出过 tar 却报成功（ADR 044）。
- **守门改动**：`tools/check-timers.mjs` 现在导出纯函数 `checkTimers()`，单测**直接调用、不再 spawn**
  （沙箱会间歇 `EBUSY`，让无关断言变红）。CLI 用法不变。
- **本沙箱的两个限制（换机器不一定有）**：
  ① `spawnSync` 起 exe 会被间歇拒绝（`EBUSY`）—— 构建被拒时用 `.workbuddy/build-inproc.cjs`
  （进程内调 tsc / esbuild）；② **便携版"长期驻留"验不了**（detached spawn 的进程会被沙箱回收），
  只能验"它完整启动并初始化完成"，**长期驻留要用户双击确认**。
- **产物**：`dist-win/desktop-pet/` + `dist-win/desktop-pet-1.0.0-win-x64.zip`（**156.8 MB**）。
  若见到 **373 MB** 的 zip，那是坏文件（其实是 tar），别发。
- **下一件事**：用户尚未指定。**先问他**；并提示这一版可发给那位朋友复验（他是缺陷现场）。
