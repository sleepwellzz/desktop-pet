# 变更日志（编年史）

> 2026-09-18 从 `PLAN.md` §9 整段迁出（内容未改，只搬位置）。
> 按日期追加即可；`PLAN.md` 只留最近三条。

---

- **2026-09-21**：**启动脚本第二次修正（ADR 029 §6）**：`-ArgumentList '%CD%'` 里的路径含空格
  被 PowerShell **按空白拆参数** ⇒ electron 报 `Unable to find Electron app at <工程目录>`。
  改用 `-ArgumentList '.'` + `-WorkingDirectory '%CD%'`。**用户实测通过**：窗口自己关、宠物留在托盘。
  这个 bug 值得记的是**我的验证方式**：用不含空格的临时路径测启动方式，三轮全绿却没碰真实路径
  —— 与"判据测的量不支持结论"是同一类错误（本项目一轮内出现三次变体）。
- **2026-09-21**：**双通道逐出已实施（ADR 030）** —— `#2` 结项。规则：**按通道优先级，不按到达顺序**，
  hook 高于被动源；低优先级上报被**丢弃**（在所有记账之前）；`holdMs` 10 分钟释放作安全底线；
  无 `origin` 的人工喂状态永远放行。配置在 `desktop-pet.json → statusTimeouts.dominance`。
  顺带确认一条实施顺序：**告警必须在逐出之前**（否则逐出后冲突再也不报警）。单测 **319 项**。
- **2026-09-21**：**无控制台启动的修正（ADR 029）** —— **推翻 ADR 028 的结论**。
  用户实测"关掉 bat 窗口宠物就消失"，我用三种启动方式各起 3 个进程实测复现：
  `start` 全部被带走、`Start-Process` 与 `DETACHED_PROCESS` 全部存活 ⇒ 改回 `Start-Process`。
  **我错在判据不纯**：测的是"启动时会不会新建控制台"，要证明的是"有没有附加到 cmd 已有的控制台"
  —— 两个不同命题（本项目第三次栽在这上面）。
- **2026-09-18**：**无控制台启动（ADR 028）**：新增 `启动桌宠-后台.bat` + 日志落盘
  `~/.desktop-pet/pet.log`。**⚠️ 该版启动方式已被 ADR 029 修正**（`start` → `Start-Process`）。
- **2026-09-18**：**判据 6 的时间基准落地（ADR 026）**：`recordEvent()` 加 `recvAt`，
  端到端延迟 = `recvAt - ts` = **中位 110ms**（此前用 `ts` 之差只有 2ms，低估约 55 倍）。
  新增 `tools/measure-latency.mjs`（取数）与 `spikes/m3-latency/run.mjs`（真实窗口验证，PASS）。
  顺带清掉挂起 #8（`badgeCount` 注释）与 #9（`STATUS_TEXT` 改 `satisfies`）。
- **2026-09-18**：**#14 修复（ADR 024）**：过期的 `ready` 通报不再占面板。
  判定为 bug —— ADR 021 的"出口"原则只落地了一半（改了仲裁输出、没改面板视图），
  而留下的那一行 `isAckable=false` ⇒ 用户没有任何手段消掉它。
  修法：`viewSessions()` 过滤（只针对 `ready`、不删记录）。单测 294→**304**、边界探针 **27/27**、
  ack-revival **14/14**、真实窗口 `m3-ready-exit` 与 `m2-control` 均 PASS。
- **2026-09-18**：**Proma 冻结**（用户拍板）：先完成 WorkBuddy 通道成品，不处理 Proma 任何内容。
  11KB 设计稿降级为约 50 行占位卡（全文留 git 历史 `e84685f`）。

- **2026-09-15**：合并两条并行会话产物为统一工程；建立本文件为单一事实来源。
- **2026-09-15**：补建跨会话接续三层机制（用户级 `~/.workbuddy/MEMORY.md` 项目索引 + `AGENTS.md` + §1）。
- **2026-09-15**：建立工程管理体系（`docs/` + `journal/` + `spikes/` + `.gitignore`）；初始化 git。
- **2026-09-15**：M0 窗口层验证完成（`spikes/m0-window/`）：逐像素穿透、不夺焦点、全屏让位三项判据全过。**修订 ADR 001 第 5 条**（ADR 004）。
- **2026-09-15**：排查"工作区不出现在列表/新建对话落到时间戳目录"，定位到 `workbuddy.db` 的 `workspaces` 表机制（ADR 006）。结论：不搬家。
- **2026-09-15**：M1 渲染闭环代码完成（ADR 005）。
- **2026-09-15**：M1 人工验收，修掉两项缺陷（ADR 007）：全屏让位判据因 koffi 结构体未回写而恒 false；规格里的 `hitTestAlphaThreshold` 从未接线。负面结论：`WindowFromPoint` 不能用来验证分层窗口的透明穿透。
- **2026-09-15**：第二轮人工复验，搭真实点击探针（169 采样点）实测出两条硬结论并据此重做命中方案（**ADR 008**）：① 生效命中区域 = 整个窗口矩形（窗口内 90/90 采样点全被吃掉，M0 的"逐像素穿透是原生行为"被推翻）；② 窗口每 `setPosition` 一次长 1 DIP（150% 缩放下取整累积），这正是"拖动后死区越变越大"的机制。改为：渲染层按当前帧 alpha 显式判定 + 16ms 光标轮询 + `setContentBounds` 复位 + 缩放 1.0→0.75。复测：误吃 43→**0**，拖动前后命中图逐点一致。
- **2026-09-15**：修掉"全屏让位回来后宠物点不动/拖不动"（**ADR 009**）。最小复现是 `hide()`+`showInactive()`；症状精确到"移动事件能进、按钮事件进不去"；用 `SendMessage` 证明是 Windows 路由问题；9 个补救动作全部无效、nudge 不可靠、`webContents.reload()` 稳定有效。改为恢复时重载渲染层 + 强制重报命中状态；顺带去掉无效的 `forward` 选项、消除 4 DIP 的 view/窗口错位。两轮外部全屏实测：全屏后单击与拖动均 ✅，无累积劣化。
- **2026-09-16**：**M1 人工验收通过**（六项判据全过，含"全屏恢复后仍可拖动"），M1 正式收尾。
  下一步按 §4 开工 M2（第一件事：状态源抽象 + 接真实 hook）。本文件同步刷新为可直接接续的索引。
- **2026-09-16**：**M2 ① 完成：`状态源 → 仲裁器 → 动画`链路打通（ADR 010）**。
  新增 `src/kernel/status.ts`（仲裁器：优先级/粘滞/最短展示+限流/多会话聚合/静默兜底，时钟可注入）、
  `src/source/`（状态源接口 + 状态文件适配器）、`tools/pet-hook.mjs`（hook CLI，兼手工模拟器）；
  仲裁器跑在主进程作为全应用唯一真值，渲染层退化为哑渲染层。
  第一个真实 hook 走状态文件 `~/.desktop-pet/status.json`（避开监听端口与防火墙提示），
  事件同时落 `~/.desktop-pet/events.jsonl` 供离线查证。
  实测：单测 58 项断言全过；端到端六步核对到渲染层**实际绘制的帧行号**（7/6/0/3→8/5/0）；
  回归探针两轮全过。过程中单测抓出两个真 bug（粘滞超时会立刻重新粘滞 = 超时无效；适配器把
  "读不懂的条目"误判为"会话消失"），已在 ADR 010 记录。负面结论：探针驱动会读到上一次运行的
  残留日志（需运行标识兜底）；`child.kill()` 带不走 detached 的 Electron（需 taskkill /T）。
- **2026-09-16**：**M2 ① 人工验收通过**（用户执行，事件流水留痕于 `~/.desktop-pet/events.jsonl`）。
  返工了验收材料：`喂状态.bat` 改成循环菜单（原来喂完就退出，且 EOF 时会空转）；新增
  `docs/status-reference.png` 状态对照卡（业务状态 → 行 → 屏幕实际外观）。
  起因是验收说明把 `running` 写成"原地跑动"、实际是生日姿态 —— **业务状态名 ≠ 动作外观**，
  已约定描述动作必须引用那张图（AGENTS.md）。同时更正 PLAN 的运行参数：实际缩放 0.7、窗口 134×146。
- **2026-09-16**：**M2 ② 完成：托盘菜单与右键菜单（ADR 011）**。
  新增 `src/host/{tray,pet-menu,prefs,autostart}.ts` 与 `tools/make-tray-icon.py`（图标脚本生成）；
  托盘与宠物右键共用一套菜单模板与一张动作表，状态一律现读 `StatusArbiter.state`。
  原生菜单可行性先探针实测（可弹出、可点击、不破坏输入通路），因此不自绘菜单（避免改窗口尺寸）。
  实测：托盘动作探针 **PASS**（隐藏→显示后仍可点可拖；缩放 0.7→1.0 尺寸与锚点正确、命中仍只吃精灵；
  自启写注册表并经第二来源核对；退出后 PID 消失）；状态层 58 项断言与端到端、全屏回归探针全过。
  过程中修掉四个真缺陷：① `pointermove` 的 `button` 是 −1，误加"只处理左键"判断会静默吃掉整段拖动；
  ② 命中判定只跟光标不跟动画 → 点完宠物立刻拖动会穿透，改为渲染层每帧复评；
  ③ 桌面（WorkerW）被误判为全屏应用 → 点桌面宠物自我隐藏，按窗口类名排除；
  ④ `GetClassNameW` 返回值差一错误使 ③ 的修复静默失效一轮。
- **2026-09-16**：**M2 ③ 完成：气泡层（状态文案 + 多会话角标）与全局快捷键（ADR 012）**。
  核心取舍：气泡用**独立透明层窗口**，不扩大宠物窗口 —— 扩大会改窗口矩形，命中判定的坐标系与
  边界都得重验（ADR 008/009 的地盘）。气泡层常态整窗穿透、永不切换，因此与 ADR 009 那个
  "hide→show 后按钮事件不路由"的坑无关，全屏恢复也不需要 reload。
  策略做成纯函数（`kernel/bubble-policy.ts`）：`needs-input` 常驻、`ready` 6s / `blocked` 4s /
  `running` 2s、**同状态心跳不重置计时**；时长写进 `desktop-pet.json` 的 `bubble` 段。
  实测：快捷键真实击键触发（隐藏↔显示）；气泡 2 秒自动收起、needs-input 常驻带 `+1` 角标、
  拖动时跟随；**新增窗口没吃掉任何点击**（命中图误吃 0、拖动前后一致）；全屏与托盘回归全过；
  单测 58 → **81 项**。
  **最大的坑**：宠物包里写的是人话 `Win+Alt+P`，而 Electron 里 Windows 键叫 `Super` ——
  `register('Win+Alt+P')` **返回 true 但永不触发**（静默失效）。注册前一律 `normalizeAccelerator()`。
  另两条负面结论：窗口标题由页面 `<title>` 决定（`setTitle` 会被覆盖）；下发 32 DIP 高度会读回 38，
  定位改用实测高度。
- **2026-09-16**：**M2 ③ 人工验收通过**（用户逐项测过快捷键、气泡、角标、拖动跟随），
  并修掉两项观感缺陷（**ADR 013**）：
  ① **文字下缘被裁**（1 个物理像素）—— `line-height: 1` 让行盒只有 13px，短于字体自身行高
  （雅黑 13px ≈ 17px），字形溢出后被"省略号必需的" `overflow:hidden` 切掉；改为 `line-height: 20px`；
  ② **文字没水平居中** —— flex 默认 `flex-start`，宽度估算多出的余量全落右侧（实测左 0.00 / 右 3.39px）；
  改为 `justify-content: center`（修后左右余量 1.69 / 1.70px）。
  **做法上的关键**：先用 canvas 的 `fontBoundingBox*` 反推裁切量，算出 0 —— 与真实渲染不符
  （**canvas 字体度量 ≠ Chromium 排版度量**）。改成**渲染层面的差分取证**：同一帧截屏两次
  （保留裁切 / 注入 CSS 关掉 `overflow`），比对文字墨迹的行范围，差值即被裁像素数，容差 0。
- **2026-09-16**：`喂状态.bat` 增 `8 第二会话 b=running` / `9 清掉会话 b`，角标语义写进菜单头部。
  起因：用户开两个终端分别按 1 与 4 仍看不到 `+1` —— 角标统计的是"主状态之外的其它活动会话"，
  而**两个终端写的都是同一个会话 `default`**，单会话永远得不出 `+1`。新增自测
  `spikes/m2-hotkey/check-feed-bat.mjs`（6 项断言，模拟真人连按）。单测 81 → **87 项**（补了这条场景）。
  回归全绿：命中图误吃 0、全屏两轮 OK、托盘 PASS、状态端到端六步对上。
- **2026-09-16**：**M2 ④ 接口设计出稿**（`docs/design/m2-control-bar.md`，状态 = 待用户确认，尚无实现代码）。
  先回答了那个"必须先定的问题"：**输入框的内容发去哪** —— 结论是**本轮不渲染自由文本输入框**。
  判据不是"做不出来"，而是规格里输入框的每个去处都在 agent 通道上：基线 2 的铅笔按钮 = 开新对话、
  基线 3 的 `@` 上下文 / `$` 技能、基线 4 的"聚焦 Quick Chat"、架构 6.1 的"线程列表"。
  另两条路线（本地命令通道、假输入框）都不推进验收基线，前者还会与 M3 的对话语义在同一输入框里打架。
  设计主体：本项目**第一个可聚焦窗口**（现有三层窗口全是 `focusable:false`，焦点行为是零经验区）——
  双路径显示（悬停 `showInactive` 不抢焦点 / 快捷键 `show()` 抢）、唯一不适用 ADR 008 逐像素纪律的窗口
  （矩形直角面板，窗口矩形≈可视矩形）、显示策略做成纯函数状态机（含"快捷键收起后被悬停弹回来"这条
  易错规则的抑制窗口）、动作走**白名单命令 id**（M3 加输入框时协议形状不变）。D1–D5 待用户拍板。
- **2026-09-16**：**M2 ④ 完成：悬浮控制条（ADR 014，待人工验收）**。
  D1–D5 由用户拍板（五项全按推荐）。新增 `src/host/control-bar.ts`（本项目第一个可聚焦窗口）、
  `src/kernel/bar-policy.ts`（显示策略纯函数状态机）、`src/renderer/control-bar.{html,ts}`、
  `src/main/preload-bar.ts`；`kernel/status.ts` 增只读视图 `viewSessions()`；
  `shared/ipc.ts` 增 3 个通道与 `BarView`/`BarCommand`/`BarCommandId` 白名单；
  `main/index.ts` 接线悬停判定、快捷键语义、全屏让位、拖动跟随与命令表；
  `pet-menu.ts` 加快捷键文案与控制条入口；sidecar 增 `controlBar` 段；构建链增两个入口。
  **快捷键 `Win+Alt+P` 语义改为"唤出/收起控制条"**（修订 ADR 012，规格 §3.4），
  宠物隐藏时按下先叫回宠物；隐藏宠物仍有托盘/右键/控制条三个入口。
  实测：`spikes/m2-control` 两个探针 **全 PASS** —— 悬停唤出不抢焦点 / 快捷键唤出抢焦点且键盘可达 /
  **hide→show 往返后键盘照常投递（ADR 009 在键盘路径不复现 ⇒ 控制条不需要 reload）** /
  位置下间距 8 DIP 正、水平偏差 0、高度随会话数 72↔128 / 贴底边翻到上方 / 拖动跟随 /
  命令白名单（非法 id 与不存在的会话都被拒并留日志）/ **`WS_EX_TOOLWINDOW=true`** / Esc 与 close-bar 生效。
  单测 87 → **130 项**。回归全绿：命中图**误吃 0**、全屏两轮 OK（日志里可见控制条"跟随隐藏"）、
  托盘 PASS、气泡与快捷键 PASS（用例 1 已按新语义改写）。
  过程中修掉三个真缺陷/坑：① **`win.isVisible()` 不能做显示记账**（`show()` 同一 tick 内可能仍返回
  false，导致"抢焦点"路径重复 `show()` 一次、把刚拿到的焦点又让出去）；② `skipTaskbar: true`
  **不加** `WS_EX_TOOLWINDOW`，控制条会出现在 Alt+Tab，改为用 koffi 显式设置；
  ③ 到点收起若顺手撤销 `armed` 位会让"悬停唤出"永久失效（已用单测钉住）。
  另记一条方法论教训：**测位置类规则前必须先摆位** —— 第一版探针直接量默认位置，报出两条假失败，
  实际是宠物本来就贴屏幕右下角（面板正确地翻到了上方并被夹进工作区）。
- **2026-09-16**：**M2 ④ 人工验收返工**（用户报 6 条，全部处理；细节与证据见 ADR 014 验收返工节）。
  修掉三个真缺陷：① **命中换算缺少客户区/单元格边界检查（M1 起就存在！）** —— 窗口外负坐标经
  整数运算落回图集其它行列、命中别的精灵，判定随动画帧抖动：既造成"远处鼠标触发控制条"，
  又让悬停 hideAt 被无限重置（面板收不掉）。修法是 `hitAt` 加两层边界检查；
  **教训：边界 bug 只在坐标极端时显形，探针采样必须覆盖远离目标的点**（m2-control 幽灵区域用例已固化）。
  ② 面板隐藏后旧矩形仍参与悬停判定（宽 260，比宠物宽 126 DIP）→ 不可见时不再计入。
  ③ 面板 × 没写 data-cmd，事件委托机制下点击完全无效 → 已补。
  另按用户意见：撤掉 −/+/↺（缩放走"⋯ 更多"菜单，命令白名单缩到 4 个）、hoverGraceMs 500→350、
  **「确认」改为立即生效**（不再被 500ms 限流挡住；限流本身未废，单测双向钉住）。
  残留会话排查结论：**链路无故障**（隔离验证 + 事件流水为证），是快照残留 + 用户按 8/9 操作的是
  会话 b 的误解；产品层补「清空状态会话（N 条）」菜单项 + 会话行"已静默"标注。
  回归：单测 130→**131**、m2-control PASS（含幽灵区域与 × 新用例）、命中图**误吃 0**（169 点）、
  全屏两轮 PASS、托盘 PASS。**待用户复验**：远处不触发 / × 可点 / 确认即时 / 清空会话。
- **2026-09-17**：**审计两处收尾**（ADR 014 补记 6–8）。
  ① **修掉启动顺序上的 TDZ 隐患**：状态源 start 会同步读文件并可能立刻 `pushStatus()` →
  `refreshBar()`，而 `bar` 是 `let` 声明在后面 —— 原先不崩只因 boot 全程同步，
  顺序一改或插入 `await` 就会启动即崩。把「启动异步子系统」（状态源 + 仲裁 tick）整体移到
  **所有窗口创建完毕之后**，依赖从"靠隐式保证"变成"结构上不可能"；顺带修掉那条失效的注释。
  ② **命中图残留的 2 个漏吃定位为边缘取整分歧**（探针现在打印明细：`(88,8) alpha=16`、
  `(24,56) alpha=31`，都紧贴阈值 16 的轮廓边缘；渲染层用 `floor(css/scale)`、探针基准用
  `round(css*dpr)`，边缘 1 像素内分歧）。**判据只看误吃（现为 0），漏吃有上界且可复现。**
  ③ 探针前置教训（第三次同源）：`hoverGraceMs` 500→350 后面板收得更早，控制条探针在
  "面板已收起"时读 DOM 导致 4 条连环假失败 —— 产品行为是对的（不可见时 refreshBar 早退、
  重新显示时推最新内容，铁证是重新显示后高度=128=2 条会话）；探针已补"先唤出并断言可见"的前置。
  回归全绿：单测 **131**、m2-control 两个探针 PASS、命中图**误吃 0**（4 项）、全屏两轮 PASS、托盘 PASS。
- **2026-09-17**：**M2 ⑤ 多宠物切换搁置（ADR 015，用户拍板）**。理由：手里只有一套素材，
  且没有"素材导入流程 + 规格标准" —— 造素材／定标准／写切换是**互相牵扯的三件事**，压进同一轮
  出了问题无法定位是哪一层的；而拿 `demo-pet` 复制成两份冒充"多套素材"是假验证。
  **下一步改为「M2 ④ 的人工复验与缺陷修复」**，开工话术已改写（§1），⑤ 的重启条件写在 §4 第 5 条。
- **2026-09-17**：记一条**待实测的线索**（**尚未验证，别当结论**）：状态文件是快照、重启后原样读回；
  若某条会话记录**没有 `ts` 字段**，`kernel/status.ts` 的 ingest 会把它盖章成"现在"，于是这条
  陈旧会话在启动后"新鲜" 15 分钟 —— 表现正是用户上轮报的"一启动就显示某某在运行中"。
  待下轮用探针确认（§1 的问题清单里已点名）。
- **2026-09-17**：**M2 ④ 缺陷修完 + 控制条交互模型重做**（ADR 016 / ADR 017）。两类改动：

  **A. 修两条真缺陷（ADR 017）**，都是上一轮复验定位、本轮实测 + 修复 + 钉住：
  ① **没有 `ts` 的快照被盖章成"现在"**（上一条线索证实成立）：改为用**快照 mtime** 兜底
  （同一 fd 取，避免先 stat 再 read 的竞态），且这类条目标记 `tsFromFile`、
  **不参与"ts 变了算心跳"那条分支** —— 否则同一文件里任一会话被写就会给其它无 ts 的陈旧会话续命。
  这条修的正是"一启动就显示某某在运行中"背后的机制之一。
  ② **「清空状态会话」只清了宠物没清视图**：改成**三处一起清** —— 文件 / 适配器记忆
  （新增可选接口 `StatusSource.reset?()`）/ 仲裁器记录（新增 `StatusArbiter.clearSessions()`，
  顺带清确认位、pending、sticky 并重置限流窗格）。
  判据：单测 131 → **138 项**（⑧b 8 条 + ⑬ 10 条，删悬停 8 条）；`probe-stale-sessions.mjs`
  **重写为 22 项回归哨兵**（含"刚写的无 ts 快照必须照常生效"这条**防误杀**的反向用例，
  以及"清空后行数 0 且不再冒出补事件"）；控制条探针 ⑫ 段端到端钉住"行数 2→0、
  菜单计数 2→0、高度 128→72"。

  **B. 控制条交互模型重做（ADR 016，用户拍板）**：
  **悬停唤出整条路径删除** —— 桌宠常驻角落、光标是"顺便"掠过的高频事件，自动弹出必然变成
  "我没叫它它自己冒出来"，延迟只降频不改性质；且左键单击要播动作，"停在它身上"不该带第二个含义。
  现在唤出只有三条**显式**路径（**右键宠物** / `Win+Alt+P` / 托盘菜单「控制条」），**一律抢焦点**；
  关闭五条路径（Esc / × / 再唤出一次 / 点到别处 / 宠物被隐藏）。
  **两处右键分工**：宠物右键 = 控制条，托盘右键 = 完整菜单（面板「⋯」也是这份）。
  面板动作排里的只读 `70%` 换成**宠物名**（`pet.json` 的 `displayName`，不硬编码"淘淘"）。
  `kernel/bar-policy.ts` 从"七条规则 + `armed`/`suppressUntil` 两个补丁位"缩到**五条规则**。
  新增机制：**`⋯` 弹原生菜单期间屏蔽"失焦即收"**（原生菜单会夺走面板焦点）。
  左键单击语义澄清为"播「拜一下」（第 3 行 `waving`，依据 `docs/status-reference.png`）+ 确认"。
  判据：`spikes/m2-control` 两探针 PASS（含"悬停 1.8 秒不唤出""右键宠物唤出且抢焦点 / 再右键收起"
  "⋯ 开着时面板仍活着""× 收起后不自己弹回来"）；探针里所有"确保可见"的前置统一改走
  **真实右键宠物**，手势失效即报错，不再用调试钩子静默绕过。
  回归全绿：单测 138、命中图**误吃 0**（漏吃 2 = 既有边缘基线）、全屏两轮 OK、托盘 PASS、
  状态端到端 PASS（六步帧行号全对）、气泡与快捷键 PASS。
  过程教训：**同一文件不要在一次并行编辑里改两处** —— 本轮有 4 处编辑工具报"成功"但没落盘，
  构建报错才发现（已改为改完立刻 grep 校验）。
- **2026-09-17**：**M3 第一块落地：行为层（ADR 018）**。宠物从"站在角落里循环待机"变成
  "没人用它时像个活物"：自主漫游（每 25–90 秒沿地平线走 80–320px）、微动作（挥手/跳跃）、
  打盹（空闲 5 分钟落趴卧帧、单击即醒），并带**四条抑制**（拖动中/隐藏/全屏/控制条开着一律不动）
  与「减少动态效果」下的降级（不漫游不做微动作、打盹照常）。
  分层沿用既有架构：**规则 = 纯函数**（`src/kernel/behavior.ts`，时钟与随机数可注入）、
  接线 = `main/index.ts`（33ms tick，攒输入 → 调 tick → 落命令）、渲染层 = 哑渲染层
  （新通道 `CH.behavior` 接动画覆盖，覆盖期间**不把播放器收敛回主状态**，否则每帧被拉回去走不动）。
  另新增：`overlay.moveTo()`（绝对定位，避免增量取整吞掉像素）、`CH.dragState`（拖动的显式边沿）、
  `ready({reducedMotion})`（渲染层才知道的系统偏好）。
  判据：单测 **138 → 191 项**（新增 ⑭ 段 22 项：启动不乱跑、到点才走、速度按缩放归一化、
  目标夹进工作区、贴边换方向、抑制期间不动且保留目标、running 不动、打盹与醒来、
  **漫游不重置打盹计时**、微动作播完交回、边沿命令不重发、总开关）；
  新增真实窗口探针 `spikes/m3-behavior/run.mjs`（用临时宠物包把节奏压到秒级）**7 条全过**：
  真的在走（38 次位移）/ 只沿地平线走（y 只有一个值）/ 不越界（0）/ **位移行号与方向一致 8/8** /
  抓住 3 秒一步不动且松手恢复 / 控制条开着不动 / running 期间不动 / 打盹画到第 5 行且单击能醒。
  **两条只有实测才发现的坑**：① 打盹计时若被漫游重置，"每 25–90 秒走一次"会让它**永远睡不着**
  （屏幕上只表现为"它就是不打盹"）；② 自主行走让既有的**位置类断言集体变脆**——
  7 个回归探针统一加 `--no-behavior` 接缝（与 `--no-status-source` 同一套路）。
  另按用户要求：`pet.json` 的 displayName 改为**淘淘**、待机动画放慢到 **0.8 倍**（8 → 6.4 fps）。
- **2026-09-18**：**行为层补完"任务中踱步"，并锁定"不跨屏"（ADR 019）**。
  用户拍板了 ADR 018 留的那个产品决定：**任务进行中可以离开原位，但不超过几百像素、
  频率要低、只作为点缀**；同时明确**暂不考虑多屏**（只用主屏 / 拖到哪块屏就在哪块屏活动）。
  另外把早先记的"运行中看起来像在忙"（"忙碌动作"）**明确作废** —— 用户不认这个动机，
  名字也不再使用；真正落地的是上面这条更小的要求。
  - 实现：`tickBehavior` 把规则 0 拆成两套模式 —— `idle` → 空闲模式（漫游/微动作/打盹），
    非 idle → **踱步模式**（`tickBusy`）。新增相位 `pacing` 与三个状态字段
    （`busySinceAt` / `anchorX` / `nextPaceAt`）；`rearmBehavior` 兼顾两种模式。
  - **最关键的一条**：踱步目标**由锚点算、不由当前位置算** —— 按当前位置累加会越踱越远，
    让"不超过几百像素"在几分钟后**悄悄失效**且无任何报错；锚点在进入任务状态时、
    以及每次抑制结束后重记（后者正是"拖到哪块屏就在哪块屏活动"的落地机制）。
  - 参数（`desktop-pet.json → behavior.busyPace`）：每 60–180 秒、60–300px、速度沿用漫游基线
    （另取一值会滑步）；方向朝**工作区中心**一侧（默认右下角 ⇒ 观感是"往左走几步"；
    固定朝左在贴左边缘时会永久失效）。有任务时**不计打盹**（`idleSinceAt` 恒 null）。
  - 判据：单测 **191 → 219 项**（新增 ⑥b 踱步、⑥c 不跨屏；三条核心判据是"踱几十次不漂出锚点"
    "默认节奏下 10 秒一次都不踱""换到另一块屏时目标夹在新工作区内"）；
    `spikes/m3-behavior` 的 ④ 用例从"running 期间一步不动"改成"在动但离锚点不超过上限"，
    **实测离锚点最远 111px / 上限 160px、锚点全程只有一个值、越界 0**，7 条全过。
  - **留给用户验收看一眼的取舍**：踱步期间画的是位移行（第 1/2 行），不是业务状态那行
    （另一条路是"业务动画不动、只是窗口在挪"，画面上会变成生日姿态在地面平移）。
    用户原话"动画可以照常播放"两种读法都通，记在 ADR 019 负面结论 1，改的话是一行。
- **2026-09-18**：**M3 第二块启动：状态源生态（ADR 020）—— 目标 agent 改为 WorkBuddy + Proma**。
  用户 D1 答复把目标从 Codex 改成**WorkBuddy（主力）+ Proma（第二主力，Pi agent 架构）**，
  于是重做调研，结论有实质变化：

  **三个决定性发现**：
  ① **WorkBuddy 自带完整的 Claude 系 hook 引擎**（初稿完全没料到）——
  内嵌 `@genie/agent-cli` 的 `codebuddy.js`(23MB) 里有 14 个事件的字面量数组
  （含 `PermissionRequest`/`Notification`/`PostToolUseFailure`/`WorktreeCreate`）、
  `hook_event_name`×49、`executeHook`×32、`disableAllHooks`，且 hook 是**真的 spawn 子进程**；
  配置位置 = `.codebuddy/settings.json`。
  ② **闸门实测 = YES**：进程树显示桌面把 agent 跑在内嵌
  `cli/bin/codebuddy --serve --session-id ab2f75f8-…` 上（该 uuid 与
  `~/.workbuddy/sessions/203516.json` 的 `sessionId` **完全一致**）；
  CLI 里 `DEFAULT_SCOPES = [USER, PROJECT, PROJECT_LOCAL]` 且跑 agent 的进程**没带**
  `--setting-sources` ⇒ **工程级配置会被读**（这把 D5 的"只写工程级"从保守猜测变成有证据）。
  ③ **Proma 没有可配置的 hook 面**（`app.asar` 里 `PreToolUse`/`hook_event_name` 全 0，
  `SessionStart` 25 处逐条看过是编辑器内部变量），**但被动源可用**：
  `agent-sessions.json` 218 条会话带 `title`/`updatedAt`，日志末尾有
  `type:"result"` + `terminal_reason:"completed"`。顺带实证 SDK 迁移：`sourceRuntime` = claude 38 / Pi 180。

  **用户提的关键问题（"两个软件同时跑会不会冲突/叠加"）决定了架构**：
  两个**不同** agent 天然不冲突（仲裁器从 M2 ① 就是多会话形状，`+N` 角标即为此）；
  会打架的是**同一个 agent 被两条通道同时盯** ⇒ 新增硬规则**一个 agent 一通道、互斥**
  （被动源是降级来源，是切换不是叠加）；`sessionId` 带来源前缀、标题带来源名；
  用户想要的"自己选择连哪个 agent"因此变成一次按前缀过滤，不是新机制。

  **施工顺序被实测推翻一次**：初稿把"先做被动源"当零风险的先赢，151s @1Hz 采样探针证明
  **WorkBuddy 的被动源只能给"存在性"、给不了"活动性"** —— 心跳 151 秒里只刷新 5 次
  （**粒度约 30 秒**，且与在不在干活无关）；`tasks/`、`file-history/`、`changes-index/`、`plans/`
  窗口内 **0 次变更**；`logs/` 105 次写入但内容是应用级日志（`vendor-extract.log`、`weixinpay/*.xlog`）
  ⇒ **通道 A 才是 WorkBuddy 的主路径**。另记一条**自我更正**：此前写"心跳秒级新鲜"是错的 ——
  那是**只看了一次文件的当前年龄**，而不是**看它的更新频率**，两者完全不同。

  **落地**：`tools/codebuddy-hook-map.mjs`（纯函数映射层）、`tools/pet-hook-cb.mjs`
  （hook 客户端；**退出码恒 0**、`--dump` 取证、stdin 超时兜底）、
  `tools/install-codebuddy-hooks.mjs`（安装器，**默认 dry-run**、幂等、可卸载、带备份）、
  `tools/codebuddy-hook.test.mjs`（**62 项**，含"不踩掉别的 agent 的会话"）、
  `spikes/m3-status-sources/probe-workbuddy-live.mjs`（只读采样探针）。
  工程级 `.codebuddy/settings.local.json` **已落盘**（6 个低频事件；`PreToolUse`/`PostToolUse` 不挂，
  每次 377ms）。既有 `pet-hook.mjs` **刻意未动**（人工验收入口，一批回归的着力点）。
  判据：新增 62 项 + 既有 **219 项全绿** ⇒ **281 项**。
  **待办**：判据 1/2/6/9 的真实回合验证；Proma 的采样探针与适配器。
  **闸门带出的坏消息**：当前会话 `--permission-mode fullAccess` 不弹审批
  ⇒ 判据 3（`needs-input` 端到端）大概率**验证不了，届时如实记"未验证"**。
- **2026-09-18（同日晚些）**：**真实回合验证：判据 1 通过，并抓修一个只有真跑才暴露的缺陷**。
  配置落盘后用户真发了一条消息，hook 侧留下 5 条实测记录
  （`PermissionRequest`×2 / `Stop` / `Notification` / `UserPromptSubmit`）：

  - **判据 1 ✅**：hook 真的被派发；**含空格路径正常**（命令里两个绝对路径都没被 shell 截断 ——
    这条本项目栽过一次，这次专门验了）；payload 的 `session_id` 与进程树、
    `~/.workbuddy/sessions/*.json` **三处一致**；payload 比文档更丰富
    （多了 `permission_suggestions`/`permission_mode`/`client`(实测 `"WorkBuddy"`)/`generation_id`
    /`model`/`agent_type`/`background_tasks`/`session_crons`），其中 **`client` 可直接当来源标识**。
  - **抓到一个我自己的缺陷（已修）**：`Notification` 原本一律映射成 `needs-input`，
    而实测 `notification_type` 是 **`idle_prompt`**（"干完了、在等你说话"），
    它在 `Stop` 之后**约一分钟必然出现** ⇒ **每次干完活宠物都举手要你确认**，
    让 `needs-input` 这个最宝贵的状态**贬值**。改为良性类型白名单（目前只 `idle_prompt`），
    命中则不写状态；未知类型仍按"要你注意"处理。**这条单测写不出来 —— 当时不知道有这个取值。**
    新增 8 项断言（62 → **70 项**）。
  - **一条预测被推翻**：据 `--permission-mode fullAccess` 断言"不弹审批、判据 3 验不了"，
    实测 `permission_mode` 是 `"default"` 且 **`PermissionRequest` 真弹了两次**
    ⇒ **判据 3 可以做**。
  - **状态文件侧正确**：`wb:ab2f75f8-…` → `running`，标题 `WorkBuddy · desktop-pet`。
  - **⚠️ 判据 2/6/9 未验，原因明确：验证时桌宠没有运行**（无 electron 进程），
    宠物侧 `events.jsonl` 停在 2026-09-17 ⇒ 没有可对账的时间线。**必须开着桌宠再跑一次。**
  - 判据：新增 **70 项** + 既有 **219 项** ⇒ **289 项全绿**。
- **2026-09-18（同日晚些 · 第二次验证）**：**判据 2 通过；用户撞到一个产品级缺口**。

  **判据 2（状态序列对账）✅**：用户装上桌宠后，宠物侧 `events.jsonl` 终于有了时间线 ——
  `ready`(09:56:57.853) ← `Stop`(09:56:57.851)；`running`(10:02:42.862) ← `UserPromptSubmit`(10:02:42.860)。
  序列正确、不卡在 running；且 **hook 侧 09:57:57 有一条 `Notification type=idle_prompt`
  而宠物侧无任何对应事件 ⇒ 上面那个假阳性修复在真实环境确认有效**。

  **用户报告：一启动桌宠就是"炒菜/做饭"的姿态，不是原来的默认待机。**
  诊断（**对照 `docs/status-reference.png` 确认，不是凭状态名猜**）：
  `ready` → 动画 `waving` → **落点 `review` = 第 8 行"小厨师姿态"**；
  而 `review` 是 **`loop: true` 的循环态、没有出口** ⇒ **agent 干完一次活，宠物就永久炒菜**；
  用户 10:01 启动时读到快照里 10 分钟前的 `ready`（未到 15 分钟静默兜底）⇒ 立即进入 review。
  **以前没见过，是因为那时没有真实 hook，`ready` 只来自人工喂状态。**
  支持"这是缺陷"的论据：**气泡对 `ready` 只显示 6 秒就收起（`holdMsByStatus.ready = 6000`），
  宠物却还在炒菜** —— 两者不一致。
  **但改成什么是产品决策**（该落点是 M2 ① 验收过的行为），已列为 §4 第 8 条**待用户拍板**。

  **⚠️ 判据 6 的测量陷阱（实测发现）**：`~/.desktop-pet/events.jsonl` 里的 `ts` 是**写侧产生时刻**，
  **不是宠物收到的时刻**（实测两者只差 2ms，那只是 hook 进程内"落 payload → 写状态文件"的间隔）。
  **拿它算端到端延迟会严重低估。** 要算真实延迟需另找时间基准（主进程日志或探针加戳）。

  **附带事实**：`default` 会话是 8-16 人工验收的残留（title `manual-acceptance`），
  桌宠启动时被读到并短暂报了一次 `running`（10:01:15）、4 秒后变 `idle`；
  **是噪声不是缺陷**，菜单「清空状态会话」可清掉。
- **2026-09-18（同日晚些 · 第三次反馈）**：**判据 3 也拿到证据；用户拍板 `running` 用第 8 行；
  并报告一个真缺陷（气泡跑到屏幕中间）**。

  - **判据 3（`needs-input` 端到端）✅**：`events.jsonl` 里出现两条
    `wb:<会话>` → **`needs-input`**（10:03:38.167 / .682）—— 对应真实回合的 `PermissionRequest`。
    宠物侧确实收到了这个状态（此前只验证了 hook 侧会发）。
  - **用户拍板（取代 2026-09-16 的旧决定）**：*"我认为可以让小厨师的姿态一直保持为
    Agent 在 running 的状态。"* ⇒ **`running` 改挂第 8 行"小厨师"、`ready` 不再占小厨师**。
    后者顺带修掉"没有 agent 在跑、宠物却还在炒菜"。旧决定（"running 保持第 7 行"）被取代。
    两个子问题（`ready` 落到哪、第 7 行生日归谁）并入复核定稿。已列为 §4 第 8/9 条。
  - **真缺陷：气泡出现在屏幕中间**（用户报告）。**代码级根因已定位**：
    `src/host/bubble-layer.ts` 的 `show()` 在从未 `followPet()` 过时执行
    `if (!lastPetBounds) lastPetBounds = win.getContentBounds()` ——
    **把气泡窗口自己的矩形当成了"宠物矩形"**；而 `main/index.ts:420` 调
    `bubble.show(text, badge)` 时**根本没传宠物位置**。启动后第一次推气泡必然走这条兜底
    ⇒ 位置由"气泡窗口自己被创建在哪"决定，于是跑到屏幕中央。
    **与 ADR 017 那条"没有 `ts` 不等于刚刚"是同一类错误：兜底值取了一个"看起来像但不等于"的东西。**
    修法：把宠物矩形显式传进 `show()`；**拿不到时选择"不显示"，而不是显示在错的地方**。
    用户看到的"屏幕正中"与算法反推吻合，但**"为什么恰好在正中"待用最小 Electron 探针实测**，不写进结论。
  - **用户要求下一轮先做「状态↔动作全盘复核」**（原话：*"我感觉现在宠物状态有点乱"*）。
    **任务书已出：`docs/design/m3-state-animation-audit.md`** —— 内含九行素材占用总表（审计基线）、
    **两处已看出的语义挤压**（**第 5 行趴卧被 `blocked`"已受阻"和「打盹」两种相反含义共用**；
    `ready` 是循环态且无出口）、两项待定稿、上述气泡缺陷、以及**"改映射会打破哪些既有断言"的清单**
    （`status-arbiter.test.mjs` 的 `running → running`、`run-status-e2e.mjs` 的绘制行号
    `7/6/0/3→8/5/0`、`m3-behavior` 里与行号/打盹行有关的用例）。
- **2026-09-18（同日晚些 · 第四次：M3 第三块结项）**：**状态↔动作全盘复核完成（ADR 021）**。
  起因：用户报 *"没有任何 agent 在跑，但它依然是小厨师在炒菜"*。

  **四条结论（详见 §3 与任务书 §0）**：
  - **A · 气泡定位缺陷（已修）** —— `bubble-layer.ts` 的 `show()` 从未 `followPet()` 过时
    把**气泡窗口自己的矩形**当宠物矩形（`if (!lastPetBounds) lastPetBounds = win.getContentBounds()`），
    主进程调 `bubble.show(text, badge)` 时又**根本没传宠物位置** ⇒ 启动后第一次推气泡必走兜底。
    改为 `show(text, badge, petBounds)` 显式传入；**拿不到就"不显示"**。
    实测 **798,446（屏幕正中）→ 1561,809（贴宠物正上方）**。
  - **B · `ready` 没有出口（已修）** —— 这才是"一直在炒菜"的真成因。
    `ready` → `waving` → 落点 `review`（第 8 行、`loop: true`）⇒ 干完一次活就永久炒菜。
    **⚠️ 推翻了任务书初稿的推断**：初稿说"落点每 6 秒被反复重走"，用真实仲裁器复跑证明**不成立**
    （稳定输入下 `rev` 不动、渲染层收敛不动）；真机制是"完全无出口 → 15 分钟静默兜底"。
    **也推翻了初稿的事件流证据**：`wb:de8d4bad-…` 55 条里 50 条是 `needs-input`，`ready` 只有 1 条。
    修法：新增 `statusTimeouts.readyMs`（60 秒通报时效，到点回 `idle`）+ `ack() 扩展为也消解 `ready``
    （单击宠物 = 已读）。**计时不能用 `rec.ts`** —— 它每次心跳都刷新，拿它计时会让机制在自己要防的
    场景下失效；新增独立 `readySince` map，单测钉住"心跳不推后时效"（`expiredAt === 60_000`）。
  - **C · 第 5 行语义挤压（用户拍板，已改）** —— 趴卧同时被 `blocked` 与「打盹」两种相反含义共用。
    按用户拍板 *"blocked 时打盹的样子上面应该出现文字提示"* ⇒ **`blocked` 气泡改常驻**
    （`stickyStatuses` 加 `blocked`，并从 `holdMsByStatus` 定时表摘掉）。
  - **D · `running` / `ready` 已分离** —— 用户确认自己记混了，只要求"确保两者不一样"。
    现状：`running` = 第 7 行生日（稳定态）、`ready` = 第 8 行小厨师（**60 秒后有出口**），
    不同行、不同命运，满足要求。
  - **关闭一个疑点**：`wb:default` 是 8-16 人工验收残留（`origin=file, title=manual-acceptance`，
    39 小时 114 条），**不是缺陷**。

  **判据**：单测 **219 → 255 项**（新增 ⑨b 段约 36 项）、hook **70 项**；
  新增真实窗口探针 `spikes/m3-ready-exit/run.mjs` **PASS**
  （`0.1–0.5s` 第 3 行 → `0.6–4.2s` 第 8 行 → `4.3s→` 第 0 行，`--ready-ms=4000` 压时效）；
  `m2-status` 端到端、`m2-menu`、`m2-control`、`m3-behavior`（不带 `--no-behavior`）全 PASS；
  `m2-hotkey` 气泡用例 ✅（位置 798,446 → 1561,809）、**快捷键用例 FAIL 已用 `git stash` 对照
  证明是既有环境问题**（本机 `Win+Alt+P` 不可用，已降级 `Control+Alt+P`；未改动版本同样 FAIL）。

  **两条方法论教训**：① **只跑 `tsc` 不够** —— preload 是 esbuild 打包的，改了 renderer/preload
  必须跑完整 `npm run build`，否则探针拿到"渲染层根本没初始化"的 0 采样点（README 已记）；
  ② **单测断言写错不是测试 bug 就是断言 bug** —— ⑨b 那条"重报期间输出没变过"期望 0 实际 1，
  原因是我每 10 秒重报一次、第 6 次恰好落在 60 秒到期点，于是有 1 次**合法**的输出变化；
  改为精确断言"10 次心跳里只有一次变化，且发生在第 60 秒"。

  **⚠️ 一条口径差异待用户确认**：用户拍板写的是"`ready` 落到第 7 行生日（当等你验收）"，
  但第 7 行是 `running` 的现行落点，真改过去**两者撞行**，与同轮的"确保 running 和 ready 不一样"
  相矛盾。实施取**"保持第 8 行 `review` + 叠加 60 秒出口"**。若用户更看重"生日 = 等你验收"的语义，
  改 `desktop-pet.json` 的 `statusMap.ready.then` 一行即可。

- **2026-09-18**：**用户报告缺陷修复 + 工程级审查（ADR 022）**。用户报两件事：启动要数秒、`ready` 的炒菜动画消不掉。

  **① 启动耗时（定量归因，实测）**：分段计时证明 **93% 花在 `启动桌宠.bat` 每次无条件跑全量构建** ——
  `npm run build` **29,941ms** + `npm start` 的 npm 引导 **1,232ms** + Electron→窗口 **871ms** ≈ **32s**。
  应用代码**不是**瓶颈：`boot()` 自身只花 **322ms**（283ms app ready → 605ms boot 返回）。
  额外发现：`tools/run-build.cjs` 量得有效构建仅 ≈5.3s（tsc 3.3s + 6×esbuild 1.4s + copy-assets 0.6s），
  **约 24.6 秒是 npm 自身的解析/加载开销**。新增 `--trace-boot` 接缝与 `spikes/m3-startup/` 两个计时器。
  **优化方案已写入审查报告，待用户拍板**（取舍点：改完代码「双击即生效」会变成「需先构建」）。

  **② `ready` 消不掉（两个独立成因，都已修）**：
  - **成因一：面板谓词漏了 `ready`**。`renderer/control-bar.ts` 的 `needsAck` 旧版只认 `needs-input`，
    于是 `ready` 在面板上**既没有「确认」按钮、行也不可点** —— 而内核 `ack()` 的 `want()` 早已同时覆盖两者。
    两处各写一份判断条件的典型失配。已修正谓词并加注释说明「必须与内核 `want()` 保持一致」。
  - **成因二：双通道交替反复撤销用户的确认位**。`ingest()` 原判据是「非 needs-input → needs-input 就清确认位」，
    而 hook 写 `running`、文件源每 15–60 秒重报 `needs-input` 时状态来回跳，**每次跳回来都清一次**。
    `spikes/m3-ready-ack/probe-ack-revival.mjs` 复刻出**修复前 revivals=3 → 修复后 0**。
    修法：新增 `reAskMinIntervalMs`（默认 60s，`desktop-pet.json → statusTimeouts` 可配），
    把「确认」锚定在**求助的实例**上而非状态字符串上；**离开 `needs-input` 刻意不清 `needsInputSince`**
    —— 双通道交替正是靠这一点被挡住。

  **③ 控制条新增「已确认」按钮（用户要求的功能）**：动作排加 `ack-all` 命令，
  与「单击宠物」走**同一条内核路径**（`arbiter.ack()` 不带参），因此不引入新机制、也不会两处各写一份
  「什么算待确认」。多条待确认时文案显示为「全部已确认（N）」。行内「确认」按钮同时对 `ready` 出现。

  **判据**：单测 **255 项**全过、hook **70 项**全过；新增取证探针 `spikes/m3-ready-ack/probe-ack-revival.mjs`
  **13/13 通过**（含反向用例 2b：「迟滞窗口**之外**的新求助仍应重新举手」，防误杀）。

  **产出工程级审查报告** `docs/reviews/2026-09-18-工程级审查.md`（五维度 + Critical/Major/Minor/Nit 分级）：
  **无 Critical、无安全漏洞**，总评 **7.5/10**。三条 Major：M-1 启动全量构建、M-2 双通道互斥
  **只有文档约束、代码里无任何检查**（症状级修补已做，机制级缺口仍在）、M-3 谓词两处各写一份已失配一次。
  三条 Minor：`boot()` 949 行、`ready`/`stale` 时间边界无探针覆盖（「（已过期）」标签可能是死分支）、
  隐藏时两个定时器仍 16ms/33ms 空转。

  **两条方法论教训**：① **探针的两次输入之间必须显式推进时钟** —— 情形 2b 首版因两次 `ingest` 同一时刻
  而量到 500ms 限流窗格而非被测的迟滞窗口，与 M2 ④ 第一版探针「没先把宠物挪离右下角」是**同一类判据不纯**；
  ② **「状态没变」不等于「确认位安全」** —— 只要还有第二条通道能把它变走，就得按「求助实例」而不是「状态字符串」记账。
- **2026-09-18**：**挂起清单清账（A–E 五项，ADR 023）**，全部为"可直接做"的小项，不改产品行为。
  **A · 双通道运行期告警**（清单 #2 的告警版）：给通道打标 —— hook 客户端写 `origin: 'hook'`、
  文件适配器透传条目里的 `origin`（非法取值丢弃、缺省标 `'file'`）；`StatusArbiter.ingest()` 里
  按 `sessionId` 记上一次的通道，**同一会话 10 秒内换通道**即 `⚠️ 双通道冲突` 告警（60 秒冷却，
  排序后的通道对做键）。**刻意不做逐出** —— 那是产品决策，留给机制级那轮。
  **B · `isAckable` 下沉内核**：`kernel/status.ts` 导出单一谓词，`renderer/control-bar.ts`
  改为 `import` 后直接复用，**渲染层不再有第二份判据**（这是 ADR 022 成因一的根）；
  单测补 20 组真值表 + `ack()` 五种状态的行为对齐 + **源码级断言**（控制条必须 import 内核谓词、
  且不得再出现 `s.status === 'needs-input'`）—— 把"结构性未除"变成"结构性被钉住"。
  **C · `ready`/`stale` 边界探针**：`spikes/m3-ready-ack/probe-ready-stale-boundary.mjs`（离线，18/18）。
  `ready` 59s 仍在 / 61s 退场并标 `expired`；`running` 14:59 vs 15:01 静默兜底边界。
  **顺带推翻交接文档的一处推断**：16 分钟每 15 秒重报 `ready` 后 `viewSessions()` **不清空** ——
  主状态回 idle，但**面板那一行仍长期驻留并带「（已过期）」**（`pruneStale` 只在心跳停止后才扫掉它）。
  ⇒ **「（已过期）」不是死分支，而是在生产的重报节奏下会长期挂着**，是否保留由用户拍板。
  **D · preload 诱饵文件**：`tsconfig.json` 排除三个 preload，另建 `tsconfig.preload.json`
  （`noEmit`，且必须 `"exclude": []` —— `extends` 会继承 exclude 而抵消 include，否则 TS18003）；
  `typecheck` 改为跑两份配置。`dist/main/preload-bar.js` 一类的诱饵不再产出，类型检查不丢。
  **E · 隐藏时暂停定时器**：`main/index.ts` 把 33ms 行为 tick 与 16ms 光标轮询收进 `timers` 记账，
  `show`/`hide` 事件驱动 `startTimers()`/`stopTimers()`，`quit()` 与 `will-quit` 也停。
  250ms 仲裁器 tick **故意保留**（隐藏期间托盘状态与气泡仍要更新）。
  新增 `spikes/m3-hidden-timers/` 探针：可见 ≈17 ticks/800ms、隐藏 **0 ticks/1000ms** 且 `timersRunning=false`、
  恢复后回升 ⇒ **PASS**。修掉一处连带问题：退出时定时器晚于窗口销毁 ⇒ `Object has been destroyed`。
  **判据**：单测 255 → **294 项**；hook **70 项**；`probe-ack-revival` **13/13**；`probe-ready-stale-boundary`
  **18/18**；`m3-hidden-timers` **PASS**；`probe-stale-sessions` PASS；全屏回归两轮 PASS、托盘 PASS、
  控制条 PASS、行为层 PASS；`npm run typecheck` **0 错**（两份配置）。
  **F · Proma 被动源**：仅出设计 `docs/design/m3-proma-passive-source.md`，**未写实现代码**（等用户确认）。

- **2026-09-18**：**挂起清单 #14 修复（ADR 024）：过期的 `ready` 通报不再占面板。**
  判定为 **bug** 并直接修掉（用户授权："如果你觉得是 bug，就修复"）。
  成因：ADR 021 那条"要求注意类状态必须有出口"**只落地了一半** —— 60 秒通报时效改了
  **仲裁输出**（宠物确实不炒菜了，`m3-ready-exit` 探针一直 PASS），却没改**面板视图**；
  而留下的这一行 `isAckable` 为 false ⇒ **不给「确认」按钮 ⇒ 用户没有任何手段消掉它**，
  且生产里文件源每 15–60 秒重报（重报刷新 `ts`）⇒ 15 分钟静默兜底永远不触发 ⇒ **事实上永久驻留**，
  期间还一直撑高面板。
  修法：`viewSessions()` 加一条过滤（**只针对 `ready`**，不动其余状态）。
  **刻意不删记录** —— 删了会造成周期性复活（重报时 `readySince` 被重设 ⇒ 宠物每 60 秒炒一次菜）；
  上游改口 `running` 时这一行会回来（已加反向用例防误杀）。
  连带修正确认语义的观测：`ack()` 对 `ready` 是把计时拨回"已过 60 秒"，
  修复后表现为**确认即退场**（行直接消失）而不是"行还在、标已读" —— 这是对的，
  但单测 ⑯ 与 `probe-ack-revival` 情形 3 的观测方式必须同步改。
  **判据**：单测 294 → **304 项**；`probe-ready-stale-boundary` 18 → **27/27**；
  `probe-ack-revival` 13 → **14/14**；`probe-stale-sessions` 22/22；
  真实窗口 `m3-ready-exit` **PASS**（4.3s 行号 8 → 0）、`m2-control` **PASS**；hook **70 项**；typecheck 0 错。
  **教训**：一条规则**跨层生效时每一层都要有判据** —— 仲裁输出 / 面板视图 / 托盘文案是三个观测面，
  只测渲染层画的行号会漏掉面板那一半。
- **2026-09-18**：**Proma 冻结（用户拍板）**："先把 WorkBuddy 适配好、先完成成品，
  不要先处理 Proma 的任何内容；Proma 只保留一个接口或基础，或仅放置一个任务状态即可。"
  ⇒ 11KB 设计稿降级为 ≈50 行**占位卡**（只留接口形状 / `proma:` 前缀 / `origin:'proma'` /
  已确认事实 / 解冻条件），全文留 git 历史（`git show e84685f:docs/design/m3-proma-passive-source.md`）。
  **解冻条件**：通道 A 成品化完成（判据 9 与 6 都有结论、日常跑一段无异常）→
  用户哪天正好要在 Proma 里干活时提前说一声 → 只读采样 ≈150 秒 → 只回答"能不能区分在跑与只是存在"。
