# 桌面宠物项目 · PLAN（单一事实来源）

> 本文件是接续入口。**任何新会话开始工作前，先读本文件。**
> 详细设计见 `Windows桌面宠物开发方案.html`（13 章：资产盘点、功能基线、构想、选型、架构、难点、数据契约、状态机、行为层、分阶段计划、风险表、验收清单）。
> 规格知识见用户级技能 `codex-pet-pack`（`~/.workbuddy/skills/codex-pet-pack/SKILL.md`）——权威来源，不要重新调研。
> 最后更新：2026-09-15

## 0. 接续方式（新会话先读这一节）

**新建对话时**：点输入框左下角「选择工作空间」→ 选到本目录 `<工程目录>`。这样本文件与工程文件自动进入上下文，无需重新说明。若未指定工作空间，WorkBuddy 会新建带时间戳的临时目录，工程与记忆都会分散——不要这样开。

**东西分别在哪**：

| 位置 | 内容 |
|---|---|
| 本地磁盘 `desktop-pet/`（本目录） | 代码、工具链、数据契约、资产副本、本文件 |
| 项目资产盘 tdrive（云端） | 原始 `pet.json`、`spritesheet.webp`、资产库使用指南.pdf |
| 用户级 `~/.workbuddy/MEMORY.md` | 项目索引，指向本目录 |

本地磁盘与资产盘**不互通**，需显式上传/下载。代码不上传资产盘；资产盘只放源资产与可分享文档。

**会话结束前必须做**：把本轮决策与进度写回本文件（尤其"当前状态"表与变更日志）。会话自身的记忆只在本工作区有效，换会话即不可见。

## 1. 项目是什么

解析 Codex / ChatGPT 宠物包（`pet.json` + `spritesheet.webp`），构建自研 **Windows 桌面宠物运行时**，把 agent 的活动状态可视化为一只可交互的桌宠。

宠物是**活动状态的可视化代理**，不是电子宠物游戏。

## 2. 当前状态（一句话）

资产分析与数据契约已完成，**尚未写任何运行时代码**。下一步是 M0 技术验证（窗口层 spike）。

| 里程碑 | 状态 | 说明 |
|---|---|---|
| M0 技术验证 | ⬜ 未开始 | 透明置顶 + 逐像素穿透 + NOACTIVATE + 全屏检测 |
| M1 MVP 渲染闭环 | 🟨 前置就绪 | 数据契约、锚点、映射、验证器已完成；渲染层代码未写 |
| M2 对标交互面 | ⬜ 未开始 | 控制条、快捷键、多会话聚合、宠物管理 |
| M3 差异化 | ⬜ 未开始 | 行为层、状态源生态、宠物包工具链产品化 |

## 3. 文件地图

```
desktop-pet/
├── PLAN.md                      ← 本文件，接续入口
├── Windows桌面宠物开发方案.html   ← 完整设计文档（13 章）
├── 动画映射验证器.html            ← 已验证帧率/锚点/状态映射；M1 渲染层可直接移植其逻辑
├── pet-spec.html                ← 规格说明页（含模板图引导）
│
├── pet.json                     ← 原生清单，与 Codex 格式严格一致，禁止加自研字段
├── spritesheet.webp             ← 8×9 图集，1536×1872
├── behavior-map.json            ← sidecar：行→状态→帧列、逐帧 bbox、语义别名
├── desktop-pet.json             ← sidecar：运行时参数（fps、锚点、statusMap、交互、行为）
├── anchor-report.{json,txt}     ← 锚点测量产物
├── atlas-map.png                ← 图集可视化
│
├── tools/
│   ├── pet_sheet_probe.py       ← 逐格扫 alpha，实测帧数与内容包围盒
│   ├── anchor_calibrate.py      ← 生成锚点与 offsetY（自动生成，禁止手写）
│   ├── pet_spec.py              ← 规格常量与判定
│   ├── validate_pet.py          ← 宠物包合法性校验（门禁）
│   ├── make_pet_template.py     ← 生成空白模板图
│   └── make_demo_pet.py         ← 生成演示宠物包
├── examples/
│   ├── demo-pet/                ← 合法样例
│   └── broken-pet/              ← 非法样例，用于回归测试
└── assets/
    └── pet_atlas_blank_{v1,v2}.png / pet_atlas_guide_{v1,v2}.png
```

## 4. 已锁定的技术决策（不再重复讨论）

1. **宿主选型**：M1–M2 用 **Electron**（与 ChatGPT 桌面端同栈，复刻行为试错成本最低）；M2 之后按需评估平移 **Tauri 2**（常驻内存 80–160 MB vs 200–400 MB）。明确排除 Unity/Godot（逆着框架走）。
2. **内核与宿主解耦（硬性架构纪律）**：宠物内核（状态机 + 仲裁器 + 精灵播放器）写成**纯 TypeScript，不 import 任何 Electron API**。宿主适配层收窄为四个接口：`createOverlayWindow()` / `setClickThrough(mask)` / `registerHotkey()` / `tray()`。状态源走本地 HTTP 或 stdio IPC。违反此条 = 永久失去迁移选项。
3. **不污染 `pet.json`**：自研参数一律走 sidecar（`behavior-map.json` + `desktop-pet.json`），保证资产双向流通。
4. **行级锚点**：`groundY = 202`（全状态最深触地点），各状态按 `offsetY` 补偿。**禁止逐帧基线锁定**——会抹平奔跑步态与跳跃腾空弧线。
5. **点击穿透**：用窗口过程拦截 `WM_NCHITTEST` 采样 alpha。**不用** `setIgnoreMouseEvents` + 前端 `elementFromPoint` 回传（穿透态收不到事件，快速划过多屏会漏判）。
6. **位移速度按屏幕像素/秒归一化**（默认缩放 ~96 px/s），否则缩放后观感不一致。

## 5. 已实测的资产事实（宠物 taotao-new，V1）

- 网格 8 列 × 9 行，1536×1872，单格 192×208。
- 实测帧数 `[6,8,8,4,5,8,6,6,6]`，与 Codex Pets V1 官方规格**逐行一致**；总 57 帧。
- 内容在单元格内横纵居中（实测 cx≈95.5，中心 96），无网格线/标签。
- 锚点基线：idle 200、running-right 183、running-left 182、waving 201、jumping 201、**failed 141**（趴卧，需补偿 61 px，否则悬空）、waiting 200、running 202、review 202。
- 彩蛋：row 7（原生语义 running）作者画了生日派对帽 + 蛋糕；row 8（原生 review）画了小厨师。可通过 alias 覆盖复用。

## 6. 下一步：M0 技术验证 spike

写业务代码之前，先用最小 demo 证明窗口层可行。**这是唯一可能推翻选型的阶段。**

必须验证三项：
1. 透明置顶窗口 + `WM_NCHITTEST` 逐像素穿透
2. `WS_EX_NOACTIVATE` 生效——点击宠物不夺焦点
3. 全屏应用检测与自动隐藏

**退出判据**：三项全部通过。任一失败 → 换栈重做 M0，不得带着风险进 M1。
**产出**：spike 工程 + 一页结论记录（含失败项与替代方案），结论回写本文件。

## 7. 关键风险（详见方案 §12）

| 风险 | 等级 | 缓解 |
|---|---|---|
| 逐像素穿透在所选栈中难实现 | 致命 | M0 优先验证；储备三种实现路线 |
| 无真实状态源，产品空转 | 致命 | M1 起把状态源抽象成事件流；尽早接通至少一个真实 hook |
| 被误判为流氓软件（遮挡/抢焦点） | 高 | 全屏让位 + 不夺焦点 + 托盘可退出，硬性验收项 |
| 全局快捷键被占用 | 中 | 注册失败即提示 + 自动降级 + 可自定义 |
| 恶意宠物包 | 高 | 路径穿越防护 → 魔数校验 → ≤20 MiB → 尺寸精确匹配 → 字段白名单 |

## 8. 工作方式约定

1. 一条交付线 = 一个工程目录，按产品命名，不按会话命名。会话可随意开。
2. 稳定领域知识 → 用户级技能；进度与决策 → **本文件**，会话结束前必须写回。
3. 调研会话只读，不写工程代码；写代码串行。
4. 并行会话的产出必须落盘，不得只留在对话里。
5. 源资产与共享交付物放项目资产库；代码留在工作区并纳入版本管理。

## 9. 变更日志

- **2026-09-15**：合并两条并行会话的产物（运行时工程 + 规格工具链）为统一工程；建立本文件作为单一事实来源。原会话目录保留只读，不再写入。
- **2026-09-15**：补建跨会话接续机制——新增本文件 §0、工程内 `AGENTS.md`、用户级 `~/.workbuddy/MEMORY.md` 项目索引。
