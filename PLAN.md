# 桌面宠物项目 · PLAN（当前状态索引）

> 本文件只回答三件事：**现在到哪了、下一步做什么、细节去哪找**。开工前另读 `AGENTS.md`。
>
> **2026-09-18 重构**：把编年史、操作手册、里程碑详述、决策摘要**整段迁出**（1096 行 → 约 200 行）。
> **内容一条没删，全在 `docs/` 下**，见 §0 路由表。
> 理由：本文件 + `AGENTS.md` 是**每次新会话必读的固定税**（原先合计 126 KB），
> 而收工协议又要求往它里面追加 —— 让它继续膨胀下去，交接成本会线性增长。
>
> **最后更新：2026-09-21**。M3 **五块全部结项**（判据 9 通过，ADR 031）；**当前唯一待办 = #13 打包那轮**。
> Proma 通道**仍冻结**（解冻条件一已满足，但用户明确不先动 Proma）。

## 0. 详细内容路由表（先查这里，再决定读哪份）

| 想找什么 | 去哪 |
|---|---|
| **上一个会话交接（最新）** | `docs/HANDOFF-2026-09-21.md`（含接续第一件事、踩坑清单、环境事实） |
| **怎么跑起来、怎么操作**（启动/构建/喂状态/托盘/气泡/控制条/快捷键） | `docs/how-to-run.md` |
| **人工验收清单**（每个里程碑走一遍） | `docs/acceptance.md` |
| **M3 各块的详细说明**（状态源生态、状态↔动作复核、缺陷修复） | `docs/status/m3.md` |
| M0–M2 的开工顺序与已完成区块（历史） | `docs/status/m0-m2.md` |
| **为什么这么决定** | `docs/decisions/INDEX.md`（一行一条）→ 对应 ADR 全文 |
| 完整变更历史（编年史，按日期追加） | `docs/changelog.md`（本文件 §7 只留最近三条） |
| 完整设计（13 章：选型/架构/状态机/阶段计划/风险/验收） | `Windows桌面宠物开发方案.html` |
| **宠物包规格**（图集规格、行语义、常见坑） | 用户级技能 `codex-pet-pack`（权威来源，**不要重新调研**） |
| 资产实测数据（帧数、锚点、基线） | `docs/asset-facts.md` |
| **每个业务状态在屏幕上长什么样** | `docs/status-reference.png`（换宠物包后要重新生成） |
| 历史会话做过什么 | `journal/`（按日期追加） |
| 技术验证工程与回归探针 | `spikes/`（各目录有 README 说明怎么跑） |
| 运行时代码 | `src/`（kernel 内核 / source 状态源 / host 宿主 / main 入口 / renderer 渲染） |

**文档寿命**：`PLAN.md` / `AGENTS.md` 常读常更；`docs/decisions/` 只增不改（动到相关模块才读那一份）；
`journal/` 只追加；`spikes/` 结论已回写 ADR，可丢弃（`m2-hittest` 建议留着做回归）。
全部在本地磁盘**已进 git** —— 不存在"留在对话里用不上"的问题；反过来，
**只在对话里说过的结论才是会丢的**。

## 1. 接续三步

1. **新建对话**：输入框左下角「选择工作空间」→ 选 `desktop-pet`。
   **不要走"新建默认工作区"**（会生成带时间戳的目录，工程与记忆都会分散）。
2. **开工**：读本文件 + `AGENTS.md`。`AGENTS.md` 是**路由表** —— 它按"改了什么"指向
   `docs/constraints/` 下对应的约束文件，**不需要一次读完 40 条硬约束**。
3. **收工四件事**（缺一不可）：更新本文件 §3/§4 → 新增 ADR 到 `docs/decisions/` →
   追加 `journal/` 当日记录 → `git commit`。

**下次开工怎么说**（可直接复制到新对话）：

> 接 desktop-pet 项目。先读 `PLAN.md`（§3 状态表 + §4 挂起清单）和 `AGENTS.md`；
> 需要更细的上下文读 `docs/HANDOFF-2026-09-21.md`；宠物包规格读用户级技能 `codex-pet-pack`，
> 不要重新调研；已决策的事看 `docs/decisions/INDEX.md`，不要再讨论。
> M0–M2 已结项；M3 五块里四块已结项，**只剩判据 9**（要用户跑真实回合反馈体感）。
> 第一件事是**问用户判据 9 的四问**（卡顿 / 抢焦点 / 气泡烦不烦 / 快捷键误触），
> 并跑 `node tools/measure-latency.mjs --since=60` 取客观数字对照。
> 那之后再谈打包那轮（挂起 #13）。
> **不要再提议**：M2 ⑤ 多宠物、行为层忙碌动作与跨屏漫游、启动优化、Proma（都已明确搁置/冻结）。

## 2. 项目是什么

解析 Codex / ChatGPT 宠物包，做自研 Windows 桌面宠物运行时，把 agent 的活动状态
可视化成一只可交互的桌宠。宠物内核是纯 TS（不 import Electron），宿主适配层收窄为四个接口。
运行与操作细节见 `docs/how-to-run.md`。

## 3. 当前状态

进度：**M0 → M2 已全部结项；M3 五块全部结项（最后一块「状态源生态」于 2026-09-21 判据 9 通过）；M2 ⑤ 多宠物切换已搁置。**
**当前唯一待办 = 挂起 #13 打包那轮**（需用户先定形态：安装包 / 免安装）。

| 里程碑 | 状态 | 说明 |
|---|---|---|
| M0 技术验证 | ✅ 已完成 | 三项判据全过（`spikes/m0-window/`，ADR 004；"逐像素穿透"结论已被 ADR 008 修订） |
| M1 MVP 渲染闭环 | ✅ 已完成 | 2026-09-16 人工验收通过；三轮缺陷全修并实测（ADR 007/008/009） |
| M2 对标交互面 | ✅ 已完成 | ①状态源 ✅｜②托盘与右键 ✅｜③气泡·角标·快捷键 ✅｜④控制条 ✅｜⑤多宠物 ⏸ **搁置**（ADR 015） |
| M3 差异化 | ✅ 已完成 | 行为层 ✅（ADR 018/019）｜状态↔动作复核 ✅（ADR 021）｜缺陷修复与审查 ✅（ADR 022）｜挂起清账 ✅（ADR 023）｜#14 修复 ✅（ADR 024）｜**状态源生态 ✅（ADR 026 + 031）** |

**WorkBuddy 通道成品化 —— 已收口（ADR 031）**：两条判据都有结论。
① **判据 6**（ADR 026）：`recordEvent()` 加 `recvAt`，端到端延迟 = `recvAt - ts`。
最近一小时实测 **77 条实时事件：最小 2ms / 中位 62ms / p95 100ms / 最大 112ms**；
hook 通道 71 条中位 61ms。取数：`node tools/measure-latency.mjs --since=60`。
② **判据 9**（ADR 031，2026-09-21 用户四问）：卡顿 **完全没感觉** ✅｜抢焦点 **一次都没有** ✅｜
气泡 **不烦，正好** ✅｜`Win+Alt+P` **没用过快捷键** ⚠️ **未取得数据，不算通过**（"没用过"回答的是
使用频率，不是误触率）—— 并入 #13 打包那轮复核，不阻塞。
⇒ ADR 020 的止损（"判据 9 不过就砍事件集或弃用通道 A"）**不再触发**，通道 A 保住。

**最近三轮的一句话版**（详见 `docs/status/m3.md`）：
① 启动耗时 ≈32 秒，**93% 花在每次无条件全量构建**（`boot()` 只 322ms）—— 优化待你拍板；
② `ready` 消不掉有**两个独立成因**（面板谓词漏 `ready` + 双通道交替撤销确认位），都已修；
③ 挂起清单清账五项（双通道告警 / `isAckable` 下沉 / 时间边界探针 / preload 诱饵 / 隐藏停定时器）。

**已关闭，不要再提议**（理由见对应 ADR）：
M2 ⑤ 多宠物切换（ADR 015，缺素材与规格）｜行为层的"忙碌动作"（ADR 019，用户不认这个动机）｜
跨屏漫游（ADR 019，锁定"不跨屏"）｜Proma 适配器（**冻结中**，先做完 WorkBuddy 通道的成品）。

## 4. 挂起问题清单（2026-09-18 汇总）

| # | 问题 | 级别 | 状态 | 谁能推进 |
|---|---|---|---|---|
| 1 | **启动每次全量构建**（≈32s，93%） | 🟠 Major | **暂缓（用户决定）** | 用户：现在改动频繁，"改完代码双击即生效"更值钱，**等代码改动少了再优化**。方案留在审查报告里 |
| 2 | **双通道互斥只有文档约束** | 🟠 Major | ✅ **已完成（ADR 030）** | 逐出已实施：**hook 优先于被动源**，低优先级上报被丢弃；`holdMs` 10 分钟释放作安全底线。告警（ADR 023）与逐出各自独立 |
| 3 | **`isAckable` 谓词两处各写一份** | 🟠 Major | ✅ 已完成（ADR 023） | 下沉内核 + 真值表 + 源码级断言钉住 |
| 4 | **`boot()` 949 行** | 🟡 Minor | **已处理（ADR 027）** | 见下方说明：**改成"可导航"而不是强行拆函数** |
| 5 | **`ready`/`stale` 时间边界无探针** | 🟡 Minor | ✅ 已完成（ADR 023） | `probe-ready-stale-boundary.mjs` 27/27 |
| 6 | **隐藏时 16ms/33ms 定时器仍空转** | 🟡 Minor | ✅ 已完成（ADR 023） | `spikes/m3-hidden-timers/` PASS |
| 7 | **preload 双产物诱饵文件** | 🟡 Minor | ✅ 已完成（ADR 023） | tsconfig 排除 + `tsconfig.preload.json` |
| 8 | **`badgeCount` 语义与注释轻微错位**（`running` 也计入） | 🔵 Nit | ✅ 已完成（ADR 026） | 实现是对的，改的是注释：它不限于"要求注意"那两类 |
| 9 | **`STATUS_TEXT` 缺 `satisfies Record<PetStatus,string>`** | 🔵 Nit | ✅ 已完成（ADR 026） | 漏写一种状态从此变成编译错误 |
| 10 | **`ready` 落点口径**（第 7 行生日 vs 第 8 行小厨师） | — | ✅ **已确认（ADR 027）** | 用户拍板：**保持第 8 行小厨师** = 现状，无需改代码 |
| 11 | **M3 第二块收尾**：判据 9 / 判据 6 / Proma 适配器 | — | ✅ **已完成（ADR 031）** | 判据 6 中位 **62ms**、判据 9 三问通过（第四问"没用过快捷键"**未取得数据**，并入 #13）。Proma 仍**冻结**（解冻条件一已满足，但用户明确不先动 Proma） |
| 12 | **HTTP 状态源未实现**（接口已就位） | — | **降级为"不做"** | 评估：已有 hook + 文件两条通道覆盖现有需求，**没有任何 agent 需要第三种**。接口留着，将来需要时再加 —— 不再占清单 |
| 13 | **打包那轮**：注册表值名复核 + 托盘图标随包分发 | — | **保留（打包时必经）** | 不只是"值名复核"：**它也是"无控制台 + 关窗存活"的正解**（打包成 GUI exe 后由 explorer 直接启动，不存在父控制台这回事）。触发条件 = 开始打包 |
| 14 | **「（已过期）」行长期驻留** | 🟡 Minor | ✅ 已修复（ADR 024） | 判为 bug：出口只改了仲裁输出、没改面板视图 |

**建议顺序**：**只剩一件事需要用户** —— **#13 打包那轮**（先定形态：安装包 / 免安装）。
#1 已由用户决定暂缓；其余全部结项（#11 于 ADR 031 结项）。

**关于 #4 的处理方式（与审查报告的建议不同，理由写在 ADR 027）**：
审查报告建议"拆三个装配函数"，**我没有照做**。看完代码后的判断是：这些段共享同一批对象
（`overlay` / `arbiter` / `bubble` / `bar` / `prefs`），且有严格先后（快捷键必须早于托盘菜单，
否则踩 TDZ）。拆成模块级函数就得把这批对象打包成 ctx 传来传去 —— 隐式耦合变显式但更啰嗦，
而**启动路径的任何改动都要跑五套真实窗口回归**。
改为**让它可导航**：`boot()` 顶部加了装配索引（①–⑪ 的职责与顺序），分节标记统一成 `═══` 可搜格式，
顺手修掉一处被吞掉的注释。真出现"某一段要被复用"时再拆。


## 5. 目录地图

```
desktop-pet/
├── PLAN.md / AGENTS.md        入口（本文件 = 状态索引；AGENTS.md = 约束路由表）
├── docs/
│   ├── how-to-run.md          怎么跑起来、怎么操作
│   ├── acceptance.md          人工验收清单
│   ├── changelog.md           完整变更历史（编年史）
│   ├── status/                m0-m2.md（历史）/ m3.md（M3 各块详述）
│   ├── decisions/             ADR 全文（只增不改）+ INDEX.md（一行一条检索）
│   ├── constraints/           硬约束按主题分家（AGENTS.md 指向这里）
│   ├── design/                开工前的接口设计（先设计后写码）
│   └── *.png                  status-reference / scale-compare / row-7-vs-8
├── journal/                   会话日志，按日期追加
├── spikes/                    技术验证工程（各目录有 README）
├── src/                       运行时代码（kernel / source / host / main / renderer / shared）
├── tools/                     脚本（构建辅助 / 状态层单测 / hook 客户端与安装器 / 资产分析）
├── 启动桌宠.bat / 喂状态.bat   一键启动 / 人工验收时喂状态
├── pet.json / spritesheet.webp / behavior-map.json / desktop-pet.json   数据契约与资产（引用网，勿移动）
└── assets/ / examples/        模板底稿与图标 / demo 与非法样例（回归用）
```

## 6. 关键风险（未解除的）

| 风险 | 等级 | 缓解 |
|---|---|---|
| 被误判为流氓软件 | 高 | 全屏让位 + 不夺焦点 + 托盘可退出 + 不写服务/计划任务 + 控制条不出现在任务栏与 Alt+Tab。**待办：打包那轮复核注册表值名与托盘图标分发**（#13） |
| 恶意宠物包 | 高 | 路径穿越 → 魔数 → ≤20 MiB → 尺寸匹配 → 字段白名单。**状态文件同样是投毒点**（ADR 010） |
| 全局快捷键被占用 | 中 | 注册失败即提示 + 自动降级 + 可自定义 |
| 依赖平台未文档化行为 | 中 | 已**五次**被坑到。**纪律：推断出来的结论一律写探针实测**；量渲染就量渲染本身（截屏差分），不用另一套引擎的近似值当判据 |

已解除的（逐像素穿透、全屏后失去交互、点桌面导致消失）见 `docs/status/m0-m2.md`。

## 7. 变更日志（最近三条 · 全文见 `docs/changelog.md`）

- **2026-09-21**：**启动方式修正（ADR 029，同一天修了两次）—— 推翻 ADR 028 的结论。**
  三方式各起 3 个进程、用 explorer 双击真脚本、`taskkill` 不带 /F 关窗实测：
  `start` 全部被带走、`Start-Process` 与 `DETACHED_PROCESS` 全部存活；
  随后又发现 `-ArgumentList '%CD%'` 的路径含空格被 PowerShell **按空白拆参数**
  （报 `Unable to find Electron app at <工程目录>`），
  改用 `-ArgumentList '.'` + `-WorkingDirectory`。**用户实测通过**：构建窗口自己关、宠物留在托盘。
  **两次都是"判据不纯"**：先测了不支持结论的量（新建控制台 ≠ 附加控制台），
  又用不含空格的路径去测含空格的真实场景。
- **2026-09-21**：**双通道逐出已实施（ADR 030）** —— `#2` 结项。规则：**按通道优先级，不按到达顺序**，
  hook 高于被动源；低优先级上报被**丢弃**（在所有记账之前）；`holdMs` 10 分钟释放作安全底线；
  无 `origin` 的人工喂状态永远放行。配置在 `desktop-pet.json → statusTimeouts.dominance`。
  顺带确认一条实施顺序：**告警必须在逐出之前**（否则逐出后冲突再也不报警）。单测 **319 项**。

- **2026-09-21**：**判据 9 有结论（ADR 031），M3 第五块「状态源生态」结项 —— M3 全部结项。**
  用户四问：卡顿**完全没感觉** / 抢焦点**一次都没有** / 气泡**不烦，正好** / `Win+Alt+P`
  **没用过快捷键**。第四问如实记**未取得数据**（"没用过"是使用频率，不是误触率），并入 #13 复核。
  客观对照：最近一小时 **77 条实时事件，中位 62ms**（hook 通道中位 61ms），无负值、无超标。
  ⇒ ADR 020 的"判据 9 不过就弃用通道 A"止损**不再触发**。
  **另记一条口径纪律**：事件占比 ≠ 气泡在屏时长（仲裁层有出口：`ready` 60s / `needs-input` 5min /
  `running` 气泡 2s）—— "判据不纯"第五次。

> 更早的变更见 `docs/changelog.md`（编年史，只增不改）。
