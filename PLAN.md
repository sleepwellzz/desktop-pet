# 桌面宠物项目 · PLAN（当前状态索引）

> 本文件只回答三件事：**现在到哪了、下一步做什么、细节去哪找**。
> 详细内容一律外迁 `docs/`，本文件保持一屏可读。开工前另读 `AGENTS.md`。
> 最后更新：2026-09-16（M2 第一步完成：状态源 → 仲裁器 → 动画链路打通；下一步 M2 ②托盘与右键菜单）

## 1. 三句话接续

1. **新建对话**：输入框左下角「选择工作空间」→ 从列表里选 `desktop-pet`。
   该条目已登记在 WorkBuddy 的 `workspaces` 表中（机制见 ADR 006）。
   **不要走"新建默认工作区"**——那会在默认根目录 `...\WorkBuddy\` 下生成带时间戳的子目录，
   工程与记忆都会分散。
2. **开工**：读本文件 + `AGENTS.md`。需要规格细节读用户级技能 `codex-pet-pack`，不要重新调研。
3. **收工四件事**（缺一不可）：更新本文件 §3/§9 → 新增 ADR 到 `docs/decisions/` →
   追加 `journal/` 当日记录 → `git commit`。

**运行与构建**：
- 给不熟悉命令行的操作者：双击工程根目录 `启动桌宠.bat`（自动构建并启动）。
  那个窗口**不要关** —— 它既是桌宠本体，也是日志窗口（状态推送与渲染层日志都打在这里）。
- 命令行：`npm run build && npm start`。
- **这个环境的 Bash shim 没有 npm**，用 `node tools/npm-run.mjs build`（或 `typecheck` / `start`）代替。
- 批处理必须保持纯 ASCII（`cmd` 按 GBK 读 `.bat`），且必须 `call npm.cmd`。
- **喂状态给宠物**（没有真实 agent 时，也是人工验收的入口）：
  - 图形方式：双击工程根目录 `喂状态.bat`。它是**循环菜单**：按数字喂一次 → 退回菜单 → 继续按，
    按 Q 才退出（不要反复重开窗口）。1 running / 2 ready / 3 blocked / 4 needs-input /
    5 running 再喂一次 / 6 idle / 7 clear / L 看文件。
  - 命令方式：`node tools/pet-hook.mjs <状态> [--title="…"]`。
  - 状态文件默认在 `C:\Users\<用户名>\.desktop-pet\status.json`（hook 不需要知道工程路径）。
- **动作外观查 `docs/status-reference.png`**（`python tools/make-status-reference.py` 生成）：
  业务状态 → 动画状态 → 精灵图行 → 屏幕上实际长什么样。**不要凭状态名猜外观** ——
  这只包的 `running`（运行中）落在第 7 行，而作者在那里画的是**生日姿态**，真正的跑动在第 1、2 行
  （当前无业务状态映射过去）。换宠物包后要重新生成这张图。

**人工验收怎么做**（每轮里程碑结束时走一遍，步骤与期望写在
`journal/2026-09-16-状态源接入.md` 的"人工验收"一节）：启动桌宠 → 双击 `喂状态.bat` →
按 1..7 逐个喂状态 → 对照期望动作与日志。重点看**粘滞**（喂 needs-input 后再喂 running，
宠物必须保持等待）与一次性动作的**落点**（ready 要挥手后落到 review，不是回待机）。

**下次开工怎么说**（可直接复制到新对话，替换掉方括号里的内容）：

> 接 desktop-pet 项目。先读 PLAN.md（尤其 §3 状态表与 §4）和 AGENTS.md；需要宠物包规格细节读用户级技能 `codex-pet-pack`，不要重新调研；已决策的事看 `docs/decisions/`，不要再讨论。
> M2 第一步（状态源 + 真实 hook）已收尾，这次做 M2 的第二步：[托盘菜单与右键菜单（隐藏/切换宠物/大小/开机自启/退出）]。
> 先给我接口设计再动手写代码。

**两套文件系统**：本地磁盘（本目录，放代码与文档）与项目资产盘 tdrive（云端，放源资产与可分享文档）**不互通**。代码不上传资产盘。

## 2. 项目是什么

解析 Codex / ChatGPT 宠物包（`pet.json` + `spritesheet.webp`），构建自研 **Windows 桌面宠物运行时**，
把 agent 的活动状态可视化为一只可交互的桌宠。宠物是**活动状态的可视化代理**，不是电子宠物游戏。

## 3. 当前状态

进度：**M0 → M1 已完成；M2 进行中（第一步已完成）；M3 未开始。**

| 里程碑 | 状态 | 说明 |
|---|---|---|
| M0 技术验证 | ✅ 已完成 | 三项判据全过。见 `spikes/m0-window/` 与 ADR 004（其中"逐像素穿透"结论已被 ADR 008 修订） |
| M1 MVP 渲染闭环 | ✅ 已完成 | 2026-09-16 人工验收通过（六项判据全过）。三轮缺陷全部修完并实测：全屏判据（ADR 007）、命中区域与窗口尺寸漂移（ADR 008）、全屏恢复后输入通路（ADR 009） |
| M2 对标交互面 | 🔄 进行中 | ①状态源抽象 + 真实 hook ✅（ADR 010，**含人工验收通过**）｜②托盘与右键菜单 ⬜ 下一步｜③控制条与快捷键 ⬜ |
| M3 差异化 | ⬜ 未开始 | 行为层（含"忙碌动作"）、状态源生态、宠物包工具链产品化 |

**M2 ① 已完成（2026-09-16）**：`状态源 → 仲裁器 → 动画` 链路打通，**人工验收通过**。
仲裁器在主进程（全应用唯一真值），渲染层退化为哑渲染层；第一个 hook 走状态文件 + 监听。
实测：离线单测 58 项断言全过、端到端六步核对到**实际绘制的帧行号**、回归探针两轮全过；
人工验收（用户执行）通过，事件流水留痕于 `~/.desktop-pet/events.jsonl`。
步骤与期望见 `journal/2026-09-16-状态源接入.md` 的"人工验收"一节，入口是工程根目录的 `喂状态.bat`。
详见 ADR 010。

**已定的产品决策（2026-09-16，用户拍板）**：`running`（运行中）**保持映射到第 7 行**，
不改用第 1 行的跑动姿态。理由：第 1、2 行是位移姿态，M3 的自主漫游要用；`Running → 第 7 行`
是规格的官方行语义；"第 7 行画成生日"只是这只包的作者画法，换包即正常。
**"运行中看起来像在忙"这个诉求交给 M3 行为层提供忙碌动作。**

**已定型的运行参数**：缩放 **0.7**（来自 `desktop-pet.json` 的 `render.defaultScale`；实测窗口内容区
134×146 DIP = round(192×0.7) × round(208×0.7)，与之一致）、命中区域 = 精灵轮廓、
窗口 134×146 DIP 内容区、`hitTestAlphaThreshold = 16`；
仲裁参数 = 最短展示 400ms / 变化限流 500ms / 粘滞上限 5min / 会话静默兜底 15min。

> 更正（2026-09-16）：本文件此前写"缩放 0.75、窗口 144×156"，与实现不符 —— ADR 008 里
> "1.0→0.75"是当时的取值，现行值是 0.7。ADR 只增不改，**以本文件为准**。

## 4. 下一步：M2 对标交互面

**M1 人工验收：2026-09-16 通过**（六项：出现 / 拖动 / 单击挥手 / 轮廓外穿透 / 全屏让位 /
**全屏恢复后仍可拖动**；挂机内存可接受）。判据细节与"怎么测才准"的提示留在
`journal/` 与 §9 变更日志里，需要时再翻。

**开工顺序建议**（按风险从高到低排，不是按功能好看程度）：

1. ~~**状态源抽象 + 接一个真实 hook**~~ ✅ **已完成（2026-09-16，ADR 010，人工验收通过）**。
   链路上线后，风险表第一位的"产品空转"已降级。遗留的小事：HTTP 状态源未实现（接口已就位）；
   气泡与角标**只算不画**（见下方 ③ 的说明）。
2. **托盘菜单与右键菜单**（隐藏宠物 / 切换宠物 / 宠物大小 / 重置大小 / 开机自启 / 退出）。
   `desktop-pet.json` 的 `interaction.rightClickMenu` 已列好菜单项，照着实现。
   注意"退出"必须可用（硬性验收项：不能被误判为流氓软件）。
   仲裁器的输出已在 `StatusArbiter.state` 里现成可用，托盘提示直接取它，不要另算一份。
   **接口设计见 `docs/design/m2-tray-menu.md`**（开工前先读，含实测结论与不做的事）。
3. **悬浮控制条 + 快捷键**（`interaction.hideShortcut`）。`Win+Alt+P` 有冲突风险，
   必须支持自定义，且注册失败要提示并降级。
   **气泡与角标随这一轮一起做**：它们要占用宠物以外的画布区域 ⇒ 必须改窗口尺寸 ⇒
   直接触碰 ADR 008/009 刚修好的命中区域与输入通路，不能和状态链路改动混在同一轮。

**改完窗口生命周期相关代码，必须重跑**：
`node spikes/m2-hittest/run.mjs probe-fs-verify.js` —— 验证全屏让位与恢复后的交互没被打断。
M2 会新增窗口（控制条、托盘），这类改动最容易碰坏 ADR 009 修好的输入通路。

**改完状态层（`kernel/status.ts` / `source/*` / 仲裁与映射相关）必跑**：
`node tools/status-arbiter.test.mjs`（离线单测，秒级）；
动了主进程/渲染层的接线再补 `node spikes/m2-status/run-status-e2e.mjs`（端到端，约 45 秒）。

## 5. 信息去哪找

| 想找什么 | 去哪 |
|---|---|
| 完整设计（13 章：选型、架构、状态机、阶段计划、风险、验收） | `Windows桌面宠物开发方案.html` |
| 规格知识（图集规格、行语义、常见坑） | 用户级技能 `codex-pet-pack` |
| 资产实测数据（帧数、锚点、基线） | `docs/asset-facts.md` |
| 为什么这么决定（一决策一文件，只增不改） | `docs/decisions/`：001 选型 / 002 契约与锚点 / 003 目录暂不整理 / 004 穿透方案 / 005 工程结构与构建链 / 006 工作区识别 / 007 M1 验收缺陷（全屏判据 + 命中阈值缺口）/ **008 命中区域显式判定与窗口尺寸漂移** / **009 全屏恢复后输入通路失效** / **010 状态源抽象与仲裁器** |
| 状态怎么来的、仲裁规则、hook 怎么接 | ADR 010 + `spikes/m2-status/README.md` + `src/kernel/status.ts` 顶部注释 |
| 工作区为什么"不在列表里"/为什么落到时间戳目录 | `docs/decisions/006-工作区识别与登记机制.md` |
| 全屏让位/点击命中/拖动的手感怎么测的、怎么复现 | `spikes/m2-hittest/README.md` |
| 缩放前后对比图（1.0 vs 0.75） | `docs/scale-compare.png` |
| **每个业务状态在屏幕上长什么样**（含行号与真实绘制方式） | `docs/status-reference.png`（`python tools/make-status-reference.py` 重新生成） |
| 历史会话做过什么 | `journal/`（按日期追加，最新在最后） |
| 技术验证工程 | `spikes/`：m0-window 窗口层 / m1-shot 自检截图 / **m2-hittest 命中区域·尺寸漂移·全屏输入通路** / **m2-status 状态链路端到端** |
| 窗口层实测结论与踩坑清单 | `spikes/m0-window/README.md`（开工前必读，能省掉一批已知坑） |
| 运行时代码 | `src/`（kernel 内核 / **source 状态源** / host 宿主 / main 入口 / renderer 渲染），构建链见 ADR 005 |

**这些文档的寿命（要不要一直留着）**：

- `PLAN.md` / `AGENTS.md`：**常读常更**，接续的唯一入口，每轮开工先读、收工必改。
- `docs/decisions/*`：**只增不改**的决策档案。平时不必读，动到相关模块时读对应那一份。
- `journal/*`：只追加的历史台账。只在"当时为什么这么做"变成疑问时才翻。
- `spikes/*`：验证工程，**可丢弃**——结论都已回写到 ADR，丢了不丢信息。
  （`spikes/m2-hittest/` 建议留：改窗口生命周期代码时要重跑它做回归。）
- `docs/asset-facts.md`、`docs/scale-compare.png`：参考资料。

全部在本地磁盘、**已进 git**。它们不依赖任何对话——新对话只要能打开这个工程目录就能看到，
所以不存在"留在对话里用不上"的问题；反过来，**只在对话里说过的结论才是会丢的**。

## 6. 锁定决策摘要（详见 ADR）

1. **Electron 先行 + 内核纯 TS 解耦**，保留 Tauri 2 迁移路径。（ADR 001）
2. **不污染 `pet.json`**，自研参数走 sidecar（`behavior-map.json` / `desktop-pet.json`）。（ADR 002）
3. **行级固定锚点** `groundY = 202`，禁止逐帧基线锁定。（ADR 002）
4. **根目录暂不整理**，因存在相对引用网。（ADR 003）
5. 点击穿透的实现方式几经修订（**别再用前两条**）：
   ~~`WM_NCHITTEST` 采样 alpha~~ → ~~`WS_EX_LAYERED` 原生命中测试~~（ADR 008 证明其生效区域是整个窗口矩形）
   → **命中判定由渲染层按当前帧精灵 alpha 显式给出**，窗口常态 `setIgnoreMouseEvents(true)`
   整窗穿透，主进程 16ms 轮询光标驱动采样。（ADR 008）
6. **位移速度按屏幕像素/秒归一化**（默认缩放 0.7）。
7. **渲染层与 preload 用 esbuild 打包**，内核纯 TS 不 import electron，init 走"渲染层报到 → 主进程下发"握手。（ADR 005）
8. **命中区域 = 精灵轮廓**（单点判定，不做邻域膨胀）；窗口移动一律用 `setContentBounds` 并复位内容尺寸，
   防止 150% 缩放下 DIP↔物理取整误差累积。（ADR 008）
9. **窗口每次重新显示后必须重载渲染层页面**（`overlay.reload()`）+ 强制重报命中状态。
   实测 `hide()` → `showInactive()` 会切断 Windows 对**真实鼠标按钮事件**的路由（移动事件不受影响），
   只有重建渲染层能可靠恢复。（ADR 009）

## 7. 目录地图

```
desktop-pet/
├── PLAN.md / AGENTS.md        入口与约定
├── 启动桌宠.bat / 喂状态.bat   一键启动 / 人工验收时喂状态
├── package.json / tsconfig.json   构建入口（npm run build / npm start）
├── Windows桌面宠物开发方案.html  完整设计
├── 动画映射验证器.html / pet-spec.html
├── pet.json / spritesheet.webp / *.json   数据契约与资产（引用网，勿移动，见 ADR 003）
├── anchor-report.* / atlas-map.png
├── src/                       运行时代码（kernel / source / host / main / renderer / shared）
├── dist/                      构建产物（git 忽略）
├── docs/
│   ├── asset-facts.md         实测事实
│   ├── scale-compare.png      缩放对比图
│   ├── decisions/             ADR：一决策一文件，只增不改
│   └── research/              调研产出
├── journal/                   会话日志，按日期追加
├── spikes/                    技术验证工程，可丢弃但结论要回写
│   ├── m2-hittest/            真实点击探针（命中区域/尺寸漂移/全屏输入通路）
│   └── m2-status/             状态链路端到端探针（回读实际绘制的帧行号）
├── tools/                     分析、校验、构建辅助脚本（含 npm-run.mjs / pet-hook.mjs / 状态层单测）
├── examples/                  demo-pet（合法）/ broken-pet（非法，回归用）
└── assets/                    模板底稿 v1/v2
```

## 8. 关键风险（详见方案 §12）

| 风险 | 等级 | 缓解 |
|---|---|---|
| ~~逐像素穿透难实现~~ | 已解除 | 改为自研显式命中判定（ADR 008），实测误吃 0 |
| ~~全屏让位后宠物失去交互~~ | 已解除 | 恢复时重载渲染层（ADR 009），两轮外部全屏实测通过 |
| ~~无真实状态源，产品空转~~ | **已降级** | M2 ① 打通 `状态源 → 仲裁器 → 动画`（ADR 010）：文件 hook 可用、端到端实测通过。**仍待验证的是"真 agent 的 hook 配起来好不好用"**（目前只验证了链路，没有真实 Codex/Claude Code 跑一遍） |
| 被误判为流氓软件 | 高 | 全屏让位 + 不夺焦点 + 托盘可退出（硬性验收项）。**M2 ② 的"退出"必须做出来才算过** |
| 全局快捷键被占用 | 中 | 注册失败即提示 + 自动降级 + 可自定义 |
| 恶意宠物包 | 高 | 路径穿越 → 魔数 → ≤20 MiB → 尺寸匹配 → 字段白名单。**状态文件同样是投毒点**，已按同一纪律处理（取值白名单 + 文本限长 + 解析失败不清零，ADR 010） |
| 依赖平台未文档化行为 | 中 | 已三次被真实坑到（隐藏/显示切断输入、DIP 取整漂移、探针读到残留日志）。约定：**凡是推断出来的结论都要写探针实测**，结论进 ADR 的"负面结论"节 |

## 9. 变更日志

- **2026-09-15**：合并两条并行会话产物为统一工程；建立本文件为单一事实来源。
- **2026-09-15**：补建跨会话接续三层机制（用户级 `~/.workbuddy/MEMORY.md` 项目索引 + `AGENTS.md` + §1）。
- **2026-09-15**：建立工程管理体系（`docs/` + `journal/` + `spikes/` + `.gitignore`）；初始化 git。
- **2026-09-15**：M0 窗口层验证完成（`spikes/m0-window/`）：逐像素穿透、不夺焦点、全屏让位三项判据全过。**修订 ADR 001 第 5 条**（ADR 004）。
- **2026-09-15**：排查"工作区不出现在列表/新建对话落到时间戳目录"，定位到 `workbuddy.db` 的 `workspaces` 表机制（ADR 006）。结论：不搬家。
- **2026-09-15**：M1 渲染闭环代码完成（ADR 005）。
- **2026-09-15**：M1 人工验收，修掉两项缺陷（ADR 007）：全屏让位判据因 koffi 结构体未回写而恒 false；规格里的 `hitTestAlphaThreshold` 从未接线。负面结论：`WindowFromPoint` 不能用来验证分层窗口的透明穿透。
- **2026-09-15**：第二轮人工复验，搭真实点击探针（169 采样点）实测出两条硬结论并据此重做命中方案（**ADR 008**）：① 生效命中区域 = 整个窗口矩形（窗口内 90/90 采样点全被吃掉，M0 的"逐像素穿透是原生行为"被推翻）；② 窗口每 `setPosition` 一次长 1 DIP（150% 缩放下取整累积），这正是"拖动后死区越变越大"的机制。改为：渲染层按当前帧 alpha 显式判定 + 16ms 光标轮询 + `setContentBounds` 复位 + 缩放 1.0→0.75。复测：误吃 43→**0**，拖动前后命中图逐点一致。
- **2026-09-15**：修掉"全屏让位回来后宠物点不动/拖不动"（**ADR 009**）。最小复现是 `hide()`+`showInactive()`；症状精确到"移动事件能进、按钮事件进不去"；用 `SendMessage` 证明是 Windows 路由问题；9 个补救动作全部无效、nudge 不可靠、`webContents.reload()` 稳定有效。改为恢复时重载渲染层 + 强制重报命中状态；顺带去掉无效的 `forward` 选项、消除 4 DIP 的 view/窗口错位。两轮外部全屏实测：全屏后单击与拖动均 ✅，无累积劣化。
- **2026-09-16**：**M1 人工验收通过**（六项判据全过，含"全屏恢复后仍可拖动"），M1 正式收尾。
  下一步按 §4 开工 M2（第一件事：状态源抽象 + 接真实 hook）。本文件同步刷新为可直接接续的索引。
- **2026-09-16**：**M2 ① 完成：`状态源 → 仲裁器 → 动画`链路打通（ADR 010）**。
  新增 `src/kernel/status.ts`（仲裁器：优先级/粘滞/最短展示+限流/多会话聚合/静默兜底，时钟可注入）、
  `src/source/`（状态源接口 + 状态文件适配器）、`tools/pet-hook.mjs`（hook CLI，兼手工模拟器）；
  仲裁器跑在主进程作为全应用唯一真值，渲染层退化为哑渲染层。
  第一个真实 hook 走状态文件 `~/.desktop-pet/status.json`（避开监听端口与防火墙提示），
  事件同时落 `~/.desktop-pet/events.jsonl` 供离线查证。
  实测：单测 58 项断言全过；端到端六步核对到渲染层**实际绘制的帧行号**（7/6/0/3→8/5/0）；
  回归探针两轮全过。过程中单测抓出两个真 bug（粘滞超时会立刻重新粘滞 = 超时无效；适配器把
  "读不懂的条目"误判为"会话消失"），已在 ADR 010 记录。负面结论：探针驱动会读到上一次运行的
  残留日志（需运行标识兜底）；`child.kill()` 带不走 detached 的 Electron（需 taskkill /T）。
