# 桌面宠物项目 · PLAN（当前状态索引）

> 本文件只回答三件事：**现在到哪了、下一步做什么、细节去哪找**。
> 详细内容一律外迁 `docs/`，本文件保持一屏可读。开工前另读 `AGENTS.md`。
> 最后更新：2026-09-18（**M3 第二块"状态源生态"判据 1/2/3 已拿到真实回合证据**——
> 单测 **70 项** + 既有 219 项 = **289 项全绿**。
> **下一步 = M3 第三块「状态↔动作全盘复核」**（用户要求"感觉状态有点乱"），
> **任务书已出：`docs/design/m3-state-animation-audit.md`**。
> **用户已拍板**：小厨师（第 8 行）= Agent running（取代 2026-09-16 的"running 用第 7 行"）；
> 另报告一个真缺陷：**气泡出现在屏幕中间**（根因在任务书 §3）。
> **三条已关闭，不要再提议**：M2 ⑤ 多宠物切换（ADR 015）；行为层的"忙碌动作"与跨屏漫游（ADR 019）

## 1. 三句话接续

1. **新建对话**：输入框左下角「选择工作空间」→ 从列表里选 `desktop-pet`。
   该条目已登记在 WorkBuddy 的 `workspaces` 表中（机制见 ADR 006）。
   **不要走"新建默认工作区"**——那会在默认根目录 `...\WorkBuddy\` 下生成带时间戳的子目录，
   工程与记忆都会分散。
2. **开工**：读本文件 + `AGENTS.md`。需要规格细节读用户级技能 `codex-pet-pack`，不要重新调研。
3. **收工四件事**（缺一不可）：更新本文件 §3/§9 → 新增 ADR 到 `docs/decisions/` →
   追加 `journal/` 当日记录 → `git commit`。

**运行与构建**：
- 给不熟悉命令行的操作者：双击工程根目录 `启动桌宠.bat`（自动构建并启动）。
  那个窗口**不要关** —— 它既是桌宠本体，也是日志窗口（状态推送与渲染层日志都打在这里）。
- 命令行：`npm run build && npm start`。
- **这个环境的 Bash shim 没有 npm**，用 `node tools/npm-run.mjs build`（或 `typecheck` / `start`）代替。
- 批处理必须保持纯 ASCII（`cmd` 按 GBK 读 `.bat`），且必须 `call npm.cmd`。
- **喂状态给宠物**（没有真实 agent 时，也是人工验收的入口）：
  - 图形方式：双击工程根目录 `喂状态.bat`。它是**循环菜单**：按数字喂一次 → 退回菜单 → 继续按，
    按 Q 才退出（不要反复重开窗口）。1 running / 2 ready / 3 blocked / 4 needs-input /
    5 running 再喂一次 / 6 idle / 7 clear default / **8 第二会话 b=running / 9 清掉会话 b** / L 看文件。
  - **怎么看 `+N` 角标**：角标统计的是"主状态之外的其它活动会话"，所以**一个会话永远得不出 +1**。
    按 `4`（default 需要输入，进入粘滞）再按 `8`（第二会话 b 在运行）→ 气泡右侧出现 `+1`；
    按 `9` 角标消失。2026-09-16 用户踩过这个坑：开两个终端分别按 1 和 4 是**没用的** ——
    两个终端写的都是同一个会话 `default`，角标自然恒为 0。
  - 命令方式：`node tools/pet-hook.mjs <状态> [--session=<id>] [--title="…"]`。
  - 状态文件默认在 `C:\Users\<用户名>\.desktop-pet\status.json`（hook 不需要知道工程路径）。
- **托盘与右键菜单**：托盘图标左键单击 = 收起/放出宠物；右键菜单项：状态行、快捷键、隐藏/显示、
  宠物大小（0.5–1.5）、重置大小、开机自启、退出。宠物上右键弹同一套菜单。
  缩放偏好与自定义快捷键存 `~/.desktop-pet/prefs.json`（不写进工程的 sidecar，避免版本库 churn）。
- **气泡**：状态文案 + 多会话 `+N` 角标，画在宠物上方（独立透明层，不可点）。
  `needs-input` 常驻，`ready`/`blocked`/`running` 分别显示 6/4/2 秒后自动收起；时长在
  `desktop-pet.json` 的 `bubble` 段。
- **控制条**（M2 ④ 新增，本项目第一个可聚焦窗口）：**唤出方式只有三条，全是显式动作** ——
  **右键宠物**（`toggle`）、`Win+Alt+P`、托盘菜单「控制条」；**一律抢焦点**，关闭：Esc / × /
  再唤出一次 / 点到别处（失焦 200ms）/ 宠物被隐藏。
  **没有悬停唤出**（2026-09-17 用户拍板删除，ADR 016）：桌宠常驻角落、光标是顺便掠过的，
  自动弹出必然变成"我没叫它它自己冒出来"；且左键单击要播动作，"停在宠物身上"不该带第二个含义。
  贴在宠物**下方** 8 DIP，宠物贴屏幕底边时自动翻到上方。面板显示：状态摘要 + 多会话明细
  （可点「确认」解除粘滞）+ 动作排（隐藏宠物 / **宠物名** / ⋯ 弹同一份原生菜单）。
  参数在 `desktop-pet.json` 的 `controlBar` 段。
  **不出现于任务栏与 Alt+Tab**（显式设了 `WS_EX_TOOLWINDOW`，见 ADR 014 负面结论 2）。
  **它是框架性功能**（用户定位）：三段式结构 + 白名单命令通道已冻结，以后加功能只加按钮/命令。
- **两处右键分工不同**（2026-09-17 起，ADR 016）：**宠物右键 = 控制条**；
  **托盘右键 = 完整菜单**（面板里的「⋯」也弹这份）。
- **左键单击宠物** = 播一次「拜一下」（第 3 行 `waving`，见 `docs/status-reference.png`）
  兼作对 `needs-input` 的确认（ADR 010）；**不唤出任何面板**。
- **行为层（M3，ADR 018 + ADR 019）**：空闲（主状态 = `idle`）时做**大动作** ——
  沿地平线走两步（第 1、2 行位移姿态，速度 96px/s @默认缩放，按缩放线性换算）、
  偶尔做个小动作（挥手 / 跳跃）、**空闲满 5 分钟趴下打盹**（复用第 5 行趴卧帧，不需要新素材）；
  有任务在跑时只做**小动作** —— 每 60–180 秒走 60–300px，**锚在进入任务状态时的位置附近**
  （目标由锚点算，不由当前位置算，否则会越踱越远；见 ADR 019）。
  拖动中、宠物被隐藏、全屏让位、控制条打开、系统「减少动态效果」下**都不动**；
  实测"抓住它 3 秒它一步都没动"。
  参数在 `desktop-pet.json` 的 `behavior` 段，**`enabled: false` 是一键总开关**
  （`idleRoam` / `busyPace` / `idleMicroActions` 各自还能单独关）。
  **不跨屏**（锁定范围，ADR 019）—— 拖到哪块屏就在哪块屏活动。
  **回归探针要带 `--no-behavior`**（宠物自己走会把位置类断言搅乱）。
- **全局快捷键**：默认 `Win+Alt+P` = **唤出/收起控制条**（M2 ④ 起语义变了，此前是切换宠物显示）。
  宠物隐藏时按下 = 先叫回宠物再唤出。
  **注册前必须把 `Win+...` 归一化成 `Super+...`**，否则注册返回 true 但永不触发（ADR 012）。
- **动作外观查 `docs/status-reference.png`**（`python tools/make-status-reference.py` 生成）：
  业务状态 → 动画状态 → 精灵图行 → 屏幕上实际长什么样。**不要凭状态名猜外观** ——
  这只包的 `running`（运行中）落在第 7 行，而作者在那里画的是**生日姿态**，真正的跑动在第 1、2 行
  （当前无业务状态映射过去）。换宠物包后要重新生成这张图。

**人工验收怎么做**（每轮里程碑结束时走一遍，步骤与期望写在
`journal/2026-09-16-状态源接入.md` 的"人工验收"一节）：启动桌宠 → 双击 `喂状态.bat` →
按 1..9 逐个喂状态 → 对照期望动作与日志。重点看**粘滞**（喂 needs-input 后再喂 running，
宠物必须保持等待）、一次性动作的**落点**（ready 要挥手后落到 review，不是回待机）、
**`+1` 角标**（按 4 再按 8：气泡右侧出现 `+1`；按 9 消失），以及**气泡文字**（左右居中等宽余量、
字的下缘不缺笔画）。

**M2 ④ 控制条要额外测这几条**（探针覆盖不了的只有"好不好用"；探针能覆盖的见 `spikes/m2-control/README.md`。
**2026-09-17 按新交互模型（ADR 016）重写**：旧清单里的"悬停出现"已删除、`+`/`↺` 按钮上一轮就撤掉了）：
1. **光标停在宠物上 2 秒 → 面板绝不出现**（悬停已彻底移除）。顺手确认在别的窗口打字不受影响；
2. **右键宠物 → 面板出现并抢到焦点**（能按 Esc 收起）；**再右键一次 → 收起**；
   光标仍停在宠物上，**不应该自己弹回来**；
3. 托盘右键 → 仍是**那份完整菜单**（与宠物右键不是一回事）：隐藏/显示、宠物大小、重置大小、
   清空状态会话、开机自启、退出都能点；
4. 按 `Win+Alt+P` → 面板出现/收起（宠物隐藏时按下应先叫回宠物）；
5. 面板上的会话行：喂两条会话（按 4 再按 8）→ 面板列出两行；点「确认」→ 该行按钮消失，
   宠物不再举着手；
6. 动作排：`隐藏宠物`（面板应跟着消失）/ 中间显示的是**宠物名**（不是百分比）/ `⋯` 弹出与托盘
   同一份菜单 —— 点完「⋯」菜单弹出的瞬间**面板不能消失**（消失了菜单会跟着没）；
7. 把宠物**拖到屏幕最底部** → 面板应翻到宠物**上方**，且不盖住气泡；
8. 菜单里的「清空状态会话」→ 面板当场**一行不剩**、菜单项变灰（这一条上一轮是坏的，ADR 017）；
9. 左键单击宠物 → 播一次「双手拜一下」；**不该冒出任何面板**。

**下次开工怎么说**（可直接复制到新对话；方括号里是你要替换的部分）：

> 接 desktop-pet 项目。先读 PLAN.md（尤其 §3 状态表与 §4）和 AGENTS.md；需要宠物包规格细节读用户级技能 `codex-pet-pack`，不要重新调研；已决策的事看 `docs/decisions/`，不要再讨论。
> M0–M2 已结项；M3 行为层已落地（ADR 018/019）；M3 第二块"状态源生态"进行中（ADR 020）：目标 agent = WorkBuddy + Proma，双通道架构已定，映射层/hook 客户端/安装器已落地，工程级 hook 已落盘，判据 1/2/3 都已在真实回合拿到证据（单测 70 项 + 既有 219 项 = 289 项全绿）。
> **这次做 M3 第三块：状态↔动作全盘复核。任务书已经写好了，先读它：`docs/design/m3-state-animation-audit.md`。** 用户原话"感觉现在宠物状态有点乱"，要求"首先全盘复核所有的宠物状态的触发条件和是否对应的这种情况"。任务书里备好了：九行素材占用总表（审计基线）、两处已看出的语义挤压（第 5 行趴卧被"受阻"和"打盹"两种相反含义共用；ready 是循环态且无出口）、两项待定稿（ready 落点 / 第 7 行归属）、一个真缺陷（气泡定位，含代码级根因与修法）、以及"改映射会打破哪些既有断言"的清单。
> **用户已拍板的方向**：小厨师姿态（第 8 行）应当表示 "Agent 正在 running" —— 即 running 改挂第 8 行、ready 不再占小厨师。**这条取代 2026-09-16 的旧决定**（当时定"running 保持映射到第 7 行"）。**开工前先跟用户确认这一句解读**，再连同上面两项待定稿一起定稿。
> 纪律：**先出设计（§4 第 9 条的任务书 + 补一版 ADR）→ 用户确认 → 再改 `desktop-pet.json` 与动作映射**；改映射会打破既有断言（清单在任务书 §4），必须同一轮改掉，否则回归假红；平台行为一律探针实测、不要推断。收工按 AGENTS.md 那四件事走。
> **三条已关闭，不要再提议**：M2 ⑤ 多宠物切换（ADR 015）；行为层的"忙碌动作"与跨屏漫游（ADR 019）。

**两套文件系统**：本地磁盘（本目录，放代码与文档）与项目资产盘 tdrive（云端，放源资产与可分享文档）**不互通**。代码不上传资产盘。

## 2. 项目是什么

解析 Codex / ChatGPT 宠物包（`pet.json` + `spritesheet.webp`），构建自研 **Windows 桌面宠物运行时**，
把 agent 的活动状态可视化为一只可交互的桌宠。宠物是**活动状态的可视化代理**，不是电子宠物游戏。

## 3. 当前状态

进度：**M0 → M2 已完成；M3 进行中（第一块"行为层"已落地）；M2 ⑤ 已搁置。**

| 里程碑 | 状态 | 说明 |
|---|---|---|
| M0 技术验证 | ✅ 已完成 | 三项判据全过。见 `spikes/m0-window/` 与 ADR 004（其中"逐像素穿透"结论已被 ADR 008 修订） |
| M1 MVP 渲染闭环 | ✅ 已完成 | 2026-09-16 人工验收通过（六项判据全过）。三轮缺陷全部修完并实测：全屏判据（ADR 007）、命中区域与窗口尺寸漂移（ADR 008）、全屏恢复后输入通路（ADR 009） |
| M2 对标交互面 | ✅ 已完成 | ①状态源抽象 + 真实 hook ✅（ADR 010）｜②托盘与右键菜单 ✅（ADR 011）｜③气泡与角标 + 全局快捷键 ✅（ADR 012、013）｜④控制条 ✅（ADR 014；交互模型重做 ADR 016、缺陷修复 ADR 017，探针与回归全绿）｜⑤切换宠物（多宠物）⏸ **已搁置**（缺素材与规格，ADR 015） |
| M3 差异化 | 🔄 进行中 | 行为层 ✅（ADR 018/019）｜状态源生态 🔄 判据 **1/2/3 均已拿到真实回合证据**（ADR 020；剩 Proma 通道）｜**状态↔动作全盘复核 ⬅ 下一块**（用户要求，任务书 `docs/design/m3-state-animation-audit.md`）｜宠物包工具链产品化待定；**跨屏漫游已明确不做**（ADR 019） |

**M3 第二块：状态源生态（2026-09-18，进行中，ADR 020）**：把"链路通"升级成"真能用" ——
让桌宠如实反映**真实 agent** 的活动，不再靠"喂状态"模拟。目标 agent 由用户定为
**WorkBuddy（主力）+ Proma（第二主力，Pi agent 架构）**，不是 Codex。

- **架构是两条通道，不是每个 agent 一套适配器**：
  **A · 事件驱动 hook**（WorkBuddy；且与前缀无关地覆盖 Codex / 将来的 Claude Code ——
  它们**是同一套事件名与 stdin 契约**）｜**B · 被动会话源**（Proma）。
- **新增硬规则：一个 agent 只由一条通道负责，两条通道互斥。** 两个**不同** agent 天然不冲突
  （仲裁器从 M2 ① 起就是多会话形状，`+N` 角标即为此）；
  会打架的是**同一个 agent 被两条通道同时盯**（hook 说 running、被动源说 idle，来回刷且无报错）。
  被动源是"降级来源"，**是切换不是叠加**。
- **`sessionId` 带来源前缀**（`wb:<pid>` / `proma:<uuid>`）+ **标题带来源名**
  （气泡只显示一个标题，两个 agent 同时在跑时必须分得清谁是谁）。
  副产品：用户"只想连某一个 agent"变成**一次按前缀过滤**，不是新机制。
- **闸门实测 = YES**：进程树显示桌面把 agent 跑在内嵌 `cli/bin/codebuddy --serve --session-id …` 上
  （该 uuid 与 `~/.workbuddy/sessions/<pid>.json` 完全一致）；CLI 的
  `DEFAULT_SCOPES = [USER, PROJECT, PROJECT_LOCAL]` 且跑 agent 的进程没带 `--setting-sources`
  ⇒ **工程级 `.codebuddy/settings.local.json` 会被读**。
- **施工顺序被实测推翻一次**：初稿把"先做被动源"当零风险的先赢，采样探针证明
  **WorkBuddy 的被动源只能给"存在性"、给不了"活动性"**（心跳粒度约 30 秒且与在不在干活无关；
  `tasks/`、`file-history/` 窗口内 0 次变更；`logs/` 是应用级噪声）
  ⇒ **通道 A 才是 WorkBuddy 的主路径**，B 降为可选。
- **落地**：`tools/codebuddy-hook-map.mjs`（纯函数映射层）、`tools/pet-hook-cb.mjs`（hook 客户端，
  **退出码恒 0**、`--dump` 取证）、`tools/install-codebuddy-hooks.mjs`（安装器，**默认 dry-run**、
  幂等、可卸载、带备份）、`tools/codebuddy-hook.test.mjs`（**70 项**）、
  `spikes/m3-status-sources/probe-workbuddy-live.mjs`（只读采样探针）。
  工程级 hook 配置**已落盘**（6 个低频事件；`PreToolUse`/`PostToolUse` 不挂 —— 每次 377ms）。
- **判据 2 ✅ 已通过（桌宠运行后拿到宠物侧时间线）**：`ready`(09:56:57.853) ← `Stop`(09:56:57.851)；
  `running`(10:02:42.862) ← `UserPromptSubmit`(10:02:42.860)；序列正确、不卡在 running。
  **`idle_prompt` 在真实环境被正确忽略**（hook 侧有、宠物侧无对应事件）⇒ 上面那个修复确认有效。
- **新发现 · 待用户拍板：`ready` 的落点 `review` 是循环终态、没有出口。**
  `ready` → 挥手 → **落到第 8 行"小厨师姿态"**（对照 `docs/status-reference.png` 确认，
  就是用户看到的"炒菜"），而 `review` 是 `loop: true` ⇒ **agent 干完一次活，宠物就永久炒菜**。
  `Stop` 现在每次回合结束都触发 ⇒ 稳定复现（以前没有真实 hook，所以没暴露）。
  支持"这是缺陷"的论据：**气泡对 `ready` 只显示 6 秒就收起，宠物却还在炒菜** —— 两者不一致。
  **但改成什么是产品决策**（`ready → 挥手 → 落到 review` 本身是 M2 ① 验收过的行为），
  选项 A（推荐：`review` 60 秒后自动回 `idle`）/ B（单击宠物 = 已读，复用 `ack()`）/ C（落点改回 idle，不推荐）
  见 ADR 020。
- **⚠️ 判据 6 的测量陷阱（已实测）**：`~/.desktop-pet/events.jsonl` 里的 `ts` 是**写侧产生时刻**，
  **不是宠物收到的时刻**（实测差 2ms，那只是 hook 进程内"落 payload → 写状态文件"的间隔）。
  **拿它算端到端延迟会严重低估。** 要算真实延迟得另找时间基准（主进程日志或探针加戳）。
- **附带事实**：`default` 会话是 8-16 人工验收的残留（title `manual-acceptance`），
  桌宠启动时短暂报了一次 `running`（10:01:15）、4 秒后变 `idle`；**是噪声不是缺陷**，
  菜单「清空状态会话」可清掉。
- **判据**：新增 **70 项** + 既有 **219 项** ⇒ **289 项全绿**。
- **下一步**：① 等用户拍板 `review` 的出口；② **判据 9（会不会拖慢你正常的活）** 现在桌宠开着，
  可以直接用下一个真实回合评估；③ 判据 6 换时间基准重测；④ 判据 3（`needs-input` 端到端，
  已确认可做）；⑤ Proma 的采样探针 → 适配器。

- **止损**：若判据 9 不过就砍事件集或整个弃用通道 A，本轮以"映射层 + 客户端 + 一条明确的负面结论"收尾。

**M3 · 行为层补完：任务中踱步（2026-09-18，ADR 019）**：用户拍板了 ADR 018 留的那个产品决定。

- **可以离开原位，但不超过几百像素**：有任务在跑（running / needs-input / blocked / ready）时，
  宠物每 **60–180 秒**沿地平线走 **60–300px**，方向朝**工作区中心**那一侧
  （默认蹲在右下角 ⇒ 观感就是"往左走几步"）；
- **锚点约束是结构性的**：目标由"进入任务状态时的位置"算，不由当前位置算 ——
  按当前位置累加会越踱越远，让"不超过几百像素"在几分钟后悄悄失效且无任何报错；
- **不作"忙碌动作"**：早先记的"运行中看起来像在忙"这条**待办作废**（用户不认这个动机），
  名字也不再使用；
- **不跨屏（锁定范围）**：只在宠物所在显示器的工作区内活动 ⇒ 拖到哪块屏就在哪块屏活动。
  用户明确暂不考虑多屏；
- **有任务时不计打盹**（`idleSinceAt` 恒为 null）；抑制条件与「减少动态效果」同空闲模式。

**M3 第一块：行为层（2026-09-17）**：宠物从"站在角落里循环待机"变成"**没人用它时像个活物**"。

- **只在主状态 = `idle` 时做大动作**：有任务在跑（running / needs-input / blocked / ready）时只踱小步
  （2026-09-18 由 ADR 019 修订，此前是"原地不动"）；
- **自主漫游**：每 25–90 秒沿地平线走 80–320px（速度 96px/s @默认缩放，按缩放线性换算，
  保证放大缩小后"每秒几个身位"的观感一致）；目标点**夹进工作区**，贴边就换方向；
- **微动作**：每 40–150 秒挑一个候选（挥手 / 跳跃）播一次；
- **打盹**：空闲满 5 分钟落 `failed`（复用趴卧帧），**单击或拖动即醒**、重新计时；
- **四条抑制**：拖动中 / 宠物被隐藏 / 全屏让位 / 控制条打开 → 一步都不动；
  系统「减少动态效果」下不漫游不做微动作（打盹这种静态姿态仍然允许）。

分层沿用既有架构：规则 = 纯函数（`kernel/behavior.ts`，时钟与随机数可注入）｜接线 = `main/index.ts`｜
渲染 = 哑渲染层（接住 `CH.behavior` 覆盖，覆盖期间不收敛回主状态）。

判据：单测 **138 → 191 项**（新增 ⑭ 段 22 项）；新增真实窗口探针 `spikes/m3-behavior/run.mjs`
7 条全过（真的在走 / 只沿地平线走 / 不越界 / **位移行号与方向一致 8/8** / 抓住不动且松手恢复 /
控制条开着不动 / running 期间不动 / 打盹画第 5 行且单击能醒）。
**两条只有实测才发现的坑**：① 打盹计时若被漫游重置，"每 25–90 秒走一次"会让它**永远睡不着**
（屏幕上只表现为"它就是不打盹"）；② 自主行走会让既有的**位置类断言集体变脆** ——
7 个回归探针因此统一加了 `--no-behavior` 接缝。

**M2 ④ 交互模型重做（用户拍板，ADR 016）**：**悬停唤出整条路径删除** —— 桌宠常驻角落、光标是
"顺便"掠过的（拖窗口、去任务栏、切应用都会经过），任何自动弹出都必然产生"我没叫它它自己冒出来"
的体验，延迟只降频不改性质；而且左键单击要播动作，"停在它身上"不该带第二个含义。
现在唤出只有三条**显式**路径：**右键宠物** / `Win+Alt+P` / 托盘菜单「控制条」，一律**抢焦点**；
关闭：Esc / × / 再唤出一次 / 点到别处（失焦 200ms）/ 宠物被隐藏。
**两处右键分工**：宠物右键 = 控制条，托盘右键 = 完整菜单（面板的「⋯」也是这份）。
面板的动作排从"只读的 70%"换成**宠物名**（取 `pet.json` 的 `displayName`）。
`kernel/bar-policy.ts` 因此从"七条规则 + `armed`/`suppressUntil` 两个补丁位"缩到**五条规则**。
新增一条机制：**`⋯` 弹原生菜单期间屏蔽"失焦即收"**（原生菜单会夺焦，不屏蔽则面板与菜单一起消失）。

**M2 ④ 缺陷修复（ADR 017）**：① 快照里**没有 `ts`** 的会话此前被内核兜底盖章成"现在"，
于是昨天写的 `running` 在启动后"新鲜" 15 分钟（"一启动就显示某某在运行中"）——
改为用**快照的 mtime** 兜底，且这类条目不参与"ts 变了算心跳"那条分支；
② 菜单「清空状态会话」此前只清了文件：宠物确实松手了，但**面板照旧列两行 idle、
菜单照旧写「（2 条）」** —— 改为**文件 / 适配器记忆 / 仲裁器记录三处一起清**。

**M2 ③ 已完成（2026-09-16）**：状态**可读**了 —— 气泡层（独立窗口，不扩大宠物窗口）显示状态文案，
多会话时带 `+N` 角标；全局快捷键 `Win+Alt+P` 切换宠物显示/隐藏。
实测：快捷键真实击键触发；气泡按策略显示/2 秒收起/常驻（needs-input）；拖动宠物时跟随；
**新增窗口没吃掉任何点击**（命中图误吃 0）。详见 ADR 012。
**人工验收通过**（用户逐项测过快捷键、气泡、角标、跟随），并修掉两项观感缺陷（ADR 013）：
① 文字下缘被裁（`line-height: 1` 使行盒短于字体行高，被省略号必需的 `overflow:hidden` 切掉下缘）；
② 文字没水平居中（flex 默认左对齐，宽度估算多出的余量全落右侧）。
判据做成了**渲染层面**的差分测量（同一帧截两次，关掉 overflow 前后比对墨迹行范围），不依赖字体公式。

**M2 ② 已完成（2026-09-16）**：托盘菜单与宠物右键菜单，两处共用一套模板与一张动作表。
落地项：状态行、隐藏/显示、宠物大小（0.5–1.5）、重置大小、开机自启、退出。
实测：原生菜单可行性探针通过；隐藏→显示后仍可点可拖；缩放尺寸与锚点正确、命中仍只吃精灵；
自启写注册表并经第二来源核对；**退出后进程消失**。
**人工验收通过**（用户逐项测过托盘与右键的全部菜单项）。详见 ADR 011 与 `spikes/m2-menu/README.md`。

**M2 ① 已完成（2026-09-16）**：`状态源 → 仲裁器 → 动画` 链路打通，**人工验收通过**。
仲裁器在主进程（全应用唯一真值），渲染层退化为哑渲染层；第一个 hook 走状态文件 + 监听。
实测：离线单测 58 项断言全过、端到端六步核对到**实际绘制的帧行号**、回归探针两轮全过；
人工验收（用户执行）通过，事件流水留痕于 `~/.desktop-pet/events.jsonl`。
步骤与期望见 `journal/2026-09-16-状态源接入.md` 的"人工验收"一节，入口是工程根目录的 `喂状态.bat`。
详见 ADR 010。

**已定的产品决策（2026-09-16，用户拍板）**：`running`（运行中）**保持映射到第 7 行**，
不改用第 1 行的跑动姿态。理由：第 1、2 行是位移姿态，M3 的自主漫游要用；`Running → 第 7 行`
是规格的官方行语义；"第 7 行画成生日"只是这只包的作者画法，换包即正常。
**"运行中看起来像在忙"这个诉求交给 M3 行为层提供忙碌动作。**

**已定型的运行参数**：缩放 **0.7**（来自 `desktop-pet.json` 的 `render.defaultScale`；实测窗口内容区
134×146 DIP = round(192×0.7) × round(208×0.7)，与之一致）、命中区域 = 精灵轮廓、
窗口 134×146 DIP 内容区、`hitTestAlphaThreshold = 16`；
**待机动画 6.4 fps**（2026-09-17 按用户要求放慢到原来的 0.8 倍：原 8 fps × 0.8，每帧 156.25ms）；
仲裁参数 = 最短展示 400ms / 变化限流 500ms / 粘滞上限 5min / 会话静默兜底 15min；
行为层 = 漫游 25–90s 一次、80–320px、96px/s @默认缩放、微动作 40–150s、打盹 5min。

> 更正（2026-09-16）：本文件此前写"缩放 0.75、窗口 144×156"，与实现不符 —— ADR 008 里
> "1.0→0.75"是当时的取值，现行值是 0.7。ADR 只增不改，**以本文件为准**。

## 4. 下一步：M2 对标交互面

**M1 人工验收：2026-09-16 通过**（六项：出现 / 拖动 / 单击挥手 / 轮廓外穿透 / 全屏让位 /
**全屏恢复后仍可拖动**；挂机内存可接受）。判据细节与"怎么测才准"的提示留在
`journal/` 与 §9 变更日志里，需要时再翻。

**开工顺序建议**（按风险从高到低排，不是按功能好看程度）：

1. ~~**状态源抽象 + 接一个真实 hook**~~ ✅ **已完成（2026-09-16，ADR 010，人工验收通过）**。
   链路上线后，风险表第一位的"产品空转"已降级。遗留的小事：HTTP 状态源未实现（接口已就位）；
   气泡与角标**只算不画**（见下方 ③ 的说明）。
2. ~~**托盘菜单与右键菜单**~~ ✅ **已完成（2026-09-16，ADR 011）**。
3. ~~**气泡与角标 + 全局快捷键**~~ ✅ **已完成（2026-09-16，ADR 012，人工验收通过）**。
   气泡用独立窗口（不扩大宠物窗口 ⇒ 不动命中边界）；快捷键 `Win+Alt+P` 切换宠物显示。
   **注意：注册快捷键前必须把 `Win+...` 这类人话归一化成 `Super+...`**，否则
   `register()` 返回 true 但永不触发（ADR 012 负面结论）。
   人工验收后修掉两项观感缺陷（文字下缘被裁、不水平居中），见 ADR 013。
4. ~~**悬浮控制条**~~ ✅ **已完成（2026-09-16，ADR 014；待人工验收）**。
   范围由用户拍板（D1–D5 全按推荐）：做控制条 + 状态仪表盘 + 快捷动作，
   **本轮不渲染自由文本输入框** —— 规格里输入框的每个去处（铅笔=开新对话、`@` 上下文、
   `$` 技能、线程列表）全在 M3 的 agent 通道上。输入框的位置与命令通道**已冻结**：
   M3 只需给 `BarCommandId` 加一个成员，窗口层不用改。
   落地要点（详见 ADR 014）：本项目第一个 `focusable: true` 窗口、双路径显示
   （悬停 `showInactive` 不抢焦点 / 快捷键 `show` 抢）、唯一不适用 ADR 008 逐像素纪律的窗口
   （矩形直角面板，代价是不做圆角）、显示策略做成纯函数状态机（`kernel/bar-policy.ts`）、
   动作走白名单命令 id。
   **验收步骤见 §1 的"人工验收怎么做"**；探针与回归结果见 `spikes/m2-control/README.md`。
   **2026-09-17 修订**：悬停唤出删除、状态机缩小、面板显示宠物名 —— 见 ADR 016（§4 与
   `docs/design/m2-control-bar.md` §14 都被它取代）。
5. ⏸ **切换宠物（多宠物）—— 已搁置（ADR 015，2026-09-17 用户拍板）**：原计划扫
   `~/.codex/pets/*` → 列包 → 校验 → 热切换整个 pack（窗口尺寸、状态表、sheet、托盘图标
   全量重建）→ 失败回退。**搁置的理由不是代码难，而是手里没有"可切换的素材"，也没有素材
   导入流程与规格标准** —— 造素材、定标准、写切换是互相牵扯的三件事，不该压进同一轮；
   而拿 `demo-pet` 复制成两份冒充"多套素材"是假验证（只能证明没崩，证明不了换一套真实
   创作出来的包还能跑）。**重启条件**（满足任一条再评估）：① 手里出现第二套真实宠物包；
   ② 明确要做"素材导入 + 规格校验"这条产品线、同意先做前置的规格与示例包；③ 出现"因为
   不能换宠物而卡住别的功能"的具体场景。**下个会话不要再提议它，也不要重新论证它。**
6. ✅ **行为层 —— 已完成（2026-09-17 ADR 018 / 2026-09-18 ADR 019）**：空闲时自主漫游
   （每 25–90 秒走 80–320px，速度 96px/s @默认缩放并按缩放线性换算）、边界夹取与换方向、
   微动作（挥手/跳跃）、打盹（空闲 5 分钟后落趴卧帧且单击即醒）、四条抑制
   （拖动/隐藏/全屏/控制条开着）；有任务时**低频短距离踱步**（每 60–180 秒走 60–300px，
   锚在进入任务状态时的位置附近，方向朝工作区中心侧）。
   规则在 `kernel/behavior.ts`（纯函数 + 虚拟时钟 + 固定随机种子），接线在 `main/index.ts`，
   渲染层只接动画覆盖。判据：单测 ⑭ 段 **219 项** + `spikes/m3-behavior` 真实窗口 7 条。
   **回归探针统一加 `--no-behavior`**（宠物自己走会让位置类断言变脆）。
7. ⬅ **本轮：⑦-b 状态源生态 —— 进行中（ADR 020）**。目标 agent = **WorkBuddy + Proma**
   （用户 2026-09-18 定的；**不是 Codex**）。已完成：闸门实测、双通道架构、映射层与 hook 客户端、
   安装器、工程级 hook 落盘、62 项新单测。**下一步按顺序**：
   - **判据 1 ✅ / 判据 2 ✅ 已通过**（真实回合 + 桌宠运行后的对账，见 §3 与 ADR 020）。
     **接下来**：**① 判据 9（会不会拖慢你正常的活）** —— 桌宠现在开着，用下一个真实回合即可评估，
     **不过就砍事件集或弃用通道 A**，本轮以"映射层 + 客户端 + 两条发现"收尾；
     **② 判据 6 换时间基准重测**（`events.jsonl` 的 `ts` 是写侧时刻，不能用，见 §3）；
     **③ 判据 3（`needs-input` 端到端）** —— 已实测 `PermissionRequest` 真的会弹，这条可做；
     **④ Proma 的采样探针 → 适配器**（先把探针的读取路径参数化）。
8. ✅ **用户已拍板（2026-09-18）：小厨师姿态（第 8 行）应当表示 "Agent 正在 running"。**
   原话：*"我认为可以让小厨师的姿态一直保持为 Agent 在 running 的状态。"*
   即 **`running` 改挂第 8 行小厨师**、**`ready` 不再占小厨师** ——
   后者顺带修掉"没有 agent 在跑、宠物却还在炒菜"。
   **这条取代 2026-09-16 的那个决定**（当时定"`running` 保持映射到第 7 行"）。
   随之而来的两个子问题（`ready` 落到哪、第 7 行生日姿态归谁）并入下一条一并定稿。
9. ⬅ **下一步（M3 第三块）：状态↔动作全盘复核** —— 任务书已出：**`docs/design/m3-state-animation-audit.md`**。
   用户要求："首先全盘复核所有的宠物状态的触发条件和是否对应的这种情况"。
   任务书里已经备好：九行素材占用总表（审计基线）、**两处已看出的语义挤压**
   （第 5 行趴卧被"受阻"和"打盹"两种相反含义共用；`ready` 是循环态且无出口）、
   两项待定稿（`ready` 落点 / 第 7 行归属）、**一个真缺陷**（气泡定位，含代码级根因与修法）、
   以及"改映射会打破哪些既有断言"的清单。
   **动手纪律不变**：先出设计 → 用户确认 → 再改 `desktop-pet.json` 与动作映射；平台行为探针实测。
10. **气泡出现在屏幕中间**（用户 2026-09-18 报告，真缺陷）：`bubble-layer.ts` 的 `show()`
    在从未 `followPet()` 过时，把**气泡窗口自己的矩形**当成"宠物矩形"用
    （`if (!lastPetBounds) lastPetBounds = win.getContentBounds()`），
    而 `main/index.ts` 调 `bubble.show(text, badge)` 时**根本没传宠物位置**。
    启动后第一次推气泡必然走到这条兜底 —— 与 ADR 017 那条"没有 `ts` 不等于刚刚"是同一类错误：
    **兜底值取了一个"看起来像但不等于"的东西**。
    修法：把宠物矩形显式传进 `show()`；**拿不到时选择"不显示"，而不是显示在错的地方**。
    详见任务书 §3。
   - **Proma 的采样探针** —— 先用与 WorkBuddy 同一套探针验它的会话日志信号，
     **能用才写适配器**（它的信号是 agent 自己的消息流，性质与 WorkBuddy 那堆应用日志不同）。
   - 判据 3（`needs-input` 端到端）**大概率验不了**（`--permission-mode fullAccess` 不弹审批），
     如实记"未验证"。
   - **⑦-c 宠物包工具链产品化**：素材导入 + 规格校验（与 ⑤ 的重启条件②绑在一起，需用户拍板）。
   **已关闭的三条（不要再提议）**：~~⑦-a 行为层的"忙碌动作"~~（用户不认这个动机，ADR 019）；
   ~~跨屏漫游~~（ADR 019 锁定"不跨屏"）；~~M2 ⑤ 多宠物切换~~（ADR 015）。
   **动手纪律不变**：先出设计（`docs/design/`）与判据，用户确认后再写码；
   平台行为一律探针实测。

**改完窗口生命周期相关代码，必须重跑**：
- `node spikes/m2-hittest/run.mjs probe.js` —— 命中图（应穿透却被吃必须为 0），加了新窗口尤其要跑；
- `node spikes/m2-hittest/run.mjs probe-fs-verify.js fs-verify.json` —— 全屏让位与恢复后的交互。
  **第二个参数不能省**：探针按产出名写日志与报告，省了它驱动会一直等 `report.json` 直到超时，
  白跑一轮还看不到结论（本文件此前就漏写了这个参数）。
- `node spikes/m2-menu/run-tray.mjs` —— 隐藏/显示往返、缩放、自启、退出动作没坏；
- `node spikes/m2-control/run.mjs` —— 控制条的位置/跟随/翻转/命令白名单/焦点与键盘路径没坏。

**改完控制条（`host/control-bar.ts` / `kernel/bar-policy.ts` / `renderer/control-bar.*`）必跑**
`node tools/status-arbiter.test.mjs`（策略规则全在纯函数里，秒级）+ `node spikes/m2-control/run.mjs`。

**改完状态层（`kernel/status.ts` / `kernel/bubble-policy.ts` / `kernel/bar-policy.ts` / `source/*` / 仲裁与映射）必跑**：
`node tools/status-arbiter.test.mjs`（离线单测，秒级，现在 **138 项**）；
动了主进程/渲染层的接线再补 `node spikes/m2-status/run-status-e2e.mjs`（端到端，约 45 秒）
与 `node spikes/m2-hotkey/run.mjs`（气泡与快捷键，约 40 秒）。

**改了状态文件适配器的时间语义（`source/status-file.ts`）必跑**
`node spikes/m2-status/probe-stale-sessions.mjs`（离线、约 15 秒、真实文件 + 真实适配器）——
"陈旧快照会不会被当成新鲜"这类规则在界面上只表现为"一启动就显示某某在运行中"，
不跑这条根本看不出对错（ADR 017）。

**改了气泡外观（`renderer/bubble.html` / `bubble.ts`）必跑** `node spikes/m2-hotkey/run.mjs` ——
它会用**差分截屏**量出文字有没有被裁、有没有居中（做法见 ADR 013，别靠肉眼看截图）。

**改了 `喂状态.bat` 必跑** `node spikes/m2-hotkey/check-feed-bat.mjs` ——
用"延时逐行写 stdin 且先不关"模拟真人连按（`spawnSync({input})` 测不出这类交互菜单的问题）。

## 5. 信息去哪找

| 想找什么 | 去哪 |
|---|---|
| 完整设计（13 章：选型、架构、状态机、阶段计划、风险、验收） | `Windows桌面宠物开发方案.html` |
| 规格知识（图集规格、行语义、常见坑） | 用户级技能 `codex-pet-pack` |
| 资产实测数据（帧数、锚点、基线） | `docs/asset-facts.md` |
| 为什么这么决定（一决策一文件，只增不改） | `docs/decisions/`：001 选型 / 002 契约与锚点 / 003 目录暂不整理 / 004 穿透方案 / 005 工程结构与构建链 / 006 工作区识别 / 007 M1 验收缺陷（全屏判据 + 命中阈值缺口）/ 008 命中区域显式判定与窗口尺寸漂移 / 009 全屏恢复后输入通路失效 / 010 状态源抽象与仲裁器 / 011 托盘菜单与右键菜单 / 012 气泡层与全局快捷键 / 013 气泡文字裁切与居中修正 / **014 悬浮控制条与可聚焦窗口**（含快捷键语义修订，以及"键盘路径不受 ADR 009 影响""skipTaskbar 不加 WS_EX_TOOLWINDOW"两条实测反证）/ **015 多宠物切换搁置**（切换的真正前置是素材侧的标准与导入流程，不是运行时代码）/ **016 控制条交互模型修订**（去掉悬停唤出、右键宠物唤出、面板显示宠物名；**取代** 014 第 2、4 条）/ **017 状态快照时间语义与清空会话**（无 `ts` 用 mtime 兜底 + 清空要清三处）/ **018 行为层与自主行为**（漫游/微动作/打盹；含"打盹计时不能被漫游重置""`--no-behavior` 接缝"两条实测结论）/**019 任务中踱步与不跨屏**/**020 状态源生态与双通道**（双通道 + 通道互斥 + 命名空间前缀 + 只写工程级配置；含"心跳粒度 30 秒""curl 不比 node 快""被动源只给存在性"三条负面结论） |
| **真实 agent 怎么接**（hook 落在哪、通道怎么选、判据是什么） | **ADR 020 + `docs/design/m3-status-ecosystem.md`**（事实表区分"实测/文档/推测"）+ `spikes/m3-status-sources/` |
| **状态↔动作怎么对应、哪些该改**（含气泡定位缺陷的根因） | **`docs/design/m3-state-animation-audit.md`**（任务书：九行占用总表 + 四处可疑点 + 待定稿 + 会打破哪些断言） |
| **改了 hook 映射或客户端必跑** | `node tools/codebuddy-hook.test.mjs`（离线、秒级、**70 项**） |
| **hook 配置怎么装/卸** | `node tools/install-codebuddy-hooks.mjs`（**默认 dry-run**，`--apply` 落盘，`--uninstall --apply` 摘除）。写的是工程级 `.codebuddy/settings.local.json`（本机绝对路径，已 gitignore） |
| 开工前的接口设计（先设计后写码） | `docs/design/*.md`（如 `m2-tray-menu.md`、`m2-hotkey-bubble.md`、`m2-control-bar.md`——**已落地，文末有"实现回写"记与文档的四处偏差；其 §14 记录 2026-09-17 被 ADR 016 取代的部分**；`m3-behavior.md`——行为层的目标/边界/决策点/判据） |
| **行为层怎么定的、怎么验的**（含任务中踱步、不跨屏） | **ADR 018 / ADR 019 + `docs/design/m3-behavior.md` + `spikes/m3-behavior/README.md`** |
| 状态怎么来的、仲裁规则、hook 怎么接 | ADR 010 + `spikes/m2-status/README.md` + `src/kernel/status.ts` 顶部注释 |
| 托盘/右键菜单/缩放/自启怎么验的 | ADR 011 + `spikes/m2-menu/README.md` |
| 气泡策略与快捷键怎么验的；气泡观感（裁切/居中）怎么量出来的 | ADR 012 / 013 + `spikes/m2-hotkey/README.md` |
| **控制条怎么验的**（焦点、键盘路径、位置、命令白名单、Alt+Tab 样式位）、踩过哪些坑 | **ADR 014 + `spikes/m2-control/README.md`** |
| 候选全局快捷键在本机是否可用 | `spikes/m2-hotkey/hotkey.json`（探针结果：默认 `Win+Alt+P`→`Super+Alt+P` 可用，`Win+Shift+P` 被占用） |
| 工作区为什么"不在列表里"/为什么落到时间戳目录 | `docs/decisions/006-工作区识别与登记机制.md` |
| 全屏让位/点击命中/拖动的手感怎么测的、怎么复现 | `spikes/m2-hittest/README.md` |
| 缩放前后对比图（1.0 vs 0.75） | `docs/scale-compare.png` |
| **每个业务状态在屏幕上长什么样**（含行号与真实绘制方式） | `docs/status-reference.png`（`python tools/make-status-reference.py` 重新生成） |
| 历史会话做过什么 | `journal/`（按日期追加，最新在最后） |
| 技术验证工程 | `spikes/`：m0-window 窗口层 / m1-shot 自检截图 / m2-hittest 命中区域·尺寸漂移·全屏输入通路 / m2-status 状态链路端到端 / m2-menu 原生菜单·托盘动作·自启注册表 / m2-hotkey 气泡·快捷键·差分截屏量文字裁切 / **m2-control 控制条焦点·键盘路径·位置·命令白名单·Alt+Tab 样式位** |
| 窗口层实测结论与踩坑清单 | `spikes/m0-window/README.md`（开工前必读，能省掉一批已知坑） |
| 运行时代码 | `src/`（kernel 内核 / **source 状态源** / host 宿主 / main 入口 / renderer 渲染），构建链见 ADR 005 |

**这些文档的寿命（要不要一直留着）**：

- `PLAN.md` / `AGENTS.md`：**常读常更**，接续的唯一入口，每轮开工先读、收工必改。
- `docs/decisions/*`：**只增不改**的决策档案。平时不必读，动到相关模块时读对应那一份。
- `journal/*`：只追加的历史台账。只在"当时为什么这么做"变成疑问时才翻。
- `spikes/*`：验证工程，**可丢弃**——结论都已回写到 ADR，丢了不丢信息。
  （`spikes/m2-hittest/` 建议留：改窗口生命周期代码时要重跑它做回归。）
- `docs/asset-facts.md`、`docs/scale-compare.png`：参考资料。

全部在本地磁盘、**已进 git**。它们不依赖任何对话——新对话只要能打开这个工程目录就能看到，
所以不存在"留在对话里用不上"的问题；反过来，**只在对话里说过的结论才是会丢的**。

## 6. 锁定决策摘要（详见 ADR）

1. **Electron 先行 + 内核纯 TS 解耦**，保留 Tauri 2 迁移路径。（ADR 001）
2. **不污染 `pet.json`**，自研参数走 sidecar（`behavior-map.json` / `desktop-pet.json`）。（ADR 002）
3. **行级固定锚点** `groundY = 202`，禁止逐帧基线锁定。（ADR 002）
4. **根目录暂不整理**，因存在相对引用网。（ADR 003）
5. 点击穿透的实现方式几经修订（**别再用前两条**）：
   ~~`WM_NCHITTEST` 采样 alpha~~ → ~~`WS_EX_LAYERED` 原生命中测试~~（ADR 008 证明其生效区域是整个窗口矩形）
   → **命中判定由渲染层按当前帧精灵 alpha 显式给出**，窗口常态 `setIgnoreMouseEvents(true)`
   整窗穿透，主进程 16ms 轮询光标驱动采样。（ADR 008）
6. **位移速度按屏幕像素/秒归一化**（默认缩放 0.7）。
7. **渲染层与 preload 用 esbuild 打包**，内核纯 TS 不 import electron，init 走"渲染层报到 → 主进程下发"握手。（ADR 005）
8. **命中区域 = 精灵轮廓**（单点判定，不做邻域膨胀）；窗口移动一律用 `setContentBounds` 并复位内容尺寸，
   防止 150% 缩放下 DIP↔物理取整误差累积。（ADR 008）
9. **宠物窗口每次重新显示后必须重载渲染层页面**（`overlay.reload()`）+ 强制重报命中状态。
   实测 `hide()` → `showInactive()` 会切断 Windows 对**真实鼠标按钮事件**的路由（移动事件不受影响），
   只有重建渲染层能可靠恢复。（ADR 009）
   **注意适用范围**：实测该现象**不适用于键盘路径**，也不适用于不接收按钮事件的窗口 ——
   气泡层与 M2 ④ 的控制条都不需要 reload（ADR 014 负面结论 1）。
10. **控制条是本项目唯一可聚焦的窗口**，也是唯一不适用第 5 条逐像素纪律的窗口
    （矩形直角面板，窗口矩形 ≈ 可视矩形；代价是不做圆角）。
    **唤出一律抢焦点**（2026-09-17 修订，ADR 016 —— 取代 ADR 014 的"双路径显示"）：
    三条唤出路径（右键宠物 / `Win+Alt+P` / 托盘菜单）全是用户的显式动作，
    "可能正在打字"那个前提随悬停一起消失了。
11. **`Win+Alt+P` 的语义 = 唤出/收起控制条**（修订 ADR 012 的"切换宠物显示/隐藏"，规格 §3.4）。
    隐藏宠物仍有三个入口：托盘单击 / 右键菜单 / 面板里的按钮。（ADR 014）
11b. **两处右键分工不同**：**宠物右键 = 控制条**；**托盘右键 = 完整菜单**（面板「⋯」也是这份）。
    **不要再把悬停唤出加回来** —— 自动弹出必然变成"我没叫它它自己冒出来"，
    而且与"左键单击播动作"在同一个目标上争抢语义。（ADR 016）
11c. **左键单击宠物 = 播「拜一下」（第 3 行 `waving`）+ 确认**，不唤出任何面板。
    想改成"纯粹玩一下"，摘掉 `renderer.ts` 里那一行 `window.pet.ack()` 即可。（ADR 016）
12. **控制条显示策略是纯函数状态机**（`kernel/bar-policy.ts`），现在只有五条规则
    （`toggle` / `request-close` / `focus` 进 / `focus` 出 / `pet-hidden`）。
    ~~`armed` 位与 `suppressUntil`~~ **已随悬停一起删除**（ADR 016）——它们防的是
    "快捷键收起被悬停弹回来""宠物让位回来自动冒泡"这两个**悬停专有**的场景。
    新增：**`⋯` 弹原生菜单期间屏蔽"失焦即收"**（原生菜单会夺走面板焦点，不屏蔽则面板连菜单一起消失）。
13. **控制条必须显式设 `WS_EX_TOOLWINDOW`**：`skipTaskbar: true` **不会**加这个样式位，
    只靠它控制条会出现在 Alt+Tab 列表。（ADR 014）
14. **不要用 `win.isVisible()` 做显示记账** —— `show()` 之后同一 tick 内它可能仍返回 `false`，
    会让抢焦点的路径重复 `show()` 一次（第二次是 `showInactive`，正好把刚拿到的焦点让出去）。
    用窗口的 `show`/`hide` 事件记账。（ADR 014）
15. **状态快照里"没有 `ts`"不等于"刚刚"**：无 `ts` 的条目按**快照写入时刻（mtime）**兜底，
    且**不参与"ts 变了算心跳"那条分支**（同一份文件里所有会话共享一个 mtime）。（ADR 017）
16. **「清空状态会话」必须清三处**：文件 → 适配器的记忆（`StatusSource.reset?()`）→
    仲裁器的记录（`StatusArbiter.clearSessions()`），缺一处用户看到的都是"没清干净"。（ADR 017）
17. **行为层分两套模式**（2026-09-18 修订，ADR 019 —— 取代 ADR 018 的"`status !== 'idle'` 就完全不动"）：
    `idle` → 空闲模式（漫游 / 微动作 / 打盹）；非 idle → **踱步模式**（低频、短距离、锚在进入任务状态
    时的位置附近）。规则做成**纯函数**（`kernel/behavior.ts`，时钟与随机数可注入），
    接线与渲染层都不做判断。（ADR 018 / 019）
18. **打盹计时只由"状态离开 idle"与"用户碰它"重置** —— 漫游/微动作**不得**重置它，
    否则 25–90 秒走一次会把 5 分钟推后成"永远睡不着"；有任务时压根不计。（ADR 018 / 019）
19. **动画覆盖走独立通道 `CH.behavior`，且命令是三态**（缺省 = 不变 / `null` = 交回仲裁器 /
    对象 = 按它演）。合并成两态会让接收方每 tick 被重置一次。（ADR 018）
20. **绝不在用户抓着的候自己动**：拖动的开始/结束由渲染层**显式上报**（`CH.dragState`），
    不靠"多久没收到拖动增量"去猜 —— 用户手停一下是常态，猜错就是宠物在他手里自己走。（ADR 018）
21. **回归探针要带 `--no-behavior`**：宠物会自己走，位置类断言（命中图、控制条贴边、
    托盘点击坐标）全都变脆 —— 探针读一次窗口位置、再按它算点击坐标，中间宠物走掉了就点空。
    与 `--no-status-source` 同一套路。（ADR 018）
22. **踱步的目标必须由"锚点"算，不能由"当前位置"算** —— 后者会越踱越远，
    让用户要求的"不超过几百像素"在几分钟后悄悄失效，而且屏幕上只表现为"它怎么跑那么远"，
    没有任何报错可循。锚点在**进入任务状态时**与**每次抑制结束后**重记。（ADR 019）
23. **不跨屏是锁定范围，不是"暂时不做"**：只在宠物所在显示器的工作区内活动，
    把宠物拖到哪块屏它就在哪块屏活动（锚点重记是它的落地机制）。
    真要做跨屏得先定义"穿越时窗口矩形算哪一屏"与"多屏工作区并集"。（ADR 019）
24. **状态源是两条通道，不是每个 agent 一套适配器**：**A 事件驱动 hook**（WorkBuddy / Codex /
    将来的 Claude Code —— **同一套事件名与 stdin 契约**，所以是"1 份映射 + N 个配置路径"）｜
    **B 被动会话源**（Proma）。两条都产出 `StatusEvent`，内核不动。（ADR 020）
25. **一个 agent 只由一条通道负责，两条通道互斥。** 两个**不同** agent 天然不冲突
    （仲裁器从 M2 ① 起就是多会话形状，`+N` 角标即为此）；会打架的是**同一个 agent 被两条通道
    同时盯** —— hook 说 running、被动源说 idle，来回刷且无任何报错。
    被动源是"降级来源"，**是切换不是叠加**。（ADR 020）
26. **`sessionId` 必须带来源前缀**（`wb:<pid>` / `proma:<uuid>`），**标题必须带来源名**。
    不带前缀时两个 app 的 UUID 万一相撞会互相覆盖；标题不带来源则两个 agent 同时在跑时分不清谁是谁。
    副产品：用户"只想连某一个 agent"就是一次按前缀过滤，不需要新机制。（ADR 020）
27. **hook 客户端 / 映射层不许有能力影响 agent**：客户端**退出码恒 0**（Claude 系约定里 2 = 阻断，
    我们只观测）、有超时上限、任何异常都吞掉；映射层未知事件**忽略而不是报错**。
    配置只写**工程级**（用户级会作用于用户正在用的所有会话）且默认 `settings.local.json`
    （命令含本机绝对路径，已 gitignore）；安装器**默认 dry-run**、幂等、可卸载、带备份。（ADR 020）
28. **判断一个信号的时间分辨率，要看它的更新频率，不是看它当前的年龄。** 这条是踩坑换来的：
    "心跳文件看起来很新"推不出"它一直在写" —— 实测 151 秒里只刷新 5 次（粒度约 30 秒）。
    同源的纪律：**被动源给的"存在"不等于"在干活"**，把它当 running 判据会制造假阳性。（ADR 020）

## 7. 目录地图

```
desktop-pet/
├── PLAN.md / AGENTS.md        入口与约定
├── 启动桌宠.bat / 喂状态.bat   一键启动 / 人工验收时喂状态
├── package.json / tsconfig.json   构建入口（npm run build / npm start）
├── Windows桌面宠物开发方案.html  完整设计
├── 动画映射验证器.html / pet-spec.html
├── pet.json / spritesheet.webp / *.json   数据契约与资产（引用网，勿移动，见 ADR 003）
├── anchor-report.* / atlas-map.png
├── src/                       运行时代码（kernel / source / host / main / renderer / shared）
├── dist/                      构建产物（git 忽略）
├── docs/
│   ├── asset-facts.md         实测事实
│   ├── scale-compare.png      缩放对比图
│   ├── status-reference.png   业务状态 → 行 → 屏幕实际外观（脚本生成）
│   ├── decisions/             ADR：一决策一文件，只增不改
│   ├── design/                开工前的接口设计（先设计后写码）
│   └── research/              调研产出
├── journal/                   会话日志，按日期追加
├── spikes/                    技术验证工程，可丢弃但结论要回写
│   ├── m2-hittest/            真实点击探针（命中区域/尺寸漂移/全屏输入通路）
│   ├── m2-status/             状态链路端到端探针（回读实际绘制的帧行号）
│   ├── m2-menu/               原生菜单可行性 / 托盘动作（隐藏·缩放·自启·退出）
│   ├── m2-hotkey/             气泡与快捷键 / 差分截屏量文字裁切 / 喂状态.bat 自测
│   └── m2-control/            控制条：焦点与键盘路径 / 位置与翻转 / 命令白名单 / Alt+Tab 样式位
├── spikes/m3-behavior/        行为层：真的在走 / 只沿地平线 / 方向与行号一致 / 抓住不动 / 踱步不越锚点 / 打盹
├── tools/                     分析、校验、构建辅助脚本（npm-run / pet-hook / 布局与图标生成 / 状态层单测）
├── assets/                    模板底稿 v1/v2 + tray.ico（脚本生成，勿手画）
├── examples/                  demo-pet（合法）/ broken-pet（非法，回归用）
└── 喂状态.bat                  人工验收时喂状态（循环菜单，Q 退出）
```

## 8. 关键风险（详见方案 §12）

| 风险 | 等级 | 缓解 |
|---|---|---|
| ~~逐像素穿透难实现~~ | 已解除 | 改为自研显式命中判定（ADR 008），实测误吃 0 |
| ~~全屏让位后宠物失去交互~~ | 已解除 | 恢复时重载渲染层（ADR 009），两轮外部全屏实测通过 |
| ~~无真实状态源，产品空转~~ | **已降级** | M2 ① 打通 `状态源 → 仲裁器 → 动画`（ADR 010）：文件 hook 可用、端到端实测通过。**仍待验证的是"真 agent 的 hook 配起来好不好用"**（目前只验证了链路，没有真实 Codex/Claude Code 跑一遍） |
| 被误判为流氓软件 | 高 | 全屏让位 + 不夺焦点 + **托盘可退出（ADR 011 已落地并实测"退出后进程消失"）** + 不写服务/计划任务 + **控制条不出现于任务栏与 Alt+Tab（M2 ④ 显式设 `WS_EX_TOOLWINDOW`，探针读回 0x188）**。**待办：打包那轮复核注册表值名与托盘图标随包分发** |
| ~~点桌面导致宠物消失~~ | 已解除 | ADR 011 实测抓到：桌面（WorkerW）被误判成全屏应用 → 按窗口类名排除（含类名差一错误的坑）。回归用例已进 `spikes/m2-menu/probe-tray.js` |
| 全局快捷键被占用 | 中 | 注册失败即提示 + 自动降级 + 可自定义 |
| 恶意宠物包 | 高 | 路径穿越 → 魔数 → ≤20 MiB → 尺寸匹配 → 字段白名单。**状态文件同样是投毒点**，已按同一纪律处理（取值白名单 + 文本限长 + 解析失败不清零，ADR 010） |
| 依赖平台未文档化行为 | 中 | 已**五次**被真实坑到（隐藏/显示切断输入、DIP 取整漂移、探针读到残留日志、canvas 字体度量 ≠ Chromium 排版度量、**`skipTaskbar: true` 不加 `WS_EX_TOOLWINDOW`**）。约定：**凡是推断出来的结论都要写探针实测**；而且要量渲染结果就量**渲染本身**（截屏差分），别用"另一套引擎算出的近似值"当判据。结论进 ADR 的"负面结论"节 |

## 9. 变更日志

- **2026-09-15**：合并两条并行会话产物为统一工程；建立本文件为单一事实来源。
- **2026-09-15**：补建跨会话接续三层机制（用户级 `~/.workbuddy/MEMORY.md` 项目索引 + `AGENTS.md` + §1）。
- **2026-09-15**：建立工程管理体系（`docs/` + `journal/` + `spikes/` + `.gitignore`）；初始化 git。
- **2026-09-15**：M0 窗口层验证完成（`spikes/m0-window/`）：逐像素穿透、不夺焦点、全屏让位三项判据全过。**修订 ADR 001 第 5 条**（ADR 004）。
- **2026-09-15**：排查"工作区不出现在列表/新建对话落到时间戳目录"，定位到 `workbuddy.db` 的 `workspaces` 表机制（ADR 006）。结论：不搬家。
- **2026-09-15**：M1 渲染闭环代码完成（ADR 005）。
- **2026-09-15**：M1 人工验收，修掉两项缺陷（ADR 007）：全屏让位判据因 koffi 结构体未回写而恒 false；规格里的 `hitTestAlphaThreshold` 从未接线。负面结论：`WindowFromPoint` 不能用来验证分层窗口的透明穿透。
- **2026-09-15**：第二轮人工复验，搭真实点击探针（169 采样点）实测出两条硬结论并据此重做命中方案（**ADR 008**）：① 生效命中区域 = 整个窗口矩形（窗口内 90/90 采样点全被吃掉，M0 的"逐像素穿透是原生行为"被推翻）；② 窗口每 `setPosition` 一次长 1 DIP（150% 缩放下取整累积），这正是"拖动后死区越变越大"的机制。改为：渲染层按当前帧 alpha 显式判定 + 16ms 光标轮询 + `setContentBounds` 复位 + 缩放 1.0→0.75。复测：误吃 43→**0**，拖动前后命中图逐点一致。
- **2026-09-15**：修掉"全屏让位回来后宠物点不动/拖不动"（**ADR 009**）。最小复现是 `hide()`+`showInactive()`；症状精确到"移动事件能进、按钮事件进不去"；用 `SendMessage` 证明是 Windows 路由问题；9 个补救动作全部无效、nudge 不可靠、`webContents.reload()` 稳定有效。改为恢复时重载渲染层 + 强制重报命中状态；顺带去掉无效的 `forward` 选项、消除 4 DIP 的 view/窗口错位。两轮外部全屏实测：全屏后单击与拖动均 ✅，无累积劣化。
- **2026-09-16**：**M1 人工验收通过**（六项判据全过，含"全屏恢复后仍可拖动"），M1 正式收尾。
  下一步按 §4 开工 M2（第一件事：状态源抽象 + 接真实 hook）。本文件同步刷新为可直接接续的索引。
- **2026-09-16**：**M2 ① 完成：`状态源 → 仲裁器 → 动画`链路打通（ADR 010）**。
  新增 `src/kernel/status.ts`（仲裁器：优先级/粘滞/最短展示+限流/多会话聚合/静默兜底，时钟可注入）、
  `src/source/`（状态源接口 + 状态文件适配器）、`tools/pet-hook.mjs`（hook CLI，兼手工模拟器）；
  仲裁器跑在主进程作为全应用唯一真值，渲染层退化为哑渲染层。
  第一个真实 hook 走状态文件 `~/.desktop-pet/status.json`（避开监听端口与防火墙提示），
  事件同时落 `~/.desktop-pet/events.jsonl` 供离线查证。
  实测：单测 58 项断言全过；端到端六步核对到渲染层**实际绘制的帧行号**（7/6/0/3→8/5/0）；
  回归探针两轮全过。过程中单测抓出两个真 bug（粘滞超时会立刻重新粘滞 = 超时无效；适配器把
  "读不懂的条目"误判为"会话消失"），已在 ADR 010 记录。负面结论：探针驱动会读到上一次运行的
  残留日志（需运行标识兜底）；`child.kill()` 带不走 detached 的 Electron（需 taskkill /T）。
- **2026-09-16**：**M2 ① 人工验收通过**（用户执行，事件流水留痕于 `~/.desktop-pet/events.jsonl`）。
  返工了验收材料：`喂状态.bat` 改成循环菜单（原来喂完就退出，且 EOF 时会空转）；新增
  `docs/status-reference.png` 状态对照卡（业务状态 → 行 → 屏幕实际外观）。
  起因是验收说明把 `running` 写成"原地跑动"、实际是生日姿态 —— **业务状态名 ≠ 动作外观**，
  已约定描述动作必须引用那张图（AGENTS.md）。同时更正 PLAN 的运行参数：实际缩放 0.7、窗口 134×146。
- **2026-09-16**：**M2 ② 完成：托盘菜单与右键菜单（ADR 011）**。
  新增 `src/host/{tray,pet-menu,prefs,autostart}.ts` 与 `tools/make-tray-icon.py`（图标脚本生成）；
  托盘与宠物右键共用一套菜单模板与一张动作表，状态一律现读 `StatusArbiter.state`。
  原生菜单可行性先探针实测（可弹出、可点击、不破坏输入通路），因此不自绘菜单（避免改窗口尺寸）。
  实测：托盘动作探针 **PASS**（隐藏→显示后仍可点可拖；缩放 0.7→1.0 尺寸与锚点正确、命中仍只吃精灵；
  自启写注册表并经第二来源核对；退出后 PID 消失）；状态层 58 项断言与端到端、全屏回归探针全过。
  过程中修掉四个真缺陷：① `pointermove` 的 `button` 是 −1，误加"只处理左键"判断会静默吃掉整段拖动；
  ② 命中判定只跟光标不跟动画 → 点完宠物立刻拖动会穿透，改为渲染层每帧复评；
  ③ 桌面（WorkerW）被误判为全屏应用 → 点桌面宠物自我隐藏，按窗口类名排除；
  ④ `GetClassNameW` 返回值差一错误使 ③ 的修复静默失效一轮。
- **2026-09-16**：**M2 ③ 完成：气泡层（状态文案 + 多会话角标）与全局快捷键（ADR 012）**。
  核心取舍：气泡用**独立透明层窗口**，不扩大宠物窗口 —— 扩大会改窗口矩形，命中判定的坐标系与
  边界都得重验（ADR 008/009 的地盘）。气泡层常态整窗穿透、永不切换，因此与 ADR 009 那个
  "hide→show 后按钮事件不路由"的坑无关，全屏恢复也不需要 reload。
  策略做成纯函数（`kernel/bubble-policy.ts`）：`needs-input` 常驻、`ready` 6s / `blocked` 4s /
  `running` 2s、**同状态心跳不重置计时**；时长写进 `desktop-pet.json` 的 `bubble` 段。
  实测：快捷键真实击键触发（隐藏↔显示）；气泡 2 秒自动收起、needs-input 常驻带 `+1` 角标、
  拖动时跟随；**新增窗口没吃掉任何点击**（命中图误吃 0、拖动前后一致）；全屏与托盘回归全过；
  单测 58 → **81 项**。
  **最大的坑**：宠物包里写的是人话 `Win+Alt+P`，而 Electron 里 Windows 键叫 `Super` ——
  `register('Win+Alt+P')` **返回 true 但永不触发**（静默失效）。注册前一律 `normalizeAccelerator()`。
  另两条负面结论：窗口标题由页面 `<title>` 决定（`setTitle` 会被覆盖）；下发 32 DIP 高度会读回 38，
  定位改用实测高度。
- **2026-09-16**：**M2 ③ 人工验收通过**（用户逐项测过快捷键、气泡、角标、拖动跟随），
  并修掉两项观感缺陷（**ADR 013**）：
  ① **文字下缘被裁**（1 个物理像素）—— `line-height: 1` 让行盒只有 13px，短于字体自身行高
  （雅黑 13px ≈ 17px），字形溢出后被"省略号必需的" `overflow:hidden` 切掉；改为 `line-height: 20px`；
  ② **文字没水平居中** —— flex 默认 `flex-start`，宽度估算多出的余量全落右侧（实测左 0.00 / 右 3.39px）；
  改为 `justify-content: center`（修后左右余量 1.69 / 1.70px）。
  **做法上的关键**：先用 canvas 的 `fontBoundingBox*` 反推裁切量，算出 0 —— 与真实渲染不符
  （**canvas 字体度量 ≠ Chromium 排版度量**）。改成**渲染层面的差分取证**：同一帧截屏两次
  （保留裁切 / 注入 CSS 关掉 `overflow`），比对文字墨迹的行范围，差值即被裁像素数，容差 0。
- **2026-09-16**：`喂状态.bat` 增 `8 第二会话 b=running` / `9 清掉会话 b`，角标语义写进菜单头部。
  起因：用户开两个终端分别按 1 与 4 仍看不到 `+1` —— 角标统计的是"主状态之外的其它活动会话"，
  而**两个终端写的都是同一个会话 `default`**，单会话永远得不出 `+1`。新增自测
  `spikes/m2-hotkey/check-feed-bat.mjs`（6 项断言，模拟真人连按）。单测 81 → **87 项**（补了这条场景）。
  回归全绿：命中图误吃 0、全屏两轮 OK、托盘 PASS、状态端到端六步对上。
- **2026-09-16**：**M2 ④ 接口设计出稿**（`docs/design/m2-control-bar.md`，状态 = 待用户确认，尚无实现代码）。
  先回答了那个"必须先定的问题"：**输入框的内容发去哪** —— 结论是**本轮不渲染自由文本输入框**。
  判据不是"做不出来"，而是规格里输入框的每个去处都在 agent 通道上：基线 2 的铅笔按钮 = 开新对话、
  基线 3 的 `@` 上下文 / `$` 技能、基线 4 的"聚焦 Quick Chat"、架构 6.1 的"线程列表"。
  另两条路线（本地命令通道、假输入框）都不推进验收基线，前者还会与 M3 的对话语义在同一输入框里打架。
  设计主体：本项目**第一个可聚焦窗口**（现有三层窗口全是 `focusable:false`，焦点行为是零经验区）——
  双路径显示（悬停 `showInactive` 不抢焦点 / 快捷键 `show()` 抢）、唯一不适用 ADR 008 逐像素纪律的窗口
  （矩形直角面板，窗口矩形≈可视矩形）、显示策略做成纯函数状态机（含"快捷键收起后被悬停弹回来"这条
  易错规则的抑制窗口）、动作走**白名单命令 id**（M3 加输入框时协议形状不变）。D1–D5 待用户拍板。
- **2026-09-16**：**M2 ④ 完成：悬浮控制条（ADR 014，待人工验收）**。
  D1–D5 由用户拍板（五项全按推荐）。新增 `src/host/control-bar.ts`（本项目第一个可聚焦窗口）、
  `src/kernel/bar-policy.ts`（显示策略纯函数状态机）、`src/renderer/control-bar.{html,ts}`、
  `src/main/preload-bar.ts`；`kernel/status.ts` 增只读视图 `viewSessions()`；
  `shared/ipc.ts` 增 3 个通道与 `BarView`/`BarCommand`/`BarCommandId` 白名单；
  `main/index.ts` 接线悬停判定、快捷键语义、全屏让位、拖动跟随与命令表；
  `pet-menu.ts` 加快捷键文案与控制条入口；sidecar 增 `controlBar` 段；构建链增两个入口。
  **快捷键 `Win+Alt+P` 语义改为"唤出/收起控制条"**（修订 ADR 012，规格 §3.4），
  宠物隐藏时按下先叫回宠物；隐藏宠物仍有托盘/右键/控制条三个入口。
  实测：`spikes/m2-control` 两个探针 **全 PASS** —— 悬停唤出不抢焦点 / 快捷键唤出抢焦点且键盘可达 /
  **hide→show 往返后键盘照常投递（ADR 009 在键盘路径不复现 ⇒ 控制条不需要 reload）** /
  位置下间距 8 DIP 正、水平偏差 0、高度随会话数 72↔128 / 贴底边翻到上方 / 拖动跟随 /
  命令白名单（非法 id 与不存在的会话都被拒并留日志）/ **`WS_EX_TOOLWINDOW=true`** / Esc 与 close-bar 生效。
  单测 87 → **130 项**。回归全绿：命中图**误吃 0**、全屏两轮 OK（日志里可见控制条"跟随隐藏"）、
  托盘 PASS、气泡与快捷键 PASS（用例 1 已按新语义改写）。
  过程中修掉三个真缺陷/坑：① **`win.isVisible()` 不能做显示记账**（`show()` 同一 tick 内可能仍返回
  false，导致"抢焦点"路径重复 `show()` 一次、把刚拿到的焦点又让出去）；② `skipTaskbar: true`
  **不加** `WS_EX_TOOLWINDOW`，控制条会出现在 Alt+Tab，改为用 koffi 显式设置；
  ③ 到点收起若顺手撤销 `armed` 位会让"悬停唤出"永久失效（已用单测钉住）。
  另记一条方法论教训：**测位置类规则前必须先摆位** —— 第一版探针直接量默认位置，报出两条假失败，
  实际是宠物本来就贴屏幕右下角（面板正确地翻到了上方并被夹进工作区）。
- **2026-09-16**：**M2 ④ 人工验收返工**（用户报 6 条，全部处理；细节与证据见 ADR 014 验收返工节）。
  修掉三个真缺陷：① **命中换算缺少客户区/单元格边界检查（M1 起就存在！）** —— 窗口外负坐标经
  整数运算落回图集其它行列、命中别的精灵，判定随动画帧抖动：既造成"远处鼠标触发控制条"，
  又让悬停 hideAt 被无限重置（面板收不掉）。修法是 `hitAt` 加两层边界检查；
  **教训：边界 bug 只在坐标极端时显形，探针采样必须覆盖远离目标的点**（m2-control 幽灵区域用例已固化）。
  ② 面板隐藏后旧矩形仍参与悬停判定（宽 260，比宠物宽 126 DIP）→ 不可见时不再计入。
  ③ 面板 × 没写 data-cmd，事件委托机制下点击完全无效 → 已补。
  另按用户意见：撤掉 −/+/↺（缩放走"⋯ 更多"菜单，命令白名单缩到 4 个）、hoverGraceMs 500→350、
  **「确认」改为立即生效**（不再被 500ms 限流挡住；限流本身未废，单测双向钉住）。
  残留会话排查结论：**链路无故障**（隔离验证 + 事件流水为证），是快照残留 + 用户按 8/9 操作的是
  会话 b 的误解；产品层补「清空状态会话（N 条）」菜单项 + 会话行"已静默"标注。
  回归：单测 130→**131**、m2-control PASS（含幽灵区域与 × 新用例）、命中图**误吃 0**（169 点）、
  全屏两轮 PASS、托盘 PASS。**待用户复验**：远处不触发 / × 可点 / 确认即时 / 清空会话。
- **2026-09-17**：**审计两处收尾**（ADR 014 补记 6–8）。
  ① **修掉启动顺序上的 TDZ 隐患**：状态源 start 会同步读文件并可能立刻 `pushStatus()` →
  `refreshBar()`，而 `bar` 是 `let` 声明在后面 —— 原先不崩只因 boot 全程同步，
  顺序一改或插入 `await` 就会启动即崩。把「启动异步子系统」（状态源 + 仲裁 tick）整体移到
  **所有窗口创建完毕之后**，依赖从"靠隐式保证"变成"结构上不可能"；顺带修掉那条失效的注释。
  ② **命中图残留的 2 个漏吃定位为边缘取整分歧**（探针现在打印明细：`(88,8) alpha=16`、
  `(24,56) alpha=31`，都紧贴阈值 16 的轮廓边缘；渲染层用 `floor(css/scale)`、探针基准用
  `round(css*dpr)`，边缘 1 像素内分歧）。**判据只看误吃（现为 0），漏吃有上界且可复现。**
  ③ 探针前置教训（第三次同源）：`hoverGraceMs` 500→350 后面板收得更早，控制条探针在
  "面板已收起"时读 DOM 导致 4 条连环假失败 —— 产品行为是对的（不可见时 refreshBar 早退、
  重新显示时推最新内容，铁证是重新显示后高度=128=2 条会话）；探针已补"先唤出并断言可见"的前置。
  回归全绿：单测 **131**、m2-control 两个探针 PASS、命中图**误吃 0**（4 项）、全屏两轮 PASS、托盘 PASS。
- **2026-09-17**：**M2 ⑤ 多宠物切换搁置（ADR 015，用户拍板）**。理由：手里只有一套素材，
  且没有"素材导入流程 + 规格标准" —— 造素材／定标准／写切换是**互相牵扯的三件事**，压进同一轮
  出了问题无法定位是哪一层的；而拿 `demo-pet` 复制成两份冒充"多套素材"是假验证。
  **下一步改为「M2 ④ 的人工复验与缺陷修复」**，开工话术已改写（§1），⑤ 的重启条件写在 §4 第 5 条。
- **2026-09-17**：记一条**待实测的线索**（**尚未验证，别当结论**）：状态文件是快照、重启后原样读回；
  若某条会话记录**没有 `ts` 字段**，`kernel/status.ts` 的 ingest 会把它盖章成"现在"，于是这条
  陈旧会话在启动后"新鲜" 15 分钟 —— 表现正是用户上轮报的"一启动就显示某某在运行中"。
  待下轮用探针确认（§1 的问题清单里已点名）。
- **2026-09-17**：**M2 ④ 缺陷修完 + 控制条交互模型重做**（ADR 016 / ADR 017）。两类改动：

  **A. 修两条真缺陷（ADR 017）**，都是上一轮复验定位、本轮实测 + 修复 + 钉住：
  ① **没有 `ts` 的快照被盖章成"现在"**（上一条线索证实成立）：改为用**快照 mtime** 兜底
  （同一 fd 取，避免先 stat 再 read 的竞态），且这类条目标记 `tsFromFile`、
  **不参与"ts 变了算心跳"那条分支** —— 否则同一文件里任一会话被写就会给其它无 ts 的陈旧会话续命。
  这条修的正是"一启动就显示某某在运行中"背后的机制之一。
  ② **「清空状态会话」只清了宠物没清视图**：改成**三处一起清** —— 文件 / 适配器记忆
  （新增可选接口 `StatusSource.reset?()`）/ 仲裁器记录（新增 `StatusArbiter.clearSessions()`，
  顺带清确认位、pending、sticky 并重置限流窗格）。
  判据：单测 131 → **138 项**（⑧b 8 条 + ⑬ 10 条，删悬停 8 条）；`probe-stale-sessions.mjs`
  **重写为 22 项回归哨兵**（含"刚写的无 ts 快照必须照常生效"这条**防误杀**的反向用例，
  以及"清空后行数 0 且不再冒出补事件"）；控制条探针 ⑫ 段端到端钉住"行数 2→0、
  菜单计数 2→0、高度 128→72"。

  **B. 控制条交互模型重做（ADR 016，用户拍板）**：
  **悬停唤出整条路径删除** —— 桌宠常驻角落、光标是"顺便"掠过的高频事件，自动弹出必然变成
  "我没叫它它自己冒出来"，延迟只降频不改性质；且左键单击要播动作，"停在它身上"不该带第二个含义。
  现在唤出只有三条**显式**路径（**右键宠物** / `Win+Alt+P` / 托盘菜单「控制条」），**一律抢焦点**；
  关闭五条路径（Esc / × / 再唤出一次 / 点到别处 / 宠物被隐藏）。
  **两处右键分工**：宠物右键 = 控制条，托盘右键 = 完整菜单（面板「⋯」也是这份）。
  面板动作排里的只读 `70%` 换成**宠物名**（`pet.json` 的 `displayName`，不硬编码"淘淘"）。
  `kernel/bar-policy.ts` 从"七条规则 + `armed`/`suppressUntil` 两个补丁位"缩到**五条规则**。
  新增机制：**`⋯` 弹原生菜单期间屏蔽"失焦即收"**（原生菜单会夺走面板焦点）。
  左键单击语义澄清为"播「拜一下」（第 3 行 `waving`，依据 `docs/status-reference.png`）+ 确认"。
  判据：`spikes/m2-control` 两探针 PASS（含"悬停 1.8 秒不唤出""右键宠物唤出且抢焦点 / 再右键收起"
  "⋯ 开着时面板仍活着""× 收起后不自己弹回来"）；探针里所有"确保可见"的前置统一改走
  **真实右键宠物**，手势失效即报错，不再用调试钩子静默绕过。
  回归全绿：单测 138、命中图**误吃 0**（漏吃 2 = 既有边缘基线）、全屏两轮 OK、托盘 PASS、
  状态端到端 PASS（六步帧行号全对）、气泡与快捷键 PASS。
  过程教训：**同一文件不要在一次并行编辑里改两处** —— 本轮有 4 处编辑工具报"成功"但没落盘，
  构建报错才发现（已改为改完立刻 grep 校验）。
- **2026-09-17**：**M3 第一块落地：行为层（ADR 018）**。宠物从"站在角落里循环待机"变成
  "没人用它时像个活物"：自主漫游（每 25–90 秒沿地平线走 80–320px）、微动作（挥手/跳跃）、
  打盹（空闲 5 分钟落趴卧帧、单击即醒），并带**四条抑制**（拖动中/隐藏/全屏/控制条开着一律不动）
  与「减少动态效果」下的降级（不漫游不做微动作、打盹照常）。
  分层沿用既有架构：**规则 = 纯函数**（`src/kernel/behavior.ts`，时钟与随机数可注入）、
  接线 = `main/index.ts`（33ms tick，攒输入 → 调 tick → 落命令）、渲染层 = 哑渲染层
  （新通道 `CH.behavior` 接动画覆盖，覆盖期间**不把播放器收敛回主状态**，否则每帧被拉回去走不动）。
  另新增：`overlay.moveTo()`（绝对定位，避免增量取整吞掉像素）、`CH.dragState`（拖动的显式边沿）、
  `ready({reducedMotion})`（渲染层才知道的系统偏好）。
  判据：单测 **138 → 191 项**（新增 ⑭ 段 22 项：启动不乱跑、到点才走、速度按缩放归一化、
  目标夹进工作区、贴边换方向、抑制期间不动且保留目标、running 不动、打盹与醒来、
  **漫游不重置打盹计时**、微动作播完交回、边沿命令不重发、总开关）；
  新增真实窗口探针 `spikes/m3-behavior/run.mjs`（用临时宠物包把节奏压到秒级）**7 条全过**：
  真的在走（38 次位移）/ 只沿地平线走（y 只有一个值）/ 不越界（0）/ **位移行号与方向一致 8/8** /
  抓住 3 秒一步不动且松手恢复 / 控制条开着不动 / running 期间不动 / 打盹画到第 5 行且单击能醒。
  **两条只有实测才发现的坑**：① 打盹计时若被漫游重置，"每 25–90 秒走一次"会让它**永远睡不着**
  （屏幕上只表现为"它就是不打盹"）；② 自主行走让既有的**位置类断言集体变脆**——
  7 个回归探针统一加 `--no-behavior` 接缝（与 `--no-status-source` 同一套路）。
  另按用户要求：`pet.json` 的 displayName 改为**淘淘**、待机动画放慢到 **0.8 倍**（8 → 6.4 fps）。
- **2026-09-18**：**行为层补完"任务中踱步"，并锁定"不跨屏"（ADR 019）**。
  用户拍板了 ADR 018 留的那个产品决定：**任务进行中可以离开原位，但不超过几百像素、
  频率要低、只作为点缀**；同时明确**暂不考虑多屏**（只用主屏 / 拖到哪块屏就在哪块屏活动）。
  另外把早先记的"运行中看起来像在忙"（"忙碌动作"）**明确作废** —— 用户不认这个动机，
  名字也不再使用；真正落地的是上面这条更小的要求。
  - 实现：`tickBehavior` 把规则 0 拆成两套模式 —— `idle` → 空闲模式（漫游/微动作/打盹），
    非 idle → **踱步模式**（`tickBusy`）。新增相位 `pacing` 与三个状态字段
    （`busySinceAt` / `anchorX` / `nextPaceAt`）；`rearmBehavior` 兼顾两种模式。
  - **最关键的一条**：踱步目标**由锚点算、不由当前位置算** —— 按当前位置累加会越踱越远，
    让"不超过几百像素"在几分钟后**悄悄失效**且无任何报错；锚点在进入任务状态时、
    以及每次抑制结束后重记（后者正是"拖到哪块屏就在哪块屏活动"的落地机制）。
  - 参数（`desktop-pet.json → behavior.busyPace`）：每 60–180 秒、60–300px、速度沿用漫游基线
    （另取一值会滑步）；方向朝**工作区中心**一侧（默认右下角 ⇒ 观感是"往左走几步"；
    固定朝左在贴左边缘时会永久失效）。有任务时**不计打盹**（`idleSinceAt` 恒 null）。
  - 判据：单测 **191 → 219 项**（新增 ⑥b 踱步、⑥c 不跨屏；三条核心判据是"踱几十次不漂出锚点"
    "默认节奏下 10 秒一次都不踱""换到另一块屏时目标夹在新工作区内"）；
    `spikes/m3-behavior` 的 ④ 用例从"running 期间一步不动"改成"在动但离锚点不超过上限"，
    **实测离锚点最远 111px / 上限 160px、锚点全程只有一个值、越界 0**，7 条全过。
  - **留给用户验收看一眼的取舍**：踱步期间画的是位移行（第 1/2 行），不是业务状态那行
    （另一条路是"业务动画不动、只是窗口在挪"，画面上会变成生日姿态在地面平移）。
    用户原话"动画可以照常播放"两种读法都通，记在 ADR 019 负面结论 1，改的话是一行。
- **2026-09-18**：**M3 第二块启动：状态源生态（ADR 020）—— 目标 agent 改为 WorkBuddy + Proma**。
  用户 D1 答复把目标从 Codex 改成**WorkBuddy（主力）+ Proma（第二主力，Pi agent 架构）**，
  于是重做调研，结论有实质变化：

  **三个决定性发现**：
  ① **WorkBuddy 自带完整的 Claude 系 hook 引擎**（初稿完全没料到）——
  内嵌 `@genie/agent-cli` 的 `codebuddy.js`(23MB) 里有 14 个事件的字面量数组
  （含 `PermissionRequest`/`Notification`/`PostToolUseFailure`/`WorktreeCreate`）、
  `hook_event_name`×49、`executeHook`×32、`disableAllHooks`，且 hook 是**真的 spawn 子进程**；
  配置位置 = `.codebuddy/settings.json`。
  ② **闸门实测 = YES**：进程树显示桌面把 agent 跑在内嵌
  `cli/bin/codebuddy --serve --session-id ab2f75f8-…` 上（该 uuid 与
  `~/.workbuddy/sessions/203516.json` 的 `sessionId` **完全一致**）；
  CLI 里 `DEFAULT_SCOPES = [USER, PROJECT, PROJECT_LOCAL]` 且跑 agent 的进程**没带**
  `--setting-sources` ⇒ **工程级配置会被读**（这把 D5 的"只写工程级"从保守猜测变成有证据）。
  ③ **Proma 没有可配置的 hook 面**（`app.asar` 里 `PreToolUse`/`hook_event_name` 全 0，
  `SessionStart` 25 处逐条看过是编辑器内部变量），**但被动源可用**：
  `agent-sessions.json` 218 条会话带 `title`/`updatedAt`，日志末尾有
  `type:"result"` + `terminal_reason:"completed"`。顺带实证 SDK 迁移：`sourceRuntime` = claude 38 / Pi 180。

  **用户提的关键问题（"两个软件同时跑会不会冲突/叠加"）决定了架构**：
  两个**不同** agent 天然不冲突（仲裁器从 M2 ① 就是多会话形状，`+N` 角标即为此）；
  会打架的是**同一个 agent 被两条通道同时盯** ⇒ 新增硬规则**一个 agent 一通道、互斥**
  （被动源是降级来源，是切换不是叠加）；`sessionId` 带来源前缀、标题带来源名；
  用户想要的"自己选择连哪个 agent"因此变成一次按前缀过滤，不是新机制。

  **施工顺序被实测推翻一次**：初稿把"先做被动源"当零风险的先赢，151s @1Hz 采样探针证明
  **WorkBuddy 的被动源只能给"存在性"、给不了"活动性"** —— 心跳 151 秒里只刷新 5 次
  （**粒度约 30 秒**，且与在不在干活无关）；`tasks/`、`file-history/`、`changes-index/`、`plans/`
  窗口内 **0 次变更**；`logs/` 105 次写入但内容是应用级日志（`vendor-extract.log`、`weixinpay/*.xlog`）
  ⇒ **通道 A 才是 WorkBuddy 的主路径**。另记一条**自我更正**：此前写"心跳秒级新鲜"是错的 ——
  那是**只看了一次文件的当前年龄**，而不是**看它的更新频率**，两者完全不同。

  **落地**：`tools/codebuddy-hook-map.mjs`（纯函数映射层）、`tools/pet-hook-cb.mjs`
  （hook 客户端；**退出码恒 0**、`--dump` 取证、stdin 超时兜底）、
  `tools/install-codebuddy-hooks.mjs`（安装器，**默认 dry-run**、幂等、可卸载、带备份）、
  `tools/codebuddy-hook.test.mjs`（**62 项**，含"不踩掉别的 agent 的会话"）、
  `spikes/m3-status-sources/probe-workbuddy-live.mjs`（只读采样探针）。
  工程级 `.codebuddy/settings.local.json` **已落盘**（6 个低频事件；`PreToolUse`/`PostToolUse` 不挂，
  每次 377ms）。既有 `pet-hook.mjs` **刻意未动**（人工验收入口，一批回归的着力点）。
  判据：新增 62 项 + 既有 **219 项全绿** ⇒ **281 项**。
  **待办**：判据 1/2/6/9 的真实回合验证；Proma 的采样探针与适配器。
  **闸门带出的坏消息**：当前会话 `--permission-mode fullAccess` 不弹审批
  ⇒ 判据 3（`needs-input` 端到端）大概率**验证不了，届时如实记"未验证"**。
- **2026-09-18（同日晚些）**：**真实回合验证：判据 1 通过，并抓修一个只有真跑才暴露的缺陷**。
  配置落盘后用户真发了一条消息，hook 侧留下 5 条实测记录
  （`PermissionRequest`×2 / `Stop` / `Notification` / `UserPromptSubmit`）：

  - **判据 1 ✅**：hook 真的被派发；**含空格路径正常**（命令里两个绝对路径都没被 shell 截断 ——
    这条本项目栽过一次，这次专门验了）；payload 的 `session_id` 与进程树、
    `~/.workbuddy/sessions/*.json` **三处一致**；payload 比文档更丰富
    （多了 `permission_suggestions`/`permission_mode`/`client`(实测 `"WorkBuddy"`)/`generation_id`
    /`model`/`agent_type`/`background_tasks`/`session_crons`），其中 **`client` 可直接当来源标识**。
  - **抓到一个我自己的缺陷（已修）**：`Notification` 原本一律映射成 `needs-input`，
    而实测 `notification_type` 是 **`idle_prompt`**（"干完了、在等你说话"），
    它在 `Stop` 之后**约一分钟必然出现** ⇒ **每次干完活宠物都举手要你确认**，
    让 `needs-input` 这个最宝贵的状态**贬值**。改为良性类型白名单（目前只 `idle_prompt`），
    命中则不写状态；未知类型仍按"要你注意"处理。**这条单测写不出来 —— 当时不知道有这个取值。**
    新增 8 项断言（62 → **70 项**）。
  - **一条预测被推翻**：据 `--permission-mode fullAccess` 断言"不弹审批、判据 3 验不了"，
    实测 `permission_mode` 是 `"default"` 且 **`PermissionRequest` 真弹了两次**
    ⇒ **判据 3 可以做**。
  - **状态文件侧正确**：`wb:ab2f75f8-…` → `running`，标题 `WorkBuddy · desktop-pet`。
  - **⚠️ 判据 2/6/9 未验，原因明确：验证时桌宠没有运行**（无 electron 进程），
    宠物侧 `events.jsonl` 停在 2026-09-17 ⇒ 没有可对账的时间线。**必须开着桌宠再跑一次。**
  - 判据：新增 **70 项** + 既有 **219 项** ⇒ **289 项全绿**。
- **2026-09-18（同日晚些 · 第二次验证）**：**判据 2 通过；用户撞到一个产品级缺口**。

  **判据 2（状态序列对账）✅**：用户装上桌宠后，宠物侧 `events.jsonl` 终于有了时间线 ——
  `ready`(09:56:57.853) ← `Stop`(09:56:57.851)；`running`(10:02:42.862) ← `UserPromptSubmit`(10:02:42.860)。
  序列正确、不卡在 running；且 **hook 侧 09:57:57 有一条 `Notification type=idle_prompt`
  而宠物侧无任何对应事件 ⇒ 上面那个假阳性修复在真实环境确认有效**。

  **用户报告：一启动桌宠就是"炒菜/做饭"的姿态，不是原来的默认待机。**
  诊断（**对照 `docs/status-reference.png` 确认，不是凭状态名猜**）：
  `ready` → 动画 `waving` → **落点 `review` = 第 8 行"小厨师姿态"**；
  而 `review` 是 **`loop: true` 的循环态、没有出口** ⇒ **agent 干完一次活，宠物就永久炒菜**；
  用户 10:01 启动时读到快照里 10 分钟前的 `ready`（未到 15 分钟静默兜底）⇒ 立即进入 review。
  **以前没见过，是因为那时没有真实 hook，`ready` 只来自人工喂状态。**
  支持"这是缺陷"的论据：**气泡对 `ready` 只显示 6 秒就收起（`holdMsByStatus.ready = 6000`），
  宠物却还在炒菜** —— 两者不一致。
  **但改成什么是产品决策**（该落点是 M2 ① 验收过的行为），已列为 §4 第 8 条**待用户拍板**。

  **⚠️ 判据 6 的测量陷阱（实测发现）**：`~/.desktop-pet/events.jsonl` 里的 `ts` 是**写侧产生时刻**，
  **不是宠物收到的时刻**（实测两者只差 2ms，那只是 hook 进程内"落 payload → 写状态文件"的间隔）。
  **拿它算端到端延迟会严重低估。** 要算真实延迟需另找时间基准（主进程日志或探针加戳）。

  **附带事实**：`default` 会话是 8-16 人工验收的残留（title `manual-acceptance`），
  桌宠启动时被读到并短暂报了一次 `running`（10:01:15）、4 秒后变 `idle`；
  **是噪声不是缺陷**，菜单「清空状态会话」可清掉。
- **2026-09-18（同日晚些 · 第三次反馈）**：**判据 3 也拿到证据；用户拍板 `running` 用第 8 行；
  并报告一个真缺陷（气泡跑到屏幕中间）**。

  - **判据 3（`needs-input` 端到端）✅**：`events.jsonl` 里出现两条
    `wb:<会话>` → **`needs-input`**（10:03:38.167 / .682）—— 对应真实回合的 `PermissionRequest`。
    宠物侧确实收到了这个状态（此前只验证了 hook 侧会发）。
  - **用户拍板（取代 2026-09-16 的旧决定）**：*"我认为可以让小厨师的姿态一直保持为
    Agent 在 running 的状态。"* ⇒ **`running` 改挂第 8 行"小厨师"、`ready` 不再占小厨师**。
    后者顺带修掉"没有 agent 在跑、宠物却还在炒菜"。旧决定（"running 保持第 7 行"）被取代。
    两个子问题（`ready` 落到哪、第 7 行生日归谁）并入复核定稿。已列为 §4 第 8/9 条。
  - **真缺陷：气泡出现在屏幕中间**（用户报告）。**代码级根因已定位**：
    `src/host/bubble-layer.ts` 的 `show()` 在从未 `followPet()` 过时执行
    `if (!lastPetBounds) lastPetBounds = win.getContentBounds()` ——
    **把气泡窗口自己的矩形当成了"宠物矩形"**；而 `main/index.ts:420` 调
    `bubble.show(text, badge)` 时**根本没传宠物位置**。启动后第一次推气泡必然走这条兜底
    ⇒ 位置由"气泡窗口自己被创建在哪"决定，于是跑到屏幕中央。
    **与 ADR 017 那条"没有 `ts` 不等于刚刚"是同一类错误：兜底值取了一个"看起来像但不等于"的东西。**
    修法：把宠物矩形显式传进 `show()`；**拿不到时选择"不显示"，而不是显示在错的地方**。
    用户看到的"屏幕正中"与算法反推吻合，但**"为什么恰好在正中"待用最小 Electron 探针实测**，不写进结论。
  - **用户要求下一轮先做「状态↔动作全盘复核」**（原话：*"我感觉现在宠物状态有点乱"*）。
    **任务书已出：`docs/design/m3-state-animation-audit.md`** —— 内含九行素材占用总表（审计基线）、
    **两处已看出的语义挤压**（**第 5 行趴卧被 `blocked`"已受阻"和「打盹」两种相反含义共用**；
    `ready` 是循环态且无出口）、两项待定稿、上述气泡缺陷、以及**"改映射会打破哪些既有断言"的清单**
    （`status-arbiter.test.mjs` 的 `running → running`、`run-status-e2e.mjs` 的绘制行号
    `7/6/0/3→8/5/0`、`m3-behavior` 里与行号/打盹行有关的用例）。
