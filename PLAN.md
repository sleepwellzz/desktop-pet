# 桌面宠物项目 · PLAN（当前状态索引）

> 本文件只回答三件事：**现在到哪了、下一步做什么、细节去哪找**。
> 详细内容一律外迁 `docs/`，本文件保持一屏可读。开工前另读 `AGENTS.md`。
> 最后更新：2026-09-15

## 1. 三句话接续

1. **新建对话**：输入框左下角「选择工作空间」→ 从列表里选 `desktop-pet`。
   该条目已登记在 WorkBuddy 的 `workspaces` 表中（机制见 ADR 006）。
   **不要走"新建默认工作区"**——那会在默认根目录 `...\WorkBuddy\` 下生成带时间戳的子目录，
   工程与记忆都会分散。
2. **开工**：读本文件 + `AGENTS.md`。
3. **收工**：更新本文件 §3 状态表与 §9 变更日志，然后 `git commit`。
4. **启动宠物**（给不熟悉命令行的操作者）：双击工程根目录的 `启动桌宠.bat`，它会自动构建并启动；
   命令行等价写法是 `npm run build && npm start`。批处理必须保持纯 ASCII——`cmd` 按 GBK 读 `.bat`，
   写中文会乱码并破坏语法；且必须 `call npm.cmd`（`call "npm"` 在 Windows 上会失败）。

**两套文件系统**：本地磁盘（本目录，放代码与文档）与项目资产盘 tdrive（云端，放源资产与可分享文档）**不互通**。代码不上传资产盘。

## 2. 项目是什么

解析 Codex / ChatGPT 宠物包（`pet.json` + `spritesheet.webp`），构建自研 **Windows 桌面宠物运行时**，把 agent 的活动状态可视化为一只可交互的桌宠。宠物是**活动状态的可视化代理**，不是电子宠物游戏。

## 3. 当前状态

资产分析、数据契约、窗口层验证（M0）与 **M1 渲染闭环代码** 均已完成。M1 剩余项：人工验收交互手感。

| 里程碑 | 状态 | 说明 |
|---|---|---|
| M0 技术验证 | ✅ 已完成 | 三项判据全过。见 `spikes/m0-window/` 与 ADR 004（其中"逐像素穿透"结论已被 ADR 008 修订） |
| M1 MVP 渲染闭环 | 🟨 缺陷已修并实测，待人工复验 | 6 项判据 4 项通过；全屏让位（ADR 007）与命中区域+窗口漂移（ADR 008）均已修复并由探针实测，缩放改为 0.75。复验清单见 §4 |
| M2 对标交互面 | ⬜ 未开始 | 控制条、快捷键、多会话聚合、宠物管理 |
| M3 差异化 | ⬜ 未开始 | 行为层、状态源生态、宠物包工具链产品化 |

## 4. 下一步：人工复验（三项）

运行方式：工程根目录双击 `启动桌宠.bat`，或 `npm run build && npm start`。

**2026-09-15 人工验收结果**：

| 判据 | 结果 |
|---|---|
| 1 宠物出现、idle 动画在播 | ✅ |
| 2 按住可拖动 | ✅ |
| 3 单击挥手一次后回 idle | ✅ |
| 4 点击宠物四周空白 → 穿透 | ❌ 修复前窗口内 100% 吃点击 → 已重做（ADR 008），探针实测误吃 = 0 |
| 5 全屏应用让位 | ❌ → 已修复（ADR 007），**待复验** |
| 6 挂机内存 | ✅ |

**请复验这三项**：

1. **全屏让位**：打开一个**真全屏**应用（视频全屏按钮或 F11，不是"网页内全屏"），宠物应消失，退出后回来。
2. **命中区域**（ADR 008 重做后的效果）：
   - 沿宠物轮廓走一圈、贴着它但**不碰到**的地方单击 —— 应当**不**触发挥手，点击应落到下层应用；
   - 单击宠物**身体**（含耳朵、尾巴、爪子这些细窄处） —— 应当触发挥手；
   - 按住身体拖动，来回拖十几次 —— 死区**不应**随拖动变大。
3. **缩放**：宠物现在应在 0.75 倍（面积 0.5625 倍），确认观感是否合适。
   若还想调：改 `desktop-pet.json` 的 `render.defaultScale`（0.5 更小 / 1.0 回原样），改完 `npm run build`。

> 测量提示：判断"点击有没有穿透"，**别用桌面空白处**——那里点了本来就没有可见反馈。
> 把记事本或文件资源管理器**最大化**放在宠物下面，再点宠物四周：文字光标/选中反馈能明确告诉你点击落到哪了。

自动化部分已验证：`--screenshot` 自检两帧姿态不同（动画在播）、背景全透明、宠物包校验全过；
命中区域与窗口漂移由 `spikes/m2-hittest/` 的真实点击探针实测（结论见 ADR 008）。

## 5. 信息去哪找

| 想找什么 | 去哪 |
|---|---|
| 完整设计（13 章：选型、架构、状态机、阶段计划、风险、验收） | `Windows桌面宠物开发方案.html` |
| 规格知识（图集规格、行语义、常见坑） | 用户级技能 `codex-pet-pack` |
| 资产实测数据（帧数、锚点、基线） | `docs/asset-facts.md` |
| 为什么这么决定 | `docs/decisions/`（001 选型 / 002 契约与锚点 / 003 目录暂不整理 / 004 穿透方案 / 005 工程结构与构建链 / 006 工作区识别 / 007 M1 验收缺陷修复 / 008 命中区域显式判定与窗口尺寸漂移） |
| 工作区为什么"不在列表里"/为什么落到时间戳目录 | `docs/decisions/006-工作区识别与登记机制.md` |
| 全屏让位/点击命中是怎么判定的、为什么这么修 | `docs/decisions/007-*.md`（全屏）与 `008-*.md`（命中） |
| 命中区域怎么测的、怎么复现 | `spikes/m2-hittest/README.md` |
| 缩放前后对比图（1.0 vs 0.75） | `docs/scale-compare.png` |
| 历史会话做过什么 | `journal/`（按日期追加） |
| 技术验证工程 | `spikes/`（m0-window 窗口层 / m1-shot 自检截图 / **m2-hittest 命中区域与尺寸漂移**） |
| 渲染逻辑参考 | `动画映射验证器.html`（已验证帧率、锚点、状态映射，M1 可直接移植） |
| 窗口层实测结论与踩坑清单 | `spikes/m0-window/README.md`（开工前必读，能省掉一批已知坑） |
| 运行时代码 | `src/`（kernel 内核 / host 宿主 / main 入口 / renderer 渲染），构建链见 ADR 005 |
| 构建/启动命令 | `npm run build` / `npm start`；Bash 环境里没有 npm，用 `node tools/npm-run.mjs build` |

## 6. 锁定决策摘要（详见 ADR）

1. **Electron 先行 + 内核纯 TS 解耦**，保留 Tauri 2 迁移路径。（ADR 001）
2. **不污染 `pet.json`**，自研参数走 sidecar。（ADR 002）
3. **行级固定锚点** `groundY = 202`，禁止逐帧基线锁定。（ADR 002）
4. **根目录暂不整理**，因存在相对引用网。（ADR 003）
5. ~~点击穿透用 `WM_NCHITTEST` 采样 alpha~~（ADR 004 作废）→ ~~`WS_EX_LAYERED` 原生命中测试，
   alpha=0 自动穿透~~（**ADR 008 作废**：实测生效命中区域是整个窗口矩形，与像素 alpha 无关）
   → **命中判定改由渲染层按当前帧精灵 alpha 显式给出，窗口常态整窗穿透**
   （`setIgnoreMouseEvents(true,{forward:true})` + 16ms 光标轮询）。（ADR 008）
6. **位移速度按屏幕像素/秒归一化**（默认缩放 ≈96 px/s；缩放已改为 0.75）。
7. **渲染层与 preload 用 esbuild 打包**，内核纯 TS 不 import electron，init 走"渲染层报到 → 主进程下发"握手。（ADR 005）
8. **命中区域 = 精灵轮廓（单点判定，不做邻域膨胀）**；窗口移动一律用 `setContentBounds`
   并复位内容尺寸，防止 150% 缩放下 DIP↔物理取整误差累积。（ADR 008）

## 7. 目录地图

```
desktop-pet/
├── PLAN.md / AGENTS.md        入口与约定
├── package.json / tsconfig.json   构建入口（npm run build / npm start）
├── Windows桌面宠物开发方案.html  完整设计
├── 动画映射验证器.html / pet-spec.html
├── pet.json / spritesheet.webp / *.json   数据契约与资产（引用网，勿移动，见 ADR 003）
├── anchor-report.* / atlas-map.png
├── src/                       运行时代码（kernel / host / main / renderer / shared）
├── dist/                      构建产物（git 忽略）
├── docs/
│   ├── asset-facts.md         实测事实
│   ├── decisions/             ADR：一决策一文件，只增不改
│   └── research/              调研产出
├── journal/                   会话日志，按日期追加
├── spikes/                    技术验证工程，可丢弃但结论要回写
├── tools/                     分析、校验、模板、构建辅助脚本
├── examples/                  demo-pet（合法）/ broken-pet（非法，回归用）
└── assets/                    模板底稿 v1/v2
```

## 8. 关键风险（详见方案 §12）

| 风险 | 等级 | 缓解 |
|---|---|---|
| 逐像素穿透难实现 | 致命 | ~~M0 优先验证~~ 已解除：Electron 透明窗口原生支持，实测通过（ADR 004） |
| 无真实状态源，产品空转 | 致命 | M1 起把状态源抽象成事件流；尽早接一个真实 hook |
| 被误判为流氓软件 | 高 | 全屏让位 + 不夺焦点 + 托盘可退出，硬性验收项 |
| 全局快捷键被占用 | 中 | 注册失败即提示 + 自动降级 + 可自定义 |
| 恶意宠物包 | 高 | 路径穿越 → 魔数 → ≤20 MiB → 尺寸匹配 → 字段白名单 |

## 9. 变更日志

- **2026-09-15**：合并两条并行会话产物为统一工程；建立本文件为单一事实来源。原会话目录保留只读。
- **2026-09-15**：补建跨会话接续三层机制（用户级 `~/.workbuddy/MEMORY.md` 项目索引 + 工程 `AGENTS.md` + 本文件 §1）。
- **2026-09-15**：建立工程管理体系——`docs/`（asset-facts + decisions + research）、`journal/`、`spikes/`、`.gitignore`；初始化 git 并首次提交。本文件重构为索引式，详细内容外迁。
- **2026-09-15**：M0 窗口层技术验证完成（`spikes/m0-window/`）。三项判据全过：逐像素穿透（跨进程）、`WS_EX_NOACTIVATE` 不夺焦点、全屏让位。**修订 ADR 001 第 5 条**——改用分层窗口原生命中测试，作废 WM_NCHITTEST 采样与掩码回传方案（ADR 004）。Electron 选型确认保留，进入 M1。
- **2026-09-15**：排查并解决"工作区不出现在选择列表 / 新建对话落到时间戳目录"问题。定位到真实机制：
  列表源为 `workbuddy.db` 的 `workspaces` 表 ∪ 会话历史路径（**不是** `~/.workbuddy/projects/` 索引目录，
  更正 journal 前次判断）；时间戳目录来自 `WorkspaceDomain.generate()` 在默认根目录下建子目录。
  结论：**不搬家**、不改 `defaultWorkspacePath`，新建对话时从列表选 `desktop-pet` 条目。见 ADR 006。
- **2026-09-15**：M1 渲染闭环代码完成（`src/`，工程结构见 ADR 005）。宠物包加载与校验（路径穿越/魔数/尺寸/锚点自洽/帧列连续）、状态机播放器、行级锚点绘制、拖拽、单击挥手、全屏让位均已实现；`--screenshot` 自检两帧姿态不同、画面干净。剩余：Justin 人工验收交互手感（清单见 §4）。
- **2026-09-15**：排查并解决工作区识别问题（ADR 006 + journal 更正）。工作区列表数据源是 `workbuddy.db` 的 `workspaces` 表，不是 `~/.workbuddy/projects/` 索引目录；带时间戳目录来自 `WorkspaceDomain.generate()`。结论：不搬家。
- **2026-09-15**：M1 人工验收完成，修掉两项缺陷（ADR 007）——① 全屏让位完全失效：`GetWindowRect` 的 koffi 结构体未回写导致判据恒 false，改用 Buffer 解码并修正 `MonitorFromWindow` 标志；② 命中精度：规格里的 `hitTestAlphaThreshold` 从未接线，已在渲染层实现（清零 alpha<16 的像素，实测清理 1 万+像素并与图像统计互相印证）。同时得出负面结论：`WindowFromPoint` 不能用于验证分层窗口的 alpha 穿透，相关探针已删除。待复验两项，见 §4。
- **2026-09-15**：第二轮人工复验（反馈：穿透区域不稳定、拖动后死区变大、挥手触发左右偏宽、宠物偏大）。搭真实点击探针 `spikes/m2-hittest/` 逐点实测 169 个采样点，得出两条硬结论：**① 生效命中区域 = 整个窗口矩形**（窗口内 90/90 采样点全被吃掉，与像素 alpha 无关——M0"逐像素穿透是原生行为"的判断被推翻，M0 只验证过窗口外的穿透）；**② 窗口每 `setPosition` 一次长 1 DIP**（150% 缩放下 DIP↔物理取整误差累积），这正是"拖动后死区越变越大"的机制。据此重做命中方案：窗口常态整窗穿透 + 渲染层按当前帧精灵 alpha 显式判定（16ms 光标轮询驱动，因为实测 `forward:true` 的鼠标移动转发不生效），拖动改 `setContentBounds` 消除漂移，`defaultScale` 1.0→0.75（面积 0.5625 倍）。复测：误吃 43→**0**，拖动前后命中图逐点一致，窗口尺寸不再漂移。见 ADR 008 与 `journal/2026-09-15-M1命中区域重做.md`。新增 `tools/npm-run.mjs`（Bash 环境无 npm）。
